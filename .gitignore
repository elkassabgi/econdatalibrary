# Data staging (large; never commit)
data/
*.part
*.zip
*.parquet

# Logs & runtime
*.log
runlogs/
__pycache__/
*.pyc
.pytest_cache/

# Secrets
.env*
*.key
*.pem
.dev.vars
*.dev.vars

# Heavy artifacts (live in R2/D1, not git)
dist/
db/
logs/
emails/
_raw_insider_zips/
_raw_insider_extract/
*.db
_backfill_*
_series_census*
_supported_sources_ts.txt
_scratch_*.py
_verify_*.py
_target_schema.json

# Node
node_modules/

# Desktop/session-local ops (Windows watchdogs, not part of the product)
.claude/
RELAUNCH_*.ps1
EconGuard.cmd

# Local notes
scratch/

# MCP registry DNS-auth private seed (Ed25519) — never commit
mcp/dns_key.hex

# ---------------------------------------------------------------------------
# PRIVATE — never commit. Owner-only working material. This repo is PUBLIC.
# Third-party correspondence (verbatim private replies from named people at
# other organisations, incl. refusals, internal markings and quoted fees),
# internal audit/session notes, credential-location maps, and live-security
# writeups. Publishing any of these breaches counterparties' confidence or
# hands out an attack map.
# ---------------------------------------------------------------------------
REDISTRIBUTION_EMAIL_TRAIL.md
REDISTRIBUTION_COMPLIANCE.md
PERMISSION_EMAIL_DRAFTS.md
THIRD_PARTY_CARVEOUTS.md
emails/
AUTH_SSO_PLAN.md
AUTH_SSO_BUILD_LOG.md
AUTH_SSO_FRONTEND_ROLLOUT.md
AUTH_SSO_HANDOFF.md
DEPLOY_LOCK_SSO.md
PHASE3_BUILD_BRIEF_3.2_3.3.md
RESUME_DB_DOC_TASK.md
docs/DATABASE_REFERENCE.md
CUTOVER_AND_BACKFILL_PLAN.md
STRATEGY.md
STATUS.md
SESSION_LOG_*.md
OVERSIGHT_REPORT_*.md
R2_SETUP.md
mcp/MCP_REGISTRATION_PLAYBOOK.md
.session-catchup/

# Stale pre-purge catalog artifacts (generated 2026-06-24, regenerable via
# catalog/gen_catalog.py). They enumerate 29 GATED sources with observation
# counts — incl. fred=48,048,976 and famafrench=238,797 — i.e. a public,
# machine-readable claim to hold data we were never licensed to redistribute.
# The LIVE site (catalog/site/*.html) is clean and unaffected.
catalog/catalog.json
catalog/catalog.html
# Credential-location maps (which key lives in which file on the owner's disk)
# plus hundreds of absolute D:/ paths.
UPDATE_CAPABILITY_MATRIX.json
UPDATE_CAPABILITY_MATRIX.md
# Untracked-but-unignored: a single `git add -A` would have committed these.
# .wrangler holds wrangler-account.json with a personal email address.
.wrangler/
_rollout_backups/
_updater_proof/
mcp/mcp-publisher.exe
mcp/mcp-publisher.tar.gz
dbnomics_license_manifest.json

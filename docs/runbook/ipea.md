# ipea

*Generated by `tools/gen_runbook.py` on 2026-08-06 22:53 UTC — every fact below is read from the system. Re-run the tool to refresh; do not hand-edit.*

## 1. At a glance

| | |
|---|---|
| in registry | yes |
| live (scheduled) | True |
| served to users | yes |
| catalogued series | 1,241 |
| strategy | `extend_by_date` |
| check cadence | monthly |
| runs where | cloud (GitHub Actions) |
| refresh cost | medium |
| store dir | `data/clean_full/ipea/` |

## 2. Current state (from `data/_aqueduct/state.db`)

- **unit `_all`** — status `partial`
  - last SUCCESS: `NEVER`  ·  last attempt: `2026-07-31T23:38:31+00:00`
  - obs_count: `666954`  ·  last_obs_date: `2070-01-01`  ·  stored vintage: `—`
  - last error:
    ```
    298/1491 sub-unit(s) transient-failed; will retry [IFS_PETROLEUM, IFS_SOJAGP, IFS12_BEEFB12,
    IFS12_MAIZE12, IFS12_PETROLEUM12, IFS12_SOJAGP12, +292 more]
    ```

> `obs_count` is **not comparable across runs** — most fetchers report rows merged this run, thirteen fall back to the whole store when nothing was written. To ask how big this source is, count the store. See ledger R326 and the comment in `updater/state.py`.

> A `partial` NEVER sets `last_success_utc` (R231). A source reading `last SUCCESS: NEVER` may be ingesting millions of rows perfectly well and failing one sub-unit. Check `obs_count` and the run history before concluding it is dead.

> **A FUTURE `last_obs_date` IS USUALLY CORRECT, NOT A BUG.** Many publishers ship genuine PROJECTIONS: CSO Ireland's `18_Population_Projections` runs to 2057, Estonia's `rahvastik` to 2085, ABS to 2046/2071, UN WPP to 2101, IMF WEO to 2031. `health.py` already separates the two — `frontier` is the furthest period held INCLUDING projections (display only), `newest_obs` filters to periods <= today and is the recency signal — because 28 of 93 units legitimately reported a future frontier. Do NOT "repair" a future date until you have looked at the series key and the year sequence.
>
> What IS a defect: a SENTINEL (9999-12-31, 2999-12-31) or a COUNTER read as a year (years 1, 2, 3 ... contiguous from 1 — no projection starts at year 1). Tell them apart by the key and the sequence, never by the size of the number. See R320/R322 and `tools/audit_impossible_dates.py`, whose bounds (before 1500, after 2200) are deliberately far outside every real projection horizon.

### Recent runs

| when (UTC) | status | obs | dur | note |
|---|---|---|---|---|
| 2026-07-31T23:38:31 | partial | 666954 | 909.3s | 298/1491 sub-unit(s) transient-failed; will retry [IFS_PETROLEUM, IFS_SOJAGP, IFS12_BEEFB12, IFS12_MAIZE12, IF |

## 3. How it works

**Why this strategy** (registry `strategy_reason`):

> CORRECTED 2026-07-29: IPEA OData4 does NOT honour a server-side date filter. Probed with raw $filter, URL-encoded %24filter, and cutoffs 2020-01-01 and 2026-01-01 — every variant returns HTTP 200 and the FULL series (68/68 obs on ABATE_ABPEAV). The API accepts the query string and ignores it: a silent no-op. The fetcher therefore pulls each due series whole and relies on merge dedup on (series_key, obs_date). The defect it fixes is real and unchanged — the ingest SKIPS any series already on disk, so existing series never extend and only brand-new codes are ever picked up.

**Adapter contract** (registry `adapter`):

- `vintage_signal`: Date-tail per series: compute max(obs_date) per series_key from existing parquet and request ValoresSerie with $filter=VALDATA gt {max}. New series picked up from the freshly fetched Metadados active-series list (SERSTATUS='A'). No single upstream manifest/last-modified.
- `since_param`: $filter=VALDATA gt {max(obs_date) per series_key} on the IPEA OData4 ValoresSerie endpoint.
- `out_paths_note`: Single file: data/clean_full/ipea/ipea.parquet (~622k rows; schema series_key,obs_date,value; series_key = SERCODIGO or SERCODIGO:TERCODIGO). Dedup on (series_key,obs_date). CRITICAL: current checkpoint writes pq.write_table directly over the file (no .tmp/os.replace) -- add atomic write before enabling incremental to avoid corrupting the single output.
- `rate_note`: No API key (public ipeadata.gov.br OData4, plain http://). RATE 0.3s between series GETs; HTTP 429 -> 60s sleep; 3 retries.

**Fetcher module**: [`updater/strategies/fetchers/ipea.py`](../../updater/strategies/fetchers/ipea.py)

<details><summary>Its own module docstring — the authoritative description, including every defect previously found and why the code looks the way it does</summary>

```
S3 date-tail fetcher — IPEA (Instituto de Pesquisa Econômica Aplicada, Brazil).

API: OData v4 at http://www.ipeadata.gov.br/api/odata4 — no key.
  catalog  GET /Metadados                      -> every series + SERSTATUS + SERNUMERICA
  values   GET /ValoresSerie(SERCODIGO='X')    -> VALDATA / VALVALOR observations
series_key IS the SERCODIGO, exactly as jobs/ingest_ipea.py writes it.

WHY A DATE-TAIL FETCHER AND NOT A WRAPPER. The first-pass ingest resumes by SKIPPING any
series it already holds:

    done   = set(tbl.column("series_key").to_pylist())
    to_do  = [s for s in active if s["SERCODIGO"] not in done]

That is correct for a backfill and inert as an updater: a series already on disk is never
looked at again, so IPEA can publish ten more years of a series and we would never fetch one
of them. Running that on a schedule would only ever pick up BRAND-NEW series codes — the
green run would assert currency it had not checked.

THE SERVER-SIDE DATE FILTER DOES NOT WORK — TESTED, NOT ASSUMED. The registry's
strategy_reason claims "IPEA OData4 ValoresSerie accepts a server-side date filter
($filter=VALDATA gt {date})". It does not. Probed with raw `$filter`, URL-encoded `%24filter`,
and cutoffs of 2020-01-01 and 2026-01-01: every variant returns HTTP 200 and the FULL series
(68 of 68 observations on ABATE_ABPEAV). The API accepts the query string and ignores the
filter, which is the worst failure mode — a silent no-op that looks like it worked.

So no filter is sent. Each due series is fetched whole and `merge_and_write` dedups on
(series_key, obs_date); existing series still EXTEND, the run just carries more bytes than a
real date-tail would. That is affordable here — ~1,500 series of modest history — and it is
honest, whereas sending a filter the server discards would leave the next reader believing the
transfer was narrowed.

VINTAGE. `current_vintage` hashes the catalogue's active series codes. That moves when IPEA
adds or retires a series, which is a real change worth a run — but it deliberately does NOT
try to encode "some series got new observations", because IPEA exposes no catalogue-level
timestamp for that. Returning a token that cannot see the common case would be worse than
returning one that only sees additions: the strategy falls back to the cadence, which is
monthly here, and each series is a single small call.

BUDGET. One HTTP call per due series, so a full sweep is ~1,200 calls at RATE. A wall-clock
budget stops cleanly and defers the rest to the next tick rather than being killed at the job
ceiling; deferred series are untouched, not failed.

HONEST-STATUS: catalogue unreachable -> TransientError (partial, retried, data kept). A
per-series fetch/parse failure -> transient_unit for that series only. A series that returns
no usable observations is NOT an error — it is the normal steady state and is counted as
unchanged, never as empty.
```

</details>

**Ingest script(s)**: `jobs/ingest_ipea.py`

## 4. If it stops updating — diagnose in this order

Run everything from `E:\research\econfindatalibrary`.

```bash
# 1. What does the system actually say? (not what you remember)
python -c "import sys,sqlite3;sys.path.insert(0,'.');from updater import config;con=sqlite3.connect(f'file:{config.STATE_DB}?mode=ro',uri=True);print(*con.execute(\"SELECT unit_id,status,last_success_utc,last_attempt_utc,last_error FROM unit_state WHERE source_id='ipea'\"),sep=chr(10))"

# 2. Is it even being ATTEMPTED? 'scheduled' is not 'attempted' (R246).
#    A daily run reaches ~20 of ~106 cloud sources; most are skipped on cadence, which is normal.
python -m updater.run --source ipea --dry

# 3. Run it for real and READ THE REASON. Failures name themselves now.
AQUEDUCT_BACKEND=r2 python -u -m updater.run --source ipea --force

# 4. Is the PUBLISHER healthy, or is it us? Probe upstream directly, never a relay.
#    (DBnomics is BANNED — every source must be reached at its own publisher.)

# 5. Is the store intact? obs_count in state is NOT the answer.
AQUEDUCT_BACKEND=r2 python -c "import sys,os;sys.path.insert(0,'.');from updater import config,blob;d=config.source_dir('ipea');fs=[f for f in blob.list_parquets(d) if not os.path.basename(f).startswith('_')];print(len(fs),'files',sum(blob.row_count(os.path.join(d,os.path.basename(f))) for f in fs),'rows')"

# 6. Are its dates real? A counter-as-year or a 9999 sentinel is silent (7 sources hit).
#    NOTE: a FUTURE date is usually a legitimate projection - check the key before repairing.
python tools/audit_impossible_dates.py --r2 --source ipea
```

**Read the failure class before fixing anything.** From the 2026-08-04 audit of every unfetchable source, the causes were: `budget_deferral` (NOT broken — ran out of its time slice), `code_bug`, `rate_limited`, `gated_by_design`. **Zero** were an expired credential or a dead endpoint, although that is the usual first guess.

If the error names sub-units as `deferred (budget N min)` or `budget spent`, **nothing has failed** — the source ran out of its slice and the rest is taken next tick (R303). Do not 'fix' it.

## 4b. Root cause when this was last investigated (2026-08-04)

*From the unfetchable-source audit — 29 agents, every finding challenged by a skeptic before it was accepted. This source was covered by the target `ipea`.*

- **root cause**: `budget_deferral`  (confidence high)
- **which units**: 0 of 1,192 attempted sub-units failed. The 298 "transient-failed" sub-units were NEVER ATTEMPTED — they are the un-reached tail of the catalogue after the 15-minute wall-clock budget bound. Publisher order proves it exactly: today's /Metadados returns 1,490 active+numeric series; 1490 - 298 = 1192, and IFS_PETROLEUM sits at index 1192, IFS_SOJAGP 1193, IFS12_BEEFB12 1194, IFS12_MAIZE12 1195, IFS12_PETROLEUM12 1196, IFS12_SOJAGP12 1197 — the six named "offenders" are literally the first six items of the deferred tail, consecutive, in catalogue order. That is the signature of `if dl.spent(): tally.transient_unit(code)` over a stable list, not of a scattered failure set.
- **blast radius**: 298 of 1,490 active series (20%) went un-refreshed on the 2026-07-31 tick; 1,192 (80%) were fetched. Nothing was lost — deferred series are untouched, and rotate_after/save_rotation (updater/strategies/fetchers/ipea.py:139-140, 219-220) resume the next tick after the last one started, so the worst-case refresh gap for any series is 2 monthly ticks (cadence: monthly, per updater/registry.yaml). Measured staleness inside the deferred tail is 1-2 observations per series. One genuinely separate gap surfaced while measuring: the local store holds 1,241 distinct series (data/clean_full/ipea/ipea.parquet, 621,981 rows) against 1,490 active in the live catalogue — ~249 active codes added since the June first-pass ingest have never been fetched; the rotating sweep does pick them up as it walks, but only at the same 80%-per-tick rate.
- fixable by us: **True**

**Recommended fix, in order:**

> Nothing to fix upstream and nothing to fix in the tally — the mislabel was already corrected by commit e88178cb (transient_unit -> deferred_unit at the `dl.spent()` branch). Two residual actions, both ours: (1) The state row is stale and self-clearing only on the next run, which at monthly cadence is ~2026-08-31; until then dashboards will keep showing "298 transient-failed" and last_success=NULL for a healthy source. Either force one ipea tick now or accept the stale row knowingly. (2) The budget is genuinely undersized for a full sweep: 1,490 series at the measured 0.75s each needs ~18.6 min against BUDGET_MIN=15, so ipea will defer ~20% EVERY tick indefinitely, by arithmetic and not by accident. Raise BUDGET_MIN in updater/strategies/fetchers/ipea.py to ~25, or route ipea to run_location: local where tools/run_local_heavy.ps1 sets AQUEDUCT_BUDGET_MIN_OVERRIDE — either makes a single tick cover the whole catalogue and closes the 249 never-fetched new codes in one pass. Note that even after the tally fix, status stays `partial` and last_success stays NULL on any deferring tick (by the R231 contract), so raising the budget is what actually turns this source green.

<details><summary>Evidence produced during that investigation — HTTP codes, file:line, measured counts. Re-verify before acting: it was true on 2026-08-04.</summary>

- updater state (E:\research\econfindatalibrary\data\_aqueduct\state.db, unit_state ipea/_all): status='partial', last_success_utc=None, last_attempt_utc='2026-07-31T23:38:31+00:00', last_error='298/1491 sub-unit(s) transient-failed; will retry [IFS_PETROLEUM, IFS_SOJAGP, IFS12_BEEFB12, IFS12_MAIZE12, IFS12_PETROLEUM12, IFS12_SOJAGP12, +292 more]'
- runs table row id=361 for that same attempt: dur_s=909.3. BUDGET_MIN=15 at updater/strategies/fetchers/ipea.py:67 => 900.0s. The run stopped 9.3s past its budget, i.e. the deadline bound exactly; 1,192 series in 900s = 0.75s each, consistent with RATE=0.15 plus a ~0.6s round trip.
- updater/strategies/fetchers/ipea.py:145-148 TODAY reads `if dl.spent(): # Deferral, not a verdict ... tally.deferred_unit(code)`. `git show e88178cb -- updater/strategies/fetchers/ipea.py` shows that exact line was `- tally.transient_unit(code)` / `+ tally.deferred_unit(code)`.
- That fix commit is e88178cb7206ad65c256c3a3f6048e45cdb410dd, dated 2026-08-03 10:26:02 -0500 — ~2.5 days AFTER the 2026-07-31T23:38Z attempt the state row records. Its message names this source by number: "ipea 298/1491 ... all of which were deferrals". The state row is stale output of already-corrected code (R304 check applied: fix postdates attempt).
- LIVE publisher GET http://www.ipeadata.gov.br/api/odata4/Metadados -> HTTP 200 in 3.3s, Content-Type 'application/json; odata.metadata=minimal', 7,327,240 bytes, no Retry-After; 3,584 rows total, 1,490 pass the fetcher's exact SERSTATUS=='A' and SERNUMERICA filter.
- LIVE GET /ValoresSerie(SERCODIGO='X') for all four named codes: IFS_PETROLEUM HTTP 200, 74 obs; IFS_SOJAGP HTTP 200, 69 obs; IFS12_BEEFB12 HTTP 200, 833 obs; IFS12_MAIZE12 HTTP 200, 833 obs. No Retry-After header on any. No auth is used or required by this API.
- CONTROL: ABATE_ABPEAV (index 0, inside the attempted prefix, the known-good series named in the fetcher docstring) probed with the identical call -> HTTP 200, 68 obs. Identical behaviour to the four 'failed' codes, so the probe is sound and the named codes are in no way special.
- NO schema drift: obs[0] of IFS_PETROLEUM = {'SERCODIGO': 'IFS_PETROLEUM', 'VALDATA': '1952-01-01T00:00:00-02:00', 'VALVALOR': 1.92, 'NIVNOME': '', 'TERCODIGO': ''}; key set ['NIVNOME','SERCODIGO','TERCODIGO','VALDATA','VALVALOR'] — the fields ipea.py:177-179 reads. The PRODUCTION parser jobs/ingest_ipea.parse_ipea_date accepted 74/74 observations, max parsed date 2025-01-01.
- The deferral does carry a real (small) cost, measured: IFS_PETROLEUM local max obs_date 2024-01-01 vs live 2025-01-01 (1 new annual obs); IFS12_BEEFB12 and IFS12_MAIZE12 hold 831 local rows each vs 833 live (2 new monthly obs each).
- PREMISE CORRECTION on the count: the state row says 1491, today's live catalogue yields 1,490 active+numeric. One series left the active/numeric set between 2026-07-31 and 2026-08-03. Immaterial to the verdict, but the brief's '1491' is no longer the live figure.
- Not a data-quality defect either: the state row's odd last_obs_date='2070-01-01' traces to IPEA's own population-projection series DEPIS_POPHP / DEPIS_POPMP / DEPIS_POPP in the local parquet — legitimate projections, not a parse bug.

</details>

## 5. Everything that has already gone wrong here

1 ledger entry mention this source. Read them in `D:\research\hfdatalibrary\.claude\MISTAKES.md` before changing anything — several describe a fix that was tried and was WRONG.

- **R306** — I derived a class from the label text and shipped half of it as "the fix"

## 6. Rules that apply to every repair here

- **A key/parser change is a RE-PULL, never a merge** (R22). Changing what `series_key` means leaves the old and new grains coexisting; dedup cannot collapse them because they no longer collide. Use `mode="replace"`, or re-pull the table.
- **Never patch dates — re-pull** (R288). A naive date repair on `cso` would have destroyed 11 of 12 rows; only a negative control caught it. And 41,091 of `stat_slovenia`'s fabricated dates land in 1900–2200, indistinguishable from real data, so no date test can find them.
- **Verify against the store you are actually serving** (R296/R36). Under `AQUEDUCT_BACKEND=r2` the local directory is a scratch mirror of the last run only; a local check can under-report or give the opposite answer.
- **When a probe reports ABSENCE, run it against something known PRESENT** (R316). An absence looks identical whether the data is missing or the probe is broken.
- **Compare (key, date) pairs, not just keys** (R314). A re-key check that compares identifiers verifies the half you changed on purpose; the half you changed by accident is in the values.
- **A bound over a fixed order is a truncation, not a budget** (R190). If work is capped, check it ROTATES — and specifically that it rotates when the item at the head never completes (R313).
- **Read the job's ARGV, not its progress** (R323). A `--dry-run` and a real run print the same numbers.


# insee_bdm

*Generated by `tools/gen_runbook.py` on 2026-08-06 23:41 UTC — every fact below is read from the system. Re-run the tool to refresh; do not hand-edit.*

## 1. At a glance

| | |
|---|---|
| in registry | yes |
| live (scheduled) | True |
| served to users | yes |
| catalogued series | 101,848 |
| strategy | `sdmx_delta` |
| check cadence | monthly |
| runs where | cloud (GitHub Actions) |
| refresh cost | medium |
| store dir | `data/clean_full/insee_bdm/` |

## 2. Current state (from `data/_aqueduct/state.db`)

- **unit `_all`** — status `partial`
  - last SUCCESS: `NEVER`  ·  last attempt: `2026-08-06T00:58:23+00:00`
  - obs_count: `10829157`  ·  last_obs_date: `2026-12-31`  ·  stored vintage: `—`
  - last error:
    ```
    +254629 new rows; csv_derive failed 43354/77501 series [insee_bdm:001762873,
    insee_bdm:001762874, insee_bdm:001762875, insee_bdm:001762876, insee_bdm:001762877, +43349 more]
    ```

> `obs_count` is **not comparable across runs** — most fetchers report rows merged this run, thirteen fall back to the whole store when nothing was written. To ask how big this source is, count the store. See ledger R326 and the comment in `updater/state.py`.

> A `partial` NEVER sets `last_success_utc` (R231). A source reading `last SUCCESS: NEVER` may be ingesting millions of rows perfectly well and failing one sub-unit. Check `obs_count` and the run history before concluding it is dead.

> **A FUTURE `last_obs_date` IS USUALLY CORRECT, NOT A BUG.** Many publishers ship genuine PROJECTIONS: CSO Ireland's `18_Population_Projections` runs to 2057, Estonia's `rahvastik` to 2085, ABS to 2046/2071, UN WPP to 2101, IMF WEO to 2031. `health.py` already separates the two — `frontier` is the furthest period held INCLUDING projections (display only), `newest_obs` filters to periods <= today and is the recency signal — because 28 of 93 units legitimately reported a future frontier. Do NOT "repair" a future date until you have looked at the series key and the year sequence.
>
> What IS a defect: a SENTINEL (9999-12-31, 2999-12-31) or a COUNTER read as a year (years 1, 2, 3 ... contiguous from 1 — no projection starts at year 1). Tell them apart by the key and the sequence, never by the size of the number. See R320/R322 and `tools/audit_impossible_dates.py`, whose bounds (before 1500, after 2200) are deliberately far outside every real projection horizon.

### Recent runs

| when (UTC) | status | obs | dur | note |
|---|---|---|---|---|
| 2026-08-06T00:58:23 | partial | 10829157 | 3407.0s | +254629 new rows; csv_derive failed 43354/77501 series [insee_bdm:001762873, insee_bdm:001762874, insee_bdm:00 |
| 2026-07-31T00:59:33 | partial | 10823103 | 6272.0s | 201/201 sub-unit(s) transient-failed; will retry |
| 2026-07-16T10:51:33 | partial | 10816347 | 666.7s | +241903 new rows; csv coherence unmet: 201 changed series_keys have no catalog mapping for insee_bdm and the s |
| 2026-07-14T06:19:48 | partial | 0 | 0.0s | insee_bdm source dir missing: /home/runner/work/econdatalibrary/econdatalibrary/data/clean_full/insee_bdm |

## 3. How it works

**Why this strategy** (registry `strategy_reason`):

> INSEE BDM is SDMX 2.1 (api.insee.fr/series/BDM/V1) and the profile confirms data/{FLOW_ID} accepts updatedAfter + startPeriod/lastNObservations; pull only obs newer than last_obs per dataflow and merge.

**Adapter contract** (registry `adapter`):

- `vintage_signal`: Per-dataflow change detection via GET data/{FLOW_ID}?updatedAfter=<last_success_timestamp>; enumerate the ~243 flow ids from the dataflow/all/all/latest catalog. No global manifest.
- `since_param`: startPeriod=<max(obs_date)+1 period> on data/{FLOW_ID} (use updatedAfter=<last_success ISO datetime> to gate which flows changed).
- `out_paths_note`: Multi-file: one parquet per dataflow at data/clean_full/insee_bdm/{FLOW_ID}.parquet (~201-243 files). Merge per flow by read+concat+dedup on (idbank,obs_date); add .tmp staging since current writer has no atomic write.
- `rate_note`: No API key (public, Accept: application/xml). 0.5s between flows; HTTP 429 -> 30s*(attempt+1) backoff. No declared per-minute cap.

**Fetcher module**: [`updater/strategies/fetchers/insee_bdm.py`](../../updater/strategies/fetchers/insee_bdm.py)

<details><summary>Its own module docstring — the authoritative description, including every defect previously found and why the code looks the way it does</summary>

```
S3 fetcher — INSEE BDM (Banque de Donnees Macroeconomiques). SDMX 2.1, no key.

Layout (set by jobs/ingest_insee_bdm.py): ONE parquet per DATAFLOW under
clean_full/insee_bdm/<FLOW_ID>.parquet. Each file holds that flow's full long table:
  idbank   : the BDM series id (a flow contains many idbanks)
  obs_date : date32, parsed from SDMX TIME_PERIOD (M=YYYY-MM->1st, T=YYYY-Qn->1st of
             quarter, A=YYYY->Dec-31, daily=YYYY-MM-DD)
  value    : float64 (OBS_VALUE)
  dataflow : the flow id (constant within a file)
The natural identity within a flow is (idbank, obs_date) — that is the dedup key.

Incremental (sdmx_delta / date-tail). Each on-disk dataflow file is a SUB-UNIT:
  - read the file's max(obs_date) and request ONLY the year-tail from upstream via the
    SDMX 2.1 query  GET /data/<FLOW_ID>?startPeriod=<year-of-max>
    startPeriod with a bare year matches January of that year onward and works
    UNIFORMLY for monthly/quarterly/annual series (verified live), so we re-fetch at
    most ~1 year of overlap per flow — cheap — and, because it RE-fetches the boundary
    period, an in-place revision of the latest value is also captured (merge dedups the
    overlap, new row wins). No per-series FREQ detection is needed.
  - parse with the SAME logic as jobs/ingest_insee_bdm.py (Series IDBANK attr, Obs
    TIME_PERIOD / OBS_VALUE, parse_period) — enumeration + endpoints reused verbatim.
  - merge ONLY via merge.merge_and_write(path, tbl, mode="merge",
    dedup_keys=("idbank","obs_date")); never write parquet here.

INSEE BDM does NOT reliably honor ?updatedAfter (it returns the full series), so we do
not depend on it; the year-tail startPeriod is the cheap delta primitive.

HONEST-STATUS CONTRACT (Tally + finalize):
  Per dataflow sub-unit we record:
    added_unit(n)    rows merged for the flow (n>0 new, n==0 empty)
    empty_unit()     200 with a valid SDMX envelope but no new Obs in the tail window
                     (a legitimately-quiet flow), or no on-disk obs to anchor from
    transient_unit() timeout/5xx/429/network drop, OR a 200 whose body is not valid XML
                     (retry next run) -> the WHOLE run returns 'partial'
    structural_unit()200 whose body parses as XML but is NOT an SDMX data message (the
                     expected StructureSpecificData/GenericData root is gone), i.e. a
                     real structural break -> DefinitiveError in finalize()
  finalize() => 'ok'/'no_change' only when no sub-unit transient/structural-failed;
  'partial' on any transient (orchestrator does NOT stamp success, re-runs next tick);
  DefinitiveError on a structural break or a large all-empty/error window. merge always
  preserves existing data (never shrinks).
```

</details>

**Ingest script(s)**: `jobs/ingest_insee_bdm.py`

## 4. If it stops updating — diagnose in this order

Run everything from `E:\research\econfindatalibrary`.

```bash
# 1. What does the system actually say? (not what you remember)
python -c "import sys,sqlite3;sys.path.insert(0,'.');from updater import config;con=sqlite3.connect(f'file:{config.STATE_DB}?mode=ro',uri=True);print(*con.execute(\"SELECT unit_id,status,last_success_utc,last_attempt_utc,last_error FROM unit_state WHERE source_id='insee_bdm'\"),sep=chr(10))"

# 2. Is it even being ATTEMPTED? 'scheduled' is not 'attempted' (R246).
#    A daily run reaches ~20 of ~106 cloud sources; most are skipped on cadence, which is normal.
python -m updater.run --source insee_bdm --dry

# 3. Run it for real and READ THE REASON. Failures name themselves now.
AQUEDUCT_BACKEND=r2 python -u -m updater.run --source insee_bdm --force

# 4. Is the PUBLISHER healthy, or is it us? Probe upstream directly, never a relay.
#    (DBnomics is BANNED — every source must be reached at its own publisher.)

# 5. Is the store intact? obs_count in state is NOT the answer.
AQUEDUCT_BACKEND=r2 python -c "import sys,os;sys.path.insert(0,'.');from updater import config,blob;d=config.source_dir('insee_bdm');fs=[f for f in blob.list_parquets(d) if not os.path.basename(f).startswith('_')];print(len(fs),'files',sum(blob.row_count(os.path.join(d,os.path.basename(f))) for f in fs),'rows')"

# 6. Are its dates real? A counter-as-year or a 9999 sentinel is silent (7 sources hit).
#    NOTE: a FUTURE date is usually a legitimate projection - check the key before repairing.
python tools/audit_impossible_dates.py --r2 --source insee_bdm
```

**Read the failure class before fixing anything.** From the 2026-08-04 audit of every unfetchable source, the causes were: `budget_deferral` (NOT broken — ran out of its time slice), `code_bug`, `rate_limited`, `gated_by_design`. **Zero** were an expired credential or a dead endpoint, although that is the usual first guess.

If the error names sub-units as `deferred (budget N min)` or `budget spent`, **nothing has failed** — the source ran out of its slice and the rest is taken next tick (R303). Do not 'fix' it.

## 4b. Root cause when this was last investigated (2026-08-04)

*From the unfetchable-source audit — 29 agents, every finding challenged by a skeptic before it was accepted. This source was covered by the target `insee_bdm`.*

- **root cause**: `rate_limited`  (confidence medium)
- **which units**: 201/201 flows failed on ONE run (id 352, 2026-07-31T00:59:33Z) and that is the only failing run in the source's entire 3-run history. All 201 were genuinely attempted: the stored error carries an EMPTY `[...]` offender list, and the only labelled transient path is the circuit-breaker's "not attempted (upstream refusing)" (insee_bdm.py:238), so zero flows took it — the breaker did not exist yet (commit bfa436af landed 2026-07-31T06:11Z, ~5h after the run finished). R246 therefore does NOT overturn the 201/201 count for that run. Today: 4/4 probed flows healthy. It has not been re-attempted since, and is not due until 2026-08-06T08:11:33Z.
- **blast radius**: All 201 dataflow sub-units / 10,823,103 rows / ~101,768 catalog series (`insee_bdm:<idbank>`, per the fetcher's own comment at insee_bdm.py:223). ZERO data loss: every flow took the `total += before` path with its parquet untouched, and run 352's stored obs=10,823,103 matches the measured sum of all 201 on-disk parquets exactly. Real staleness is the cost: last successful merge was run 178 on 2026-07-16 (+241,903 rows), and upstream today already holds newer data waiting to be pulled — CREATIONS-ENTREPRISES-METHODE-2022 max 2026-06-01, VOV-2021-COMMERCE max 2026-05-01.
- fixable by us: **True**

**Recommended fix, in order:**

> NOTHING IS BROKEN RIGHT NOW — do not "fix" the endpoint. api.insee.fr/series/BDM/V1 is healthy (4/4 flows parsed live with our own parser), needs no key, and insee_bdm is legitimately not-due until 2026-08-06T08:11:33Z. To confirm recovery sooner, run it with --force (is_due is the only thing holding it). Expect it to succeed and pick up real new data (upstream already at 2026-06-01 on CREATIONS-ENTREPRISES-METHODE-2022 vs our last merge on 2026-07-16).
> 
> Three code changes, in priority order:
> 
> 1. STOP DISCARDING THE FAILURE REASON (this is why the brief had to guess and why I can only reach medium confidence). insee_bdm.py:277-280, change `except TransientError:` / `tally.transient_unit()` to `except TransientError as e: tally.transient_unit(f"{flow_id}: {e}")`. The exception message already carries the literal status code from lines 136/147/157. Without this the next occurrence is equally unattributable. Note this is a CLASS, not a one-off — `grep -rn "tally.transient_unit()" updater/strategies/fetchers/*.py` shows the same unlabelled call in _giant, _iep, abs, adb, barro_lee, bcb, bcrp, bfs, bls, boe, insee_melodi and more; sweep it rather than patching insee_bdm alone.
> 
> 2. HONOUR RETRY-AFTER. Import `retry_after_seconds` from ._common and use it in the 429/5xx branch (insee_bdm.py:154-159) in place of the fixed `min(5*(attempt+1), 30)`. The helper exists for exactly this and warns that ignoring the header is how a throttle escalates into a block. Apply to insee_melodi too — same host, same 129/144 symptom.
> 
> 3. BE GENTLER ON A PUBLIC NO-KEY API. RATE=0.3s (insee_bdm.py:66) across 201 flows returning ~200KB each is a sustained hammer; raise it (>=1s) and/or add a small jitter. The circuit breaker (bfa436af) already caps the COST of a refusal at ~13 min but does nothing to reduce the chance of earning one.

<details><summary>Evidence produced during that investigation — HTTP codes, file:line, measured counts. Re-verify before acting: it was true on 2026-08-04.</summary>

- PREMISE CORRECTED #1 — the endpoint is NOT dead. Live probe of the exact URL the fetcher builds: GET https://api.insee.fr/series/BDM/V1/data/BALANCE-PAIEMENTS?startPeriod=2024 -> HTTP 200 in 1.32s, content-type: application/xml;charset=UTF-8, 210,596 bytes, body starts `<?xml version='1.0' encoding='UTF-8'?><message:StructureSpecificData xmlns:ss="http://www.sdmx.org/resources/sdmxml/schemas/v2_1/data/structurespecific" ... Dataflow=FR1:BALANCE-PAIEMENTS(1.0)`. DNS api.insee.fr -> 194.254.37.207. No `www-authenticate`, no `retry-after`.
- PREMISE CORRECTED #2 — no auth exists to die. updater/registry.yaml, insee_bdm entry: `key_env: none`; rate_note reads 'No API key (public, Accept: application/xml)'. The fetcher sends only a UA/Accept header (insee_bdm.py:59-60). Credential is structurally impossible here.
- NO schema_drift / code_bug — the PRODUCTION parser reads the live body. Imported insee_bdm._get_xml and _parse_obs and ran them against the live response: root tag `{http://www.sdmx.org/resources/sdmxml/schemas/v2_1/message}StructureSpecificData`, PARSED n_series=197 obs=985, first row ('001694113', date(2024,5,1), 2056.0).
- Gentle 4-flow sweep (2s apart) with the production _get_xml/_parse_obs — wholesale refusal is NOT reproducible today: BALANCE-PAIEMENTS 1.29s series=197 obs=985; CREATIONS-ENTREPRISES-METHODE-2022 5.66s series=1894 obs=5868 maxdate=2026-06-01; IPPMP 0.92s series=131 obs=899; VOV-2021-COMMERCE 1.50s series=232 obs=1160 maxdate=2026-05-01. SWEEP RESULT: 4/4 returned a parseable SDMX data message.
- THE FAILURE WAS A FAST WHOLESALE REFUSAL, NOT A TIMEOUT — arithmetic on state.db runs row 352 (dur_s=6272.0, obs=10823103). Fixed retry budget for 201 all-failing flows = 201 x (5+10+15 backoff, insee_bdm.py:137/148/158 + RATE 0.3) = 6090.3s. Residual = 181.7s; I measured the local parquet overhead of one full sweep (row_count + obs_date max over all 201 files) at 2.7s, leaving 179.0s across 201x4=804 HTTP requests = ~0.223 s/request. If these had been timeouts (TIMEOUT=120, MAX_ATTEMPTS=4) the run would have taken 28.5 HOURS, not the observed 1.74. INSEE answered every request essentially instantly with a refusal.
- CONTROL — our egress was up; the refusal was INSEE-specific. Same serial orchestrator window (state.db runs 339-352, 2026-07-30T20:36 -> 2026-07-31T00:59): ecb 'partial' +5,849,110 new rows over 3973.0s; fed_board +13,675,104 rows; fhfa +3,227,580 rows; cnb 'ok' +690 rows in 5.6s; frankfurter 'ok' +725 rows in 4.8s.
- CONTROL — the probe harness itself works: GET https://data-api.ecb.europa.eu/service/data/EXR/M.USD.EUR.SP00.A?lastNObservations=1 -> HTTP 200 in 1.18s, content-type application/vnd.sdmx.genericdata+xml;version=2.1, body `<?xml version="1.0" encoding="UTF-8"?><message:GenericData ...`.
- HOST-SCOPED, not fetcher-scoped: insee_melodi targets the SAME host (updater/strategies/fetchers/insee_melodi.py:72 `BASE = "https://api.insee.fr/melodi"`) and its unit_state reads '129/144 sub-unit(s) transient-failed; will retry [DS_BPE_EDUCATION, ...]' at last_attempt 2026-07-31T23:23:21+00:00. Two independent fetchers refused wholesale on one host inside 24h.
- api.insee.fr does issue immediate connection-level refusals — my probe of https://api.insee.fr/ (host root) returned `ConnectionError: ('Connection aborted.', ConnectionResetError(10054, ...))` in 0.47s, while the two BDM paths on that same host returned 200. Fast-reset behaviour is in this host's repertoire.
- CONTRIBUTING DEFECT (ours) — we never honour Retry-After. insee_bdm.py:154-159 handles 429/500/502/503/504 with a fixed `time.sleep(min(5 * (attempt + 1), 30))`. `grep -n 'retry_after_seconds|Retry-After' updater/strategies/fetchers/insee_bdm.py` returns NOTHING; imports at line 57 are `from ._common import Tally, finalize, TransientStreak` only. _common.retry_after_seconds exists for exactly this and its docstring states ignoring the header 'is how you earn an escalating block rather than recover from a throttle'. Combined with RATE=0.3 (insee_bdm.py:66) across 201 flows of ~200KB responses on a public no-key API, we hammer.
- DIAGNOSTIC DEFECT (why the exact status code is unknowable) — insee_bdm.py:277-280 is `except TransientError:` (exception NOT bound) then `tally.transient_unit()` with NO label. The error text built at lines 136/147/157 (which contains the literal 'HTTP 429' / 'HTTP 503' / network error) is discarded, so unit_state.last_error is '201/201 sub-unit(s) transient-failed; will retry' with an empty offender list — while insee_melodi's row on the same night names its flows. _common._named and the Tally docstring exist precisely to prevent this.
- NOT budget_deferral (R303) — Tally.deferred is 0 here. finalize() emits the 'deferred by budget and taken next tick' wording (_common.py:284-288) only when tally.deferred>0; this row took the tally.transient branch (_common.py:275-278). insee_bdm has no Deadline at all — `Deadline` is not imported (insee_bdm.py:57).

</details>

## 5. Everything that has already gone wrong here

3 ledger entries mention this source. Read them in `D:\research\hfdatalibrary\.claude\MISTAKES.md` before changing anything — several describe a fix that was tried and was WRONG.

- **R249** — I mis-measured the same class twice, each time with a tool that could not see what I was claiming
- **R269** — CORRECTS R266: a synthetic test built from my hypothesis can only confirm it
- **R361** — the csv_retry_queue was write-only: the code PROMISED "retried next run instead of lost" and nothing ever read the queue

## 6. Rules that apply to every repair here

- **A key/parser change is a RE-PULL, never a merge** (R22). Changing what `series_key` means leaves the old and new grains coexisting; dedup cannot collapse them because they no longer collide. Use `mode="replace"`, or re-pull the table.
- **Never patch dates — re-pull** (R288). A naive date repair on `cso` would have destroyed 11 of 12 rows; only a negative control caught it. And 41,091 of `stat_slovenia`'s fabricated dates land in 1900–2200, indistinguishable from real data, so no date test can find them.
- **Verify against the store you are actually serving** (R296/R36). Under `AQUEDUCT_BACKEND=r2` the local directory is a scratch mirror of the last run only; a local check can under-report or give the opposite answer.
- **When a probe reports ABSENCE, run it against something known PRESENT** (R316). An absence looks identical whether the data is missing or the probe is broken.
- **Compare (key, date) pairs, not just keys** (R314). A re-key check that compares identifiers verifies the half you changed on purpose; the half you changed by accident is in the values.
- **A bound over a fixed order is a truncation, not a budget** (R190). If work is capped, check it ROTATES — and specifically that it rotates when the item at the head never completes (R313).
- **Read the job's ARGV, not its progress** (R323). A `--dry-run` and a real run print the same numbers.


{
 "generated": "2026-08-04",
 "audit": "econ-unfetchable-audit wf_ec4cdd6f-dfb (29 agents, adversarially verified)",
 "sources": {
  "bcrp": {
   "root_cause": "code_bug",
   "confidence": "high",
   "failing_units": "1 of 1 unit (`_all`). The crash aborts update() inside the SERIES loop before any merge, so 0 rows land: run 447 recorded obs=0, dur_s=0.3. It fires on the FIRST series that returns parsed rows (PD04638PD / USDPEN_mid), so sub-units 2-5 of 5 are never even requested. With the type corrected, all 5 sub-units succeed (control above).",
   "blast_radius": "The whole source, but only since the group_by removal: bcrp's last clean run was 2026-08-01T08:01:10Z (no_change, 31,408 obs, frontier 2026-07-30), and it has been unable to advance since. Exactly one crashing attempt is on record (2026-08-03T09:33:30Z); nothing has been attempted since, so the current damage is a 5-series daily FX store frozen at obs_date 2026-07-30 while the publisher is serving through today. No data was lost or corrupted — the crash happens before merge.merge_and_write, and the existing parquet is untouched.",
   "fixable_by_us": true,
   "needs_from_ahmed": "",
   "recommended_fix": "Fix at the source, the way scb was, not at the consumption points. In E:\\research\\econfindatalibrary\\updater\\strategies\\fetchers\\bcrp.py:140-158, make _per_series_last honour its own `-> dict[str, dt.date]` annotation by adding a string branch next to the existing datetime branch:\n\n    if isinstance(d, dt.datetime): d = d.date()\n    elif isinstance(d, str):\n        try: d = dt.date.fromisoformat(d[:10])\n        except ValueError: continue\n\nThen the cursor seed (276) and last_db (343) keep working via their existing isinstance guards, and 327 is fixed with them. Patching only line 327 would be the third repeat of the mistake this file has now made twice: R310 chose \"accept either type at each consumption point\" for bcrp and MISSED one, while the same commit chose fix-at-source for scb and missed none. Add a unit test that asserts every value of _per_series_last() is a dt.date, since the defect class is \"the annotation lies\", not \"this one line compares wrong\". After the fix, force a bcrp tick — the source will stay red until it is actually re-attempted, because the transient_fail row is stale (R246).",
   "evidence": [
    "PREMISE CORRECTED. The 09:33 crash PREDATES the R310 fix. state.db unit_state row: [\"bcrp\",\"_all\",\"extend_by_date\",\"date-tail\",last_success=\"2026-08-01T08:01:10+00:00\",last_attempt=\"2026-08-03T09:33:30+00:00\",\"transient_fail\",last_obs=\"2026-07-30\",obs_count=31408,last_error=\"UNEXPECTED:AttributeError(\\\"'str' object has no attribute 'isoformat'\\\")\"] — and `git log -1 --format=%ci 15f49f1c` (the R310 fix, \"bcrp + scb: the _max_by_key string class had two more members, one live\") = 2026-08-03 18:14:40 -0500 = 2026-08-03T23:14:40Z, i.e. 13h41m AFTER the crash. It did NOT crash again after the fix.",
    "R246 CHECK — it has not been ATTEMPTED since the fix. The runs table's last bcrp row is id 447, ts_utc 2026-08-03T09:33:30+00:00, status transient_fail, obs=0, dur_s=0.3; there is no row after it, and unit_state.last_attempt_utc is still that same timestamp. The red status is a stale pre-fix state row, not a fresh failure.",
    "THE R310 FIX IS PRESENT AND WORKS. `git status --porcelain -- updater/strategies/fetchers/bcrp.py` returns empty (tree clean at 15f49f1c). Running the real update() got PAST the cursor-seed loop at E:\\research\\econfindatalibrary\\updater\\strategies\\fetchers\\bcrp.py:276 (`cursors[skey] = d if isinstance(d, str) else d.isoformat()`) without raising — the AttributeError site is genuinely fixed.",
    "A THIRD LIVE SITE OF THE SAME CLASS, never touched by R310. Real update() against a copy of the production parquet raised: File \"E:\\research\\econfindatalibrary\\updater\\strategies\\fetchers\\bcrp.py\", line 327, in update / genuinely_new = any((di > last) for di in d) if last is not None else bool(d) / TypeError: '>' not supported between instances of 'datetime.date' and 'str'. `di` is a dt.date from _parse_period(); `last` is last_by_series.get(skey), an ISO STRING. Note the position: R310 fixed line 276 (cursor seed) and line 343 (last_db) and this site sits BETWEEN them in execution order — the patch stepped over it.",
    "THE STRING TYPE CONFIRMED EMPIRICALLY, not inferred: _per_series_last() on the real file printed 'BCRP:USDPEN_mid' -> '2026-07-21' type=str (also USDPEN_buy, USDPEN_sell). _common.py:379 `return {k: d.isoformat() for k, d in zip(ks, ds) ...}` is where the string is minted.",
    "THE PUBLISHER IS HEALTHY — no credential, endpoint, throttle or schema problem. GET https://estadisticas.bcrp.gob.pe/estadisticas/series/api/PD04638PD/json/2026-07-25/2026-08-03 -> HTTP 200, Content-Type: text/html; charset=UTF-8, 421 bytes, body: '{\\n\"config\":\\n{\\n\"title\":\"Tipo de cambio\",\\n\"series\":\\n[\\n{\\n\"name\":\"Tipo de cambio - TC Interbancario (S/ por US$) - Venta\",\\n\"dec\":\"3\"\\n}]\\n},\\n\"periods\":\\n[\\n{\\n\"name\":\"27.Jul.26\",\"values\":[\"n.d.\"]},...{\"name\":\"03.Ago.26\"...' — the exact {config, periods} envelope _parse() expects, carrying data through today. (text/html on a JSON body is normal for BCRP and is handled: _classify_body branches on the first character, not the header.)",
    "CONTROL — the probe is not broken and line 327 is the ONLY remaining blocker. Same live run, same 5 series, with _per_series_last monkeypatched to return dt.date instead of str: Result(status='ok', obs=31413, new_vintage='date-tail', last_obs_date='2026-07-31', error='+11240 new rows', series_cursors={'BCRP:USDPEN_buy': '2026-07-31', 'BCRP:USDPEN_mid': '2026-07-31', 'BCRP:USDPEN_sell': '2026-07-31', 'BCRP:EURPEN_buy': '2026-07-31', 'BCRP:EURPEN_sell': '2026-07-31'}). All five sub-units returned data, including the two EUR series. (That +11,240 is against my scratch copy, whose frontier is 2026-07-21 / 20,236 rows — NOT a prediction of what production, at 31,408 rows / 2026-07-30, would gain.)",
    "SIBLING CHECK — the other four _max_by_key callers are clean. boc.py:110, tcmb.py:147, riksbank.py:108 were fixed in a1c42881; scb.py:386-409 was fixed AT THE SOURCE (its _table_frontiers coerces `elif isinstance(d, str): d = dt.date.fromisoformat(d[:10])`), so its own `_parse_date(c) > stored_max` at scb.py:464 is safe. A grep for the date-vs-frontier comparison shape across all five files returns exactly one unguarded hit: bcrp.py:327."
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  },
  "ipea": {
   "root_cause": "budget_deferral",
   "confidence": "high",
   "failing_units": "0 of 1,192 attempted sub-units failed. The 298 \"transient-failed\" sub-units were NEVER ATTEMPTED — they are the un-reached tail of the catalogue after the 15-minute wall-clock budget bound. Publisher order proves it exactly: today's /Metadados returns 1,490 active+numeric series; 1490 - 298 = 1192, and IFS_PETROLEUM sits at index 1192, IFS_SOJAGP 1193, IFS12_BEEFB12 1194, IFS12_MAIZE12 1195, IFS12_PETROLEUM12 1196, IFS12_SOJAGP12 1197 — the six named \"offenders\" are literally the first six items of the deferred tail, consecutive, in catalogue order. That is the signature of `if dl.spent(): tally.transient_unit(code)` over a stable list, not of a scattered failure set.",
   "blast_radius": "298 of 1,490 active series (20%) went un-refreshed on the 2026-07-31 tick; 1,192 (80%) were fetched. Nothing was lost — deferred series are untouched, and rotate_after/save_rotation (updater/strategies/fetchers/ipea.py:139-140, 219-220) resume the next tick after the last one started, so the worst-case refresh gap for any series is 2 monthly ticks (cadence: monthly, per updater/registry.yaml). Measured staleness inside the deferred tail is 1-2 observations per series. One genuinely separate gap surfaced while measuring: the local store holds 1,241 distinct series (data/clean_full/ipea/ipea.parquet, 621,981 rows) against 1,490 active in the live catalogue — ~249 active codes added since the June first-pass ingest have never been fetched; the rotating sweep does pick them up as it walks, but only at the same 80%-per-tick rate.",
   "fixable_by_us": true,
   "needs_from_ahmed": "",
   "recommended_fix": "Nothing to fix upstream and nothing to fix in the tally — the mislabel was already corrected by commit e88178cb (transient_unit -> deferred_unit at the `dl.spent()` branch). Two residual actions, both ours: (1) The state row is stale and self-clearing only on the next run, which at monthly cadence is ~2026-08-31; until then dashboards will keep showing \"298 transient-failed\" and last_success=NULL for a healthy source. Either force one ipea tick now or accept the stale row knowingly. (2) The budget is genuinely undersized for a full sweep: 1,490 series at the measured 0.75s each needs ~18.6 min against BUDGET_MIN=15, so ipea will defer ~20% EVERY tick indefinitely, by arithmetic and not by accident. Raise BUDGET_MIN in updater/strategies/fetchers/ipea.py to ~25, or route ipea to run_location: local where tools/run_local_heavy.ps1 sets AQUEDUCT_BUDGET_MIN_OVERRIDE — either makes a single tick cover the whole catalogue and closes the 249 never-fetched new codes in one pass. Note that even after the tally fix, status stays `partial` and last_success stays NULL on any deferring tick (by the R231 contract), so raising the budget is what actually turns this source green.",
   "evidence": [
    "updater state (E:\\research\\econfindatalibrary\\data\\_aqueduct\\state.db, unit_state ipea/_all): status='partial', last_success_utc=None, last_attempt_utc='2026-07-31T23:38:31+00:00', last_error='298/1491 sub-unit(s) transient-failed; will retry [IFS_PETROLEUM, IFS_SOJAGP, IFS12_BEEFB12, IFS12_MAIZE12, IFS12_PETROLEUM12, IFS12_SOJAGP12, +292 more]'",
    "runs table row id=361 for that same attempt: dur_s=909.3. BUDGET_MIN=15 at updater/strategies/fetchers/ipea.py:67 => 900.0s. The run stopped 9.3s past its budget, i.e. the deadline bound exactly; 1,192 series in 900s = 0.75s each, consistent with RATE=0.15 plus a ~0.6s round trip.",
    "updater/strategies/fetchers/ipea.py:145-148 TODAY reads `if dl.spent(): # Deferral, not a verdict ... tally.deferred_unit(code)`. `git show e88178cb -- updater/strategies/fetchers/ipea.py` shows that exact line was `- tally.transient_unit(code)` / `+ tally.deferred_unit(code)`.",
    "That fix commit is e88178cb7206ad65c256c3a3f6048e45cdb410dd, dated 2026-08-03 10:26:02 -0500 — ~2.5 days AFTER the 2026-07-31T23:38Z attempt the state row records. Its message names this source by number: \"ipea 298/1491 ... all of which were deferrals\". The state row is stale output of already-corrected code (R304 check applied: fix postdates attempt).",
    "LIVE publisher GET http://www.ipeadata.gov.br/api/odata4/Metadados -> HTTP 200 in 3.3s, Content-Type 'application/json; odata.metadata=minimal', 7,327,240 bytes, no Retry-After; 3,584 rows total, 1,490 pass the fetcher's exact SERSTATUS=='A' and SERNUMERICA filter.",
    "LIVE GET /ValoresSerie(SERCODIGO='X') for all four named codes: IFS_PETROLEUM HTTP 200, 74 obs; IFS_SOJAGP HTTP 200, 69 obs; IFS12_BEEFB12 HTTP 200, 833 obs; IFS12_MAIZE12 HTTP 200, 833 obs. No Retry-After header on any. No auth is used or required by this API.",
    "CONTROL: ABATE_ABPEAV (index 0, inside the attempted prefix, the known-good series named in the fetcher docstring) probed with the identical call -> HTTP 200, 68 obs. Identical behaviour to the four 'failed' codes, so the probe is sound and the named codes are in no way special.",
    "NO schema drift: obs[0] of IFS_PETROLEUM = {'SERCODIGO': 'IFS_PETROLEUM', 'VALDATA': '1952-01-01T00:00:00-02:00', 'VALVALOR': 1.92, 'NIVNOME': '', 'TERCODIGO': ''}; key set ['NIVNOME','SERCODIGO','TERCODIGO','VALDATA','VALVALOR'] — the fields ipea.py:177-179 reads. The PRODUCTION parser jobs/ingest_ipea.parse_ipea_date accepted 74/74 observations, max parsed date 2025-01-01.",
    "The deferral does carry a real (small) cost, measured: IFS_PETROLEUM local max obs_date 2024-01-01 vs live 2025-01-01 (1 new annual obs); IFS12_BEEFB12 and IFS12_MAIZE12 hold 831 local rows each vs 833 live (2 new monthly obs each).",
    "PREMISE CORRECTION on the count: the state row says 1491, today's live catalogue yields 1,490 active+numeric. One series left the active/numeric set between 2026-07-31 and 2026-08-03. Immaterial to the verdict, but the brief's '1491' is no longer the live figure.",
    "Not a data-quality defect either: the state row's odd last_obs_date='2070-01-01' traces to IPEA's own population-projection series DEPIS_POPHP / DEPIS_POPMP / DEPIS_POPP in the local parquet — legitimate projections, not a parse bug."
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  },
  "insee_melodi": {
   "root_cause": "budget_deferral",
   "confidence": "high",
   "failing_units": "ZERO of 144 failed. 15 sub-units attempted and succeeded (catalog positions 1-15, DD_CNA_AGREGATS through DS_BPE); 129 sub-units (positions 16-144) were never attempted -- the 25-minute wall-clock budget stopped the sweep before reaching them. The '129/144 transient-failed' string is a labelling artefact of the pre-e88178cb code, in which the `if dl.spent():` branch called tally.transient_unit(code) instead of tally.deferred_unit(code); with the current code the same run would read '15 sub-unit(s) attempted, none failed; 129 deferred by budget and taken next tick'.",
   "blast_radius": "Zero rows lost or corrupted: the deferral path does `total += before` and never touches the parquet, and merge_and_write never shrinks. 36,430,103 observations across 84 parquet files remain published. The real exposure is FRESHNESS, not failure: because the budget has no rotation bookmark, the same 15-flow prefix is re-walked every tick and catalog positions 16-144 are never refreshed. Measured on the local store -- positions 1-14 last written 2026-07-25, position 15 onward last written 2026-07-14 or earlier (positions 132-144 stuck at 2026-06-10), and 65 of the 144 catalog flows have never landed at all. Status side-effect: `partial` never stamps last_success_utc (which is NULL for insee_melodi), so the source also reads as 'never succeeded'.",
   "fixable_by_us": true,
   "needs_from_ahmed": "",
   "recommended_fix": "Two parts; part one is already done.\n\n(1) DONE -- do not redo. Commit e88178cb (2026-08-03 10:26:02 -0500) changed insee_melodi.py:305 from `tally.transient_unit(code)` to `tally.deferred_unit(code)` inside the `if dl.spent():` branch. The stale 129/144 state row predates it (attempt 2026-07-31T23:23:21Z). No further code change is needed for the reported symptom; the row will correct itself on the next run. If you want the queue to read true before then, re-run insee_melodi or clear that unit_state.last_error.\n\n(2) OPEN and worth fixing -- the budget is a truncation. insee_melodi has a Deadline (BUDGET_MIN=25, insee_melodi.py:68/290) but no rotation and no per-flow freshness sidecar, so it re-walks catalog positions 1-15 forever and positions 16-144 are never fetched. Add the standard bookmark from _common.py, which is already imported in this module's neighbourhood:\n  - `bookmark = load_rotation(out_dir)` before the loop,\n  - `flows = rotate_after(flows, bookmark, key=lambda f: f.get(\"code\",\"\"))`,\n  - `save_rotation(out_dir, code)` after each completed flow (save on a complete pass too, so the next run wraps through the same code path).\nThis is the same fix R190 applied to abs/ecb and is exactly what the Deadline docstring (_common.py:92-100) requires. Alternatively/additionally raise BUDGET_MIN for this source under AQUEDUCT_BUDGET_MIN_OVERRIDE on the workstation -- INSEE's per-flow pages are slow (one flow took 23.0 s for page 1 alone, and the ingest log shows 27+ minute flows), so 25 minutes cannot cover 144 flows in any ordering; rotation is what makes the bound honest.\n\nDo NOT investigate INSEE as an upstream problem. api.insee.fr/melodi is healthy, keyless, and answering HTTP 200 with full 10,000-observation pages in the exact envelope the parser expects.",
   "evidence": [
    "BRIEF PREMISE IS WRONG. Nothing failed. state.db (E:\\research\\econfindatalibrary\\data\\_aqueduct\\state.db, unit_state) row: source_id=insee_melodi unit_id=_all status=partial last_attempt_utc=2026-07-31T23:23:21+00:00 obs_count=36430103 last_error='129/144 sub-unit(s) transient-failed; will retry [DS_BPE_EDUCATION, DS_BPE_EVOLUTION, DS_BPE_SPORT_CULTURE, DS_BTS_SAL_EQTP_SEX_AGE, DS_BTS_SAL_EQTP_SEX_PCS, DS_CNT_CB, +123 more]' -- that string was emitted by a budget-deferral branch, not a failure branch.",
    "CODE PROOF, file:line. E:\\research\\econfindatalibrary\\updater\\strategies\\fetchers\\insee_melodi.py:298-307 is `if dl.spent(): print('[insee_melodi] budget 25 min spent - {code} not pulled this run ...'); tally.deferred_unit(code); total += before; continue`. `git show e88178cb -- updater/strategies/fetchers/insee_melodi.py` shows that exact line was `- tally.transient_unit(code)` / `+ tally.deferred_unit(code)` -- committed 2026-08-03 10:26:02 -0500, i.e. AFTER the 2026-07-31 run that wrote the state row. The word 'deferred' lived only in the print above the call, so the label passed to the tally was a bare flow code and _common.finalize() rendered it as 'transient-failed'. BUDGET_MIN = 25 at insee_melodi.py:68.",
    "ARITHMETIC MATCHES EXACTLY, verified against the LIVE publisher catalog. GET https://api.insee.fr/melodi/dataflow/all -> HTTP 200, 0.71s, 30,706 bytes, Content-Type application/json, no Retry-After, no rate-limit headers; body starts b'[ {\\n  \"code\" : \"DD_CNA_AGREGATS\", ...'; parsed to a list of 144 flows -- exactly the denominator in '129/144'. DS_BPE_EDUCATION is at position 16 of 144 in server order; DS_BPE_EVOLUTION 17, DS_BPE_SPORT_CULTURE 18, DS_BTS_SAL_EQTP_SEX_AGE 19, DS_BTS_SAL_EQTP_SEX_PCS 20, DS_CNT_CB 21. 144 - 15 = 129. The named 'failures' are a CONTIGUOUS TAIL starting one past the last flow attempted (positions 1-15 = DD_CNA_AGREGATS .. DS_BPE) -- the signature of a wall-clock deadline, not of scattered upstream failures.",
    "DURATION PROVES IT INDEPENDENTLY. runs table row: ts_utc=2026-07-31T23:23:21+00:00 source_id=insee_melodi status=partial obs=36430103 dur_s=1580.6. Budget is 25 min = 1500 s. The run spent its budget on 15 flows and then took ~80 s to walk the remaining 129 (deferral is just a row_count + print). A run in which 129 flows genuinely transient-failed CANNOT take 26 minutes: insee_melodi.py:77-78 sets MAX_ATTEMPTS=5, TIMEOUT=120 plus backoff, so 129 real failures is many hours. Control from the same publisher and the same state.db: insee_bdm's genuine all-transient event (2026-07-31T00:59:33Z, '201/201 sub-unit(s) transient-failed') took dur_s=6272.0 (104.5 min) for 201 flows. 1580.6 s for 144 is the deferral shape, not the failure shape.",
    "THE FOUR NAMED DATASETS ALL SERVE FINE RIGHT NOW (probed live, >=2.2 s apart, keyless, no auth): GET https://api.insee.fr/melodi/data/DS_BPE_EDUCATION?startPeriod=2023&page=1 -> HTTP 200, 3.16s, 5,408,182 bytes, top-level keys ['identifier','observations','paging','publisher','title'], paging={'first': ..., 'isLast': False, 'next': '...page=2'}, n_obs=10000, obs[0]={\"attributes\":{\"UNIT_MULT\":\"0\"},\"dimensions\":{...,\"TIME_PERIOD\":...},\"measures\":{...}}. DS_BPE_EVOLUTION -> HTTP 200, 5.48s, 3,581,237 bytes, n_obs=10000, obs[0] measures {'OBS_VALUE_NIVEAU': {'value': 1.0}}. DS_BPE_SPORT_CULTURE -> HTTP 200, 8.64s, 7,246,088 bytes, n_obs=10000. DS_BTS_SAL_EQTP_SEX_AGE -> HTTP 200, 23.00s, 3,633,599 bytes, n_obs=10000, obs[0] dimensions {'GEO':'2025-UU2020-35216','SEX':'_T','FREQ':'A','TIME_PERIOD':'2023','DERA_MEASURE':'SALAIRE_NET_EQTP_MENSUEL_MOYENNE','AGE':'Y_LT25'}, measures {'OBS_VALUE_NIVEAU': {'value': 1691.908732}}. Envelope shape is exactly what insee_melodi.py:105-125 `_obs_to_row` parses (dimensions.TIME_PERIOD -> obs_date, first measure .value) -- NO schema drift, NO recode, NO credential, NO throttle.",
    "CONTROL (so ABSENT is distinguishable from a broken probe): GET https://api.insee.fr/melodi/data/DS_THIS_FLOW_DOES_NOT_EXIST_XYZ?page=1 -> HTTP 400, 0.14s, 45 bytes, body b\"DS_THIS_FLOW_DOES_NOT_EXIST_XYZ n'existe pas.\". The probe does report a missing id as missing; the four brief-named ids are present and serving. (insee_melodi.py:249-250 maps 400/404 to an empty window, which is correct for this publisher's 'n'existe pas' response.)",
    "RESIDUAL REAL DEFECT (separate from the mislabelling, still open): the budget is a TRUNCATION, not a rotation. grep for load_rotation|save_rotation|rotate_after|rotation in updater/strategies/fetchers/insee_melodi.py returns ZERO hits, and the loop at insee_melodi.py:291 is a plain `for flow in flows:` over the publisher's order with no per-flow freshness skip (`_flow_max_obs` at :309 only sets startPeriod, it never `continue`s a fresh flow). _common.py Deadline docstring lines 92-100 states the rule verbatim: 'A budgeted fetcher MUST also either (a) skip already-fresh sub-units cheaply via a per-sub-unit sidecar ... or (b) rotate its starting point with load_rotation / save_rotation / rotate_after. A bound without one of those is a truncation, not a budget.'",
    "THE TRUNCATION IS VISIBLE IN THE STORE, not just inferred. Local store E:\\research\\econfindatalibrary\\data\\clean_full\\insee_melodi holds 84 parquets against 144 catalog flows (65 catalog codes have NO parquet at all). Mtimes by catalog position: positions 1-14 all written 2026-07-25 09:05-09:07 UTC; position 15 (DS_BPE) onward carry 2026-07-14 or older (positions 132-144 are 2026-06-10). That is the contiguous stale tail the Deadline docstring predicts. Only 4 later files exist (DS_ICA p52, DS_PARC_LOGEMENTS_SERIES p61, DS_RP_SERIE_HISTORIQUE p84, DS_SIDE_CREA_DEP_REG_NAT p124, all 2026-07-29 16:05) and those bear a single identical timestamp, i.e. a targeted job, not the budgeted sweep.",
    "CATALOG ORDER IS STABLE, which is what makes the fixed-start bound bite: two GETs to /dataflow/all 2.2 s apart both returned HTTP 200 with n=144 and byte-identical code ordering (`new == old` -> True), first three ['DD_CNA_AGREGATS','DD_CNA_APU','DD_CNA_BRANCHES'], position 16 DS_BPE_EDUCATION both times. A 25-minute cap over that fixed order can never reach position 16+: jobs' own ingest log data/clean_full/insee_melodi_log.txt shows single flows taking 27+ minutes (DS_ETAT_CIVIL_DECES_COMMUNES 05:23:51 -> 05:51:26).",
    "ALREADY-KNOWN, ALREADY-LOGGED: D:\\research\\hfdatalibrary\\.claude\\MISTAKES.md R303 (line 7515) and its follow-up (line ~7665) name this exact instance -- 'insee_melodi writes tally.transient_unit(code) with the word only in the print above it' and 'insee_melodi 129/144 ... All four were deferrals'. R246 also satisfied: the run DID attempt (last_attempt_utc 2026-07-31T23:23:21Z, dur_s 1580.6), so this is not a never-ran source."
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  },
  "hagstofa": {
   "root_cause": "code_bug",
   "confidence": "high",
   "failing_units": null,
   "blast_radius": "2 of 1074 attempted sub-units this run (0.19%). ZERO rows lost or corrupted: merge is never-shrink and both tables' history is intact on disk (UMH11120 16,199 rows / 240 series keys, UMH11165 1,795 rows / 24 keys, both spanning 1949-12-31..2024-12-31). Zero rows missed either - upstream `updated` is 2024-09-06 and the latest real year is 2024, which is already stored, so the correct parse would have merged 128 + 16 revision rows that dedup to nothing new. The real damage is source-level, not table-level: 2 false \"structural\" verdicts make _common.finalize() raise DefinitiveError, so all ~1074 tables' work is reported as a schema break and hagstofa has stamped `partial` with a structural-break error on every run since at least 2026-07-14 (6 consecutive runs in the runs table), never a clean success, and the series_cursors computed in that run are thrown away with the exception. Wider class: the identical \"resolve the axis from full metadata, then pass only the time:true flag into the parser on a truncated response\" pattern exists in scb.py:619-620, ssb.py:384/624, stat_estonia.py:608-609, stat_slovenia.py:722-723, statfin.py:578-579 - read, not probed, so unverified for those publishers.",
   "fixable_by_us": true,
   "needs_from_ahmed": "",
   "recommended_fix": "One line, in updater/strategies/fetchers/hagstofa.py:378-379: stop discarding the axis already resolved at line 356. Replace `rows = parse_jsonstat2(resp, prefix, meta_time_code)` with `rows = parse_jsonstat2(resp, prefix, (tvar or {}).get(\"code\") or meta_time_code)`. `_time_var` resolves against the FULL metadata value list (date_parse_rate 0.938 on 'Ar'), which is strictly better-informed than the date-tail-truncated axis in the response (0.167). Verified on the live bodies: this yields 128 rows for UMH11120 and 16 for UMH11165 with series_keys byte-identical to what is already on disk, so no key churn and merge dedups cleanly. Second, harden core/pxweb.resolve_time_dim's step-3 last resort (core/pxweb.py:216-219): it currently returns the first TIME_CODES name in dimension order, which is how a month axis outranks a year axis - it should prefer the highest-parse-rate named candidate (even below min_rate) over dimension order, i.e. 'Ar' at 0.167 beats 'Manudur' at 0.000. Third (cheap, optional): in `_newer_time_codes`, keeping unparseable codes is right for genuine unknown periods but here it retains PxWeb normal-period sentinels ('3000'-'3005' = \"Average 1951-1980\" etc.) that dominate the truncated axis; nothing breaks if they stay, once the fix above lands. Fourth, sweep the same discard pattern in scb/ssb/stat_estonia/stat_slovenia/statfin (file:lines above) - one reported example is one instance of a class. Fifth, unrelated but measured: the budget `break` in hagstofa.update() should call tally.deferred_unit() for the untouched tables so the ~832 deferred ones are visible per R303 instead of silent.",
   "evidence": [
    "State names the 2 offenders exactly: sqlite unit_state(source_id='hagstofa') last_error = \"hagstofa: 2/1074 sub-unit(s) returned 200 but parsed 0 rows from a non-trivial body (schema/structural break); existing data kept [1_natturufar/2_vedurfar/UMH11120.px, 1_natturufar/2_vedurfar/UMH11165.px]\" (E:/research/econfindatalibrary/data/_aqueduct/state.db, run id 466, ts 2026-08-04T02:11:56Z, dur 1917.8s, status partial).",
    "LIVE GET https://px.hagstofa.is/pxen/api/v1/en/Umhverfi/1_natturufar/2_vedurfar/UMH11120.px/ -> HTTP 200, application/json; charset=utf-8, 2041 bytes. Variables: 'Maelistoed'(10), 'Manudur'(12), 'Ar'(81), 'Eining'(2). CRITICAL: NO variable carries `time: true` (meta_time_code resolves to None). Same for UMH11165.px (HTTP 200, 1660 bytes, vars 'Manudur'(12), 'Ar'(81), 'Stadur'(2), no time flag).",
    "LIVE POST (the exact date-tail body the fetcher builds, since=2024-12-31) to UMH11120.px -> HTTP 200, 12851 bytes. Body is real and current: `\"updated\":\"2024-09-06T10:00:00Z\"`, id=['Maelistoed','Manudur','Ar','Eining'], size=[10,12,6,2], value list len 1440 with 1077 non-null (first values [92.7, 23.6, 75, 30.4, 76.3, ...]). Same for UMH11165.px -> HTTP 200, 1926 bytes, `\"updated\":\"2024-09-06T11:00:00Z\"`, value len 144, 136 non-null. NOTHING UPSTREAM CHANGED - these are ordinary JSON-stat2 cubes.",
    "The response's `role` object is EMPTY (`\"role\":{}` -> core.pxweb.role_time_of() = None) on both offenders, so the parser has no authoritative time axis at all.",
    "THE DEFECT, measured: our own date-tail selection starves our own resolver. The full 'Ar' axis in the metadata has 81 codes and date_parse_rate = 0.938. The date-tail restricts it to ['2024','3001','3002','3003','3004','3005'] (UMH11120) / ['2024','3004','3003','3002','3001','3000'] (UMH11165) - one real year plus 5 PxWeb normal-period SENTINELS whose labels are 'Average precipitation 1951-1980', 'Average 1961-1990', etc. On the truncated axis date_parse_rate collapses to 0.167, BELOW core/pxweb.py:182 min_rate=0.6, so the value-driven branch (core/pxweb.py:210 `if rate < min_rate: continue`) rejects every dimension.",
    "Falling through to core/pxweb.py step 3 (literal-name last resort) returns the FIRST TIME_CODES name in dimension order, which is 'Manudur' (month, codes '0'..'11') - printed by my instrumented run: `-> resolve_time_dim = 1 (Manudur)` for UMH11120 and `-> resolve_time_dim = 0 (Manudur)` for UMH11165. Month codes parse to no date, and the label fallback ('January','February',...) parses to no date either, so every one of the 1077 / 136 non-null observations is skipped -> parse_jsonstat2 returns 0 rows. This is exactly the name-first defect core/pxweb.py's own docstring says it exists to kill, resurfacing through its last-resort branch.",
    "COUNTERFACTUAL on the same 200 body: parse_jsonstat2(resp, prefix, time_code='Ar') -> 128 rows for UMH11120 and 16 rows for UMH11165, and the keys it emits are byte-identical to what is already on disk: forced sample key 'ICE:Umhverfi:1_natturufar:2_vedurfar:UMH11120.px:Maelistoed=0:Manudur=0:Eining=0' == the on-disk key in data/clean_full/hagstofa/Umhverfi.parquet. So the parser can read the body perfectly; it is only being handed the wrong axis.",
    "SECOND COUNTERFACTUAL (same table, no date filter): POST UMH11165.px with the FULL 81-code 'Ar' axis -> HTTP 200, 13166 bytes, date_parse_rate 0.938, `resolve_time_dim = 1 (Ar)`, parse_jsonstat2 -> 1795 rows. That is EXACTLY the 1795 rows already stored for that prefix on disk. The table parses fine at full width and only fails when we narrow it.",
    "CONTROL (proves the probe is sound, not the finding): sibling table 1_natturufar/2_vedurfar/UMH11130.px, same folder, same date-tail since=2024-12-31 -> GET 200, metadata DOES carry `time: True` on 'Ar'; POST 200, 4456 bytes, response carries `\"role\":{\"time\":[\"Ar\"]}`; resolve_time_dim locks on 'Ar' authoritatively and parse_jsonstat2 returns 64 rows -> classified 'data'. Identical request shape, identical publisher, works. The only difference is the presence of the authoritative time flag.",
    "file:line for the bug: updater/strategies/fetchers/hagstofa.py:356 `tvar = _time_var(variables)` correctly resolves the time axis from the FULL metadata (my probe printed `resolved tvar code = Ar` for both offenders), but line 378 then recomputes `meta_time_code = next((v.get('code') for v in variables if v.get('time') is True), None)` -> None, and line 379 `rows = parse_jsonstat2(resp, prefix, meta_time_code)` passes that None, DISCARDING the axis it just resolved and forcing the parser to re-guess from the truncated response.",
    "file:line for the misclassification: updater/strategies/fetchers/hagstofa.py:382-386 - `body_has_values = bool(resp.get('value'))`; with since_date set and a non-empty value array this returns 'structural', which reaches _common.finalize() (updater/strategies/fetchers/_common.py:266-269) and raises DefinitiveError for the WHOLE source.",
    "NOT a budget deferral in the R303 sense, but note the run only attempted 1074 of the 1906 catalog tables (30-min HAGSTOFA_BUDGET_MIN); the fetcher's budget `break` at hagstofa.py never calls tally.deferred_unit(), so the ~832 untouched tables appear nowhere in the tally - the denominator 1074 is honest, the missing 832 are simply silent."
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  },
  "wid": {
   "root_cause": "code_bug",
   "confidence": "high",
   "failing_units": "Nothing upstream is failing. All 12 named sub-units (Al, ON, ON-MER, OO-MER, OP-MER, OQ-MER, SW, XQ, XQ-MER, XC, XE, XK — the \"+6 more\" are SW, XQ, XQ-MER, XC, XE, XK) return HTTP 200 with a CSV header and zero data rows. They are 47 bytes (9 of them) or 60 bytes (XC/XE/XK), against 45-65 MB for a real country. 412 of 424 country files parsed and merged normally on the one run that happened (412 stamped in _country_vintage.json, 412 per-country parquets on disk). 0 of 424 are genuinely broken.",
   "blast_radius": "0 observations lost — the 12 entities hold no data upstream to lose. But the source-level effect was total: DefinitiveError sank the whole run, so wid's ONE AND ONLY attempt ever (runs.id=395, 2026-08-02T02:50:16Z, 2129.7 s) recorded status=partial obs=0, source_state has no wid row at all, last_success is unstamped, and series_cursor holds 0 rows for wid — so ~2.47M served series show as never refreshed despite 412 country files having actually merged.",
   "fixable_by_us": true,
   "needs_from_ahmed": "",
   "recommended_fix": "Do not chase the reported error — it is already fixed and un-rerun. Two actions, in order.\n\n(1) RE-RUN wid and let the current code report. It is run_location: local (updater/registry.yaml:6197ff), so tools/run_local_heavy.ps1. This alone converts the stale 'schema/structural break' into the truth. But do (2) FIRST, because the very next run attempts only the 12 empties and will raise.\n\n(2) Fix the empty-entity bookkeeping in updater/strategies/fetchers/wid.py. The empty branch (lines 222-226) must record the entity the way the merged branch does, and the skip gate at line 185 must not demand a parquet for an entity that legitimately has none (`blob.exists(path)` is False for 11 of the 12, so a stamp alone will not skip them). Concretely: stamp a distinguishable value, e.g. `done[country] = f\"{_mod}|{_size}|empty\"`, set `dirty = True`, and let the gate skip on either form — requiring `blob.exists(path)` only for the non-empty form. WID's own (last-modified|size) then re-checks each entity exactly when it is republished, which is the only moment it could gain data. Verified outcome: attempted drops to 0 and finalize() returns status='no_change' / 'no new rows' (ran it) instead of raising.\n\nDefensively, also make the empty-window gate in updater/strategies/fetchers/_common.py:270 mean what it says — 'the source went dark' — rather than 'every unit we happened to attempt is one we already know is header-only'. Excluding known-empty units from that denominator is better than raising wid's empty_window_floor; a bare floor bump to 12 also returns 'no_change' (verified) but silently re-arms the moment WID publishes a 13th empty entity.\n\nNothing here is upstream. WID answered every request 200, first byte to last, with no Retry-After and no throttling.",
   "evidence": [
    "PREMISE CORRECTION 1 — the 12 are genuinely empty upstream, not a parser shape problem. Live GET https://wid.world/bulk_download/WID_data_ON-MER.csv -> HTTP 200, Content-Type='text/csv', Content-Length='47', actual_bytes=47, Last-Modified='Thu, 29 May 2025 15:27:36 GMT'; body verbatim: b'country;variable;percentile;year;value;age;pop\\n'. Identical shape for Al, ON, OO-MER, OP-MER, OQ-MER, SW, XQ, XQ-MER (all 47 B) and XC, XE, XK (60 B, header b'country;variable;percentile;year;value;age;pop;data_quality\\n').",
    "CONTROL (proves the probe and the parser both work): same code, same session, live GET WID_data_QC.csv -> HTTP 200, Content-Length='1232615'; production _parse() returned 28249 usable rows, 0 unparseable, sample key 'WID:ehfcari999:p0p100:999:i:QC' date=1990-12-31 value=2906.39582942161. So a zero-row parse is a property of those 12 bodies, not of our parser.",
    "Production _has_expected_header() returns True for all 12 bodies (ran it on the live text). Live index GET https://wid.world/bulk_download/ -> HTTP 200, 283059 bytes; production _ROW_RE matched 424 rows; exactly 12 are listed under 200 bytes, and they are exactly the 12 named. 424 and 12 both reproduce.",
    "PREMISE CORRECTION 2 — the reported error is STALE and was already fixed (R246). state.db unit_state('wid','_all').last_error is verbatim: \"wid: 12/424 sub-unit(s) returned 200 but parsed 0 rows from a non-trivial body (schema/structural break); existing data kept [Al: HTTP 200, ON-MER: HTTP 200, ON: HTTP 200, OO-MER: HTTP 200, OP-MER: HTTP 200, OQ-MER: HTTP 200, +6 more]\", stamped last_attempt_utc=2026-08-02T02:50:16+00:00. `select ... from runs where source_id='wid'` returns exactly ONE row, id=395, same timestamp — wid has been attempted once, ever.",
    "The stale message came from code that no longer exists. `git show 691e6126^:updater/strategies/fetchers/wid.py` line: `if r.status_code != 200 or len(r.content) < 200:` then `tally.structural_unit(f\"{country}: HTTP {r.status_code}\")` — that byte-length test is literally where the 'Al: HTTP 200' label is produced. Commit 691e6126 'wid: an empty upstream file is not a schema break' (2026-08-02 19:10:12 -0500 = 2026-08-03T00:10:12Z) replaced it with the _has_expected_header/empty_unit branch now at updater/strategies/fetchers/wid.py:209-226. The fix landed ~21 h AFTER the only run. wid has not run since.",
    "THE LIVE DEFECT THE FIX LEFT BEHIND (this is the actionable finding). updater/strategies/fetchers/wid.py:222-226 does `tally.empty_unit(country); continue` and never reaches the `done[country] = f\"{_mod}|{_size}\"` stamp at line 245, so the 12 empties are never recorded and are re-attempted on every run. Measured: data/clean_full/wid/_country_vintage.json holds exactly 412 entries = 424 - 12, and all 12 empties are absent from it.",
    "Replayed the production skip gate (updater/strategies/fetchers/wid.py:185) against the LIVE index: WOULD SKIP 412, WOULD ATTEMPT 12 — and the 12 are precisely the empty entities (marker=None for all; parquet_exists=False for 11 of 12). So the next run's tally is attempted=12, empty=12, added=0.",
    "That tally RAISES. Ran the real finalize() from updater/strategies/fetchers/_common.py:262 with exactly that Tally: 'finalize -> RAISED DefinitiveError: wid: all 12 attempted sub-units returned empty/404 over a large window - likely a structural break, not a quiet period; existing data kept' (the gate at _common.py:270, `attempted > empty_window_floor` with floor=10, and 12 > 10). Adding a budget deferral does NOT rescue it — the empty-window check precedes the deferral check, and it raised identically. Control: the same 12 empties plus one country that added rows returns status='ok'. So wid is about to trade one false 'schema break' over these 12 empties for a different false 'structural break' over the same 12.",
    "SEPARATE, NOT A BREAK: WID added a `data_quality` column in its 2026-07-25 republication. Range-probed (bytes=0-399, HTTP 206) FR (Content-Range bytes 0-399/68161470), US (/52574299), ZA (/45417668), DE-BY (/1168) — all now header `country;variable;percentile;year;value;age;pop;data_quality`, while QC (Last-Modified 2025-08-07) still has the 7-column header. Our _parse uses csv.DictReader by name, so it is unaffected: fed a synthetic 8-column body it returned 2/2 rows with unchanged keys ('WID:ehfcari999:p0p100:999:i:XC'). No schema_drift. Noting only that we currently discard the new column.",
    "BUDGET IS NOT THE STORY HERE (R303 checked, not assumed): the one run took 2129.7 s against BUDGET_S = 180 min (updater/strategies/fetchers/wid.py:61), and the stored error names no deferrals. tally.deferred was 0."
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  },
  "eurostat": {
   "root_cause": "gated_by_design",
   "confidence": "high",
   "failing_units": "0 flows attempted of 7,562 catalogued dataflows. This is not a per-flow failure profile at all: _require_rekeyed() raises DefinitiveError inside update() BEFORE _giant.run_giant selects a single flow (run id 345 lasted 6.8s and recorded obs=0). Store side, the dry run has so far found 4,999 of the first 5,000 files still carrying the unstable key and exactly 1 already clean.",
   "blast_radius": "The entire source is frozen and has never once updated: last_success_utc is NULL and the runs table contains a single eurostat attempt. 7,754 store parquet files / ~1.48B rows scanned so far still hold the unstable 'LAST UPDATE=' key scheme. Key inflation measured on one file: NAMA_10_GDP carries 53,747 distinct keys where 36,493 are real — 17,254 phantom series in that dataset alone, and it is one of 7,754. Nothing is corrupt or lost; the data is stale and over-keyed, and the freshest upstream vintages in the TOC are 08.07.2026 / 18.07.2026 / 30.07.2026, so real published revisions are being missed daily.",
   "fixable_by_us": true,
   "needs_from_ahmed": "Go-ahead to run tools/rekey_eurostat.py --apply, which overwrites the production eurostat store (backend=r2, 7,754 files) and resolves same-(key,obs_date) conflicts by keeping the LAST occurrence. This is only a real decision if the dry run's final 'conflicting revisions' count is non-zero; if it prints 0, every collapse was an exact duplicate, the migration changes no served value, and no sign-off is needed.",
   "recommended_fix": "1) The gate is CORRECT and is NOT masking a fetch-side problem — do not touch the fetcher's endpoints, keys or parser; they are verified working against the live publisher today. 2) Let the --dry-run finish and read its final 'conflicting revisions' line, then re-run the SAME tool with --apply (the currently running invocation writes nothing — that is the actual reason the gate has not lifted). 3) BEFORE the --apply, fix eurostat.py:209: `blob.list_parquets(out_dir)[:5]` must not be a prefix sample of a sorted list that the migration walks in the same order. Either scan every file, or sample randomly, or — cheapest and strongest — have the migration write a completion marker (e.g. _rekeyed.json holding the file count it converted) and have _require_rekeyed() demand that marker match the current store count. Otherwise a re-key that dies partway (as one already did at file 4,403/7,754) silently disarms the guard at 0.06% done and the next daily tick duplicates ~3,300 files under two key schemes. 4) Only after a clean --apply, let the normal daily tick run; expect several 'partial' ticks by design (DEFAULT_MAX_FLOWS_PER_TICK = 400 in _giant.py caps the sweep) — those are not failures.",
   "evidence": [
    "LIVE TOC probe (publisher's own host, no relay): GET https://ec.europa.eu/eurostat/api/dissemination/catalogue/toc/txt -> HTTP 200, Content-Type 'text/plain;charset=UTF-8', 1,971,935 bytes, no Retry-After. Fed to the fetcher's OWN eurostat._parse_toc: 7,562 datasets parsed, e.g. ('ei_bpm6ca_q', {'vintage': '08.07.2026', 'filename': 'EI_BPM6CA_Q.parquet'}).",
    "LIVE data probe: GET https://ec.europa.eu/eurostat/api/dissemination/sdmx/2.1/data/NAMA_10_GDP/?format=SDMX-CSV&startPeriod=2024 -> HTTP 200, Content-Type 'application/vnd.sdmx.data+csv;version=1.0.0', 5,368,348 bytes. Body first line: 'DATAFLOW,LAST UPDATE,freq,unit,na_item,geo,TIME_PERIOD,OBS_VALUE,OBS_FLAG,CONF_STATUS' then 'ESTAT:NAMA_10_GDP(1.0),30/07/26 11:00:00,A,CLV05_MEUR,B1G,AL,2024,10873.1,p,' — exactly the shape eurostat._parse_csv expects. No schema drift.",
    "Same body through eurostat._parse_csv: 68,524 rows, 35,502 distinct series_key, sample key 'freq=A:unit=CLV05_MEUR:na_item=B1G:geo=AL', dates [2024-12-31, 2024-12-31, 2025-12-31], values [10873.1, 282992.9, 284890.0]; 'LAST UPDATE' present in new keys = False. CONCLUSION: if the gate were lifted, eurostat WOULD fetch and parse.",
    "Gate premise VERIFIED TRUE against the store: pyarrow read of E:\\research\\econfindatalibrary\\data\\clean_full\\eurostat\\NAMA_10_GDP.parquet — 1,112,628 rows, first five series_key values are literally 'LAST UPDATE=09/06/26 11:00:00:freq=A:unit=CLV05_MEUR:na_item=B1G:geo=AL'. The unstable key really is still in the data.",
    "Migration correctness round-trip: tools/rekey_eurostat.stable_key applied to that file collapses 53,747 distinct keys -> 36,493, and 35,498 of the 35,502 keys the LIVE fetch emits are already present in the migrated set. The 4 misses are genuinely new upstream series (e.g. 'freq=A:unit=CP_MPPS_EU27_2020:na_item=D21:geo=EA21'), not a keying mismatch. The gate is satisfiable and the two code paths agree.",
    "State DB E:\\research\\econfindatalibrary\\data\\_aqueduct\\state.db, unit_state: source_id='eurostat', strategy='giant_changed_units', status='partial', last_success_utc=None, last_attempt_utc='2026-07-30T21:46:41+00:00', last_error=\"eurostat: existing data still uses the UNSTABLE 'LAST UPDATE=' series_key - run the one-time re-key migration...\". runs table holds exactly ONE eurostat row: id=345, dur_s=6.8, obs=0 — it aborts before a single flow is fetched.",
    "CONTROL (is the probe/scheduler broken, or just eurostat?): same state.db, 77 of 141 units have last_attempt_utc >= 2026-07-30; newest is hagstofa at 2026-08-04T02:11:56Z (partial, 2/1074 sub-units structural) and ofr at 2026-08-04T00:48:33Z status='ok', obs=433,301, '+261 new rows'. The updater is alive and other sources are succeeding — eurostat's stall is the gate, nothing else.",
    "CORRECTION TO THE BRIEF: the re-key running locally is a MEASUREMENT PASS, not the migration. Win32_Process PID 26252, CreationDate 8/3/2026 5:40:26 PM, CommandLine: python.exe -u tools/rekey_eurostat.py --dry-run. tools/rekey_eurostat.py:192 `if a.dry_run: print('--dry-run: nothing written.')` and blob.write_table_atomic is reached only under `if a.apply` (line 171). Log logs/rekey_eurostat_20260803T224026Z.log tail: '[5000/7754] touched=4,999 clean=1 rows=1,483,519,001 -> 1,481,208,948'. When it finishes, ZERO files will have been re-keyed and the gate will still fire.",
    "SECOND, INDEPENDENT DEFECT (latent, will bite on the real --apply): eurostat.py:209 `for fn in blob.list_parquets(out_dir)[:5]:` — the guard inspects only the FIRST FIVE of 7,754 files. blob.py:203-246 returns `sorted(...)` in both the R2 and local branches, and rekey_eurostat.py:81 walks that identical sorted list. So a partial --apply run re-keys those same 5 files first and the guard releases at 0.06% completion. The tool's own comment (rekey_eurostat.py:110-118) records a prior pass dying at file 4,403 of 7,754 after ~4 hours on a transient R2 read — i.e. the interrupt case is observed, not hypothetical. A resumed daily run would then merge stable-key fetches into ~3,300 still-unstable files: the exact mixed-scheme duplication the guard exists to prevent, and one never-shrink cannot catch.",
    "Store vs catalogue size: 7,754 .parquet files in data/clean_full/eurostat (7,755 dir entries incl. the _giant_state sidecar) against 7,562 dataflows in today's TOC. Not investigated further; noted, not diagnosed."
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  },
  "insee_bdm": {
   "root_cause": "rate_limited",
   "confidence": "medium",
   "failing_units": "201/201 flows failed on ONE run (id 352, 2026-07-31T00:59:33Z) and that is the only failing run in the source's entire 3-run history. All 201 were genuinely attempted: the stored error carries an EMPTY `[...]` offender list, and the only labelled transient path is the circuit-breaker's \"not attempted (upstream refusing)\" (insee_bdm.py:238), so zero flows took it — the breaker did not exist yet (commit bfa436af landed 2026-07-31T06:11Z, ~5h after the run finished). R246 therefore does NOT overturn the 201/201 count for that run. Today: 4/4 probed flows healthy. It has not been re-attempted since, and is not due until 2026-08-06T08:11:33Z.",
   "blast_radius": "All 201 dataflow sub-units / 10,823,103 rows / ~101,768 catalog series (`insee_bdm:<idbank>`, per the fetcher's own comment at insee_bdm.py:223). ZERO data loss: every flow took the `total += before` path with its parquet untouched, and run 352's stored obs=10,823,103 matches the measured sum of all 201 on-disk parquets exactly. Real staleness is the cost: last successful merge was run 178 on 2026-07-16 (+241,903 rows), and upstream today already holds newer data waiting to be pulled — CREATIONS-ENTREPRISES-METHODE-2022 max 2026-06-01, VOV-2021-COMMERCE max 2026-05-01.",
   "fixable_by_us": true,
   "needs_from_ahmed": "",
   "recommended_fix": "NOTHING IS BROKEN RIGHT NOW — do not \"fix\" the endpoint. api.insee.fr/series/BDM/V1 is healthy (4/4 flows parsed live with our own parser), needs no key, and insee_bdm is legitimately not-due until 2026-08-06T08:11:33Z. To confirm recovery sooner, run it with --force (is_due is the only thing holding it). Expect it to succeed and pick up real new data (upstream already at 2026-06-01 on CREATIONS-ENTREPRISES-METHODE-2022 vs our last merge on 2026-07-16).\n\nThree code changes, in priority order:\n\n1. STOP DISCARDING THE FAILURE REASON (this is why the brief had to guess and why I can only reach medium confidence). insee_bdm.py:277-280, change `except TransientError:` / `tally.transient_unit()` to `except TransientError as e: tally.transient_unit(f\"{flow_id}: {e}\")`. The exception message already carries the literal status code from lines 136/147/157. Without this the next occurrence is equally unattributable. Note this is a CLASS, not a one-off — `grep -rn \"tally.transient_unit()\" updater/strategies/fetchers/*.py` shows the same unlabelled call in _giant, _iep, abs, adb, barro_lee, bcb, bcrp, bfs, bls, boe, insee_melodi and more; sweep it rather than patching insee_bdm alone.\n\n2. HONOUR RETRY-AFTER. Import `retry_after_seconds` from ._common and use it in the 429/5xx branch (insee_bdm.py:154-159) in place of the fixed `min(5*(attempt+1), 30)`. The helper exists for exactly this and warns that ignoring the header is how a throttle escalates into a block. Apply to insee_melodi too — same host, same 129/144 symptom.\n\n3. BE GENTLER ON A PUBLIC NO-KEY API. RATE=0.3s (insee_bdm.py:66) across 201 flows returning ~200KB each is a sustained hammer; raise it (>=1s) and/or add a small jitter. The circuit breaker (bfa436af) already caps the COST of a refusal at ~13 min but does nothing to reduce the chance of earning one.",
   "evidence": [
    "PREMISE CORRECTED #1 — the endpoint is NOT dead. Live probe of the exact URL the fetcher builds: GET https://api.insee.fr/series/BDM/V1/data/BALANCE-PAIEMENTS?startPeriod=2024 -> HTTP 200 in 1.32s, content-type: application/xml;charset=UTF-8, 210,596 bytes, body starts `<?xml version='1.0' encoding='UTF-8'?><message:StructureSpecificData xmlns:ss=\"http://www.sdmx.org/resources/sdmxml/schemas/v2_1/data/structurespecific\" ... Dataflow=FR1:BALANCE-PAIEMENTS(1.0)`. DNS api.insee.fr -> 194.254.37.207. No `www-authenticate`, no `retry-after`.",
    "PREMISE CORRECTED #2 — no auth exists to die. updater/registry.yaml, insee_bdm entry: `key_env: none`; rate_note reads 'No API key (public, Accept: application/xml)'. The fetcher sends only a UA/Accept header (insee_bdm.py:59-60). Credential is structurally impossible here.",
    "NO schema_drift / code_bug — the PRODUCTION parser reads the live body. Imported insee_bdm._get_xml and _parse_obs and ran them against the live response: root tag `{http://www.sdmx.org/resources/sdmxml/schemas/v2_1/message}StructureSpecificData`, PARSED n_series=197 obs=985, first row ('001694113', date(2024,5,1), 2056.0).",
    "Gentle 4-flow sweep (2s apart) with the production _get_xml/_parse_obs — wholesale refusal is NOT reproducible today: BALANCE-PAIEMENTS 1.29s series=197 obs=985; CREATIONS-ENTREPRISES-METHODE-2022 5.66s series=1894 obs=5868 maxdate=2026-06-01; IPPMP 0.92s series=131 obs=899; VOV-2021-COMMERCE 1.50s series=232 obs=1160 maxdate=2026-05-01. SWEEP RESULT: 4/4 returned a parseable SDMX data message.",
    "THE FAILURE WAS A FAST WHOLESALE REFUSAL, NOT A TIMEOUT — arithmetic on state.db runs row 352 (dur_s=6272.0, obs=10823103). Fixed retry budget for 201 all-failing flows = 201 x (5+10+15 backoff, insee_bdm.py:137/148/158 + RATE 0.3) = 6090.3s. Residual = 181.7s; I measured the local parquet overhead of one full sweep (row_count + obs_date max over all 201 files) at 2.7s, leaving 179.0s across 201x4=804 HTTP requests = ~0.223 s/request. If these had been timeouts (TIMEOUT=120, MAX_ATTEMPTS=4) the run would have taken 28.5 HOURS, not the observed 1.74. INSEE answered every request essentially instantly with a refusal.",
    "CONTROL — our egress was up; the refusal was INSEE-specific. Same serial orchestrator window (state.db runs 339-352, 2026-07-30T20:36 -> 2026-07-31T00:59): ecb 'partial' +5,849,110 new rows over 3973.0s; fed_board +13,675,104 rows; fhfa +3,227,580 rows; cnb 'ok' +690 rows in 5.6s; frankfurter 'ok' +725 rows in 4.8s.",
    "CONTROL — the probe harness itself works: GET https://data-api.ecb.europa.eu/service/data/EXR/M.USD.EUR.SP00.A?lastNObservations=1 -> HTTP 200 in 1.18s, content-type application/vnd.sdmx.genericdata+xml;version=2.1, body `<?xml version=\"1.0\" encoding=\"UTF-8\"?><message:GenericData ...`.",
    "HOST-SCOPED, not fetcher-scoped: insee_melodi targets the SAME host (updater/strategies/fetchers/insee_melodi.py:72 `BASE = \"https://api.insee.fr/melodi\"`) and its unit_state reads '129/144 sub-unit(s) transient-failed; will retry [DS_BPE_EDUCATION, ...]' at last_attempt 2026-07-31T23:23:21+00:00. Two independent fetchers refused wholesale on one host inside 24h.",
    "api.insee.fr does issue immediate connection-level refusals — my probe of https://api.insee.fr/ (host root) returned `ConnectionError: ('Connection aborted.', ConnectionResetError(10054, ...))` in 0.47s, while the two BDM paths on that same host returned 200. Fast-reset behaviour is in this host's repertoire.",
    "CONTRIBUTING DEFECT (ours) — we never honour Retry-After. insee_bdm.py:154-159 handles 429/500/502/503/504 with a fixed `time.sleep(min(5 * (attempt + 1), 30))`. `grep -n 'retry_after_seconds|Retry-After' updater/strategies/fetchers/insee_bdm.py` returns NOTHING; imports at line 57 are `from ._common import Tally, finalize, TransientStreak` only. _common.retry_after_seconds exists for exactly this and its docstring states ignoring the header 'is how you earn an escalating block rather than recover from a throttle'. Combined with RATE=0.3 (insee_bdm.py:66) across 201 flows of ~200KB responses on a public no-key API, we hammer.",
    "DIAGNOSTIC DEFECT (why the exact status code is unknowable) — insee_bdm.py:277-280 is `except TransientError:` (exception NOT bound) then `tally.transient_unit()` with NO label. The error text built at lines 136/147/157 (which contains the literal 'HTTP 429' / 'HTTP 503' / network error) is discarded, so unit_state.last_error is '201/201 sub-unit(s) transient-failed; will retry' with an empty offender list — while insee_melodi's row on the same night names its flows. _common._named and the Tally docstring exist precisely to prevent this.",
    "NOT budget_deferral (R303) — Tally.deferred is 0 here. finalize() emits the 'deferred by budget and taken next tick' wording (_common.py:284-288) only when tally.deferred>0; this row took the tally.transient branch (_common.py:275-278). insee_bdm has no Deadline at all — `Deadline` is not imported (insee_bdm.py:57)."
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  },
  "credentials (whole-updater audit, elkassabgi/econdatalibrary)": {
   "root_cause": "code_bug",
   "confidence": "high",
   "failing_units": "0 of 141 sources fail for a credential reason. 4 of 13 workflow-referenced secrets do not exist (BEA_API_KEY, CENSUS_API_KEY, EIA_API_KEY, DIGEST_TO) and all 4 are inert. 3 of the 5 key-requiring fetchers mishandle an INVALID (as opposed to absent) credential: bea and nyfed degrade silently to zero rows, census misclassifies it as a transient upstream error. 2 of 5 handle it correctly (comtrade 401 -> transient, fred_releases 400 -> DefinitiveError).",
   "blast_radius": "Credential-BLOCKED sources: 0 of 141 registry entries (0 of 112 live:true). The four missing secrets (BEA_API_KEY, CENSUS_API_KEY, EIA_API_KEY, DIGEST_TO) affect nothing that runs: bea and census are excluded from every cloud run by run_location:local, the eia fetcher is keyless, DIGEST_TO has a fallback. Latent silent-degradation exposure: 2 sources. nyfed (live:true, daily, 8 FRED series) would publish zero rows with no error if FRED_API_KEY were rotated or revoked; bea (251,203 obs in unit_state, 240 catalog ids, +258,223 rows fetched 2026-08-03) would do the same if BEA_API_KEY became invalid. census is a third, milder case: an invalid key is reported as a transient upstream error rather than a credential fault, so it retries forever instead of failing definitively.",
   "fixable_by_us": true,
   "needs_from_ahmed": "",
   "recommended_fix": "Nothing is blocked and nothing is needed from Ahmed -- that is the headline correction. The BEA key exists and is valid (probe returned 100 NIPA rows); the \"blocked on Ahmed creating a GitHub secret\" note in updater-daily.yml:81-93 is stale for the route bea actually runs on, and should be rewritten to say the source runs from the workstation via _common.api_key(). Three code fixes, all ours: (1) nyfed.py:100-101 -- stop swallowing every 4xx. Split the branch: 400/401/403 must raise DefinitiveError naming FRED_API_KEY (FRED's own body says \"The value for variable api_key is not registered\"), and only 404 on a specific series id may return []. (2) jobs/ingest_bea_full.py -- add an AUTH class to _classify_error: APIErrorCode \"1\" / \"Invalid API UserId\" must raise, never fall through to the `return \"nodata\"` default at line 125. That default is the actual mechanism by which a revoked BEA key would publish a silent zero-row run. (3) census.py:568 -- widen the detector from `\"missing key\"` to also catch `\"invalid key\"` (both arrive as HTTP 200 text/html), so a bad key is a DefinitiveError naming CENSUS_API_KEY instead of the generic \"upstream error page\" TransientError at :574. Generalised rule worth adding to the ledger: every fetcher guards the ABSENCE of its key and none of them guards its INVALIDITY -- and three of these publishers signal an invalid key with a 200 or a 400 that the retry logic reads as \"no data\". Optional housekeeping: delete the vestigial `EIA_API_KEY` passthrough at updater-heavy.yml:159 (the fetcher is keyless), and either create DIGEST_TO or drop the reference. Creating BEA_API_KEY/CENSUS_API_KEY as repo secrets is NOT required and would not change any current run -- it only matters if those two sources are ever re-routed off run_location:local, which is a memory decision, not a credential one.",
   "evidence": [
    "PREMISE CONFIRMED (secret existence): `gh secret list` in /e/research/econfindatalibrary (origin = github.com/elkassabgi/econdatalibrary) returned exactly 8 names -- CLOUDFLARE_ACCOUNT_ID, CLOUDFLARE_API_TOKEN, COMTRADE_API_KEY, FRED_API_KEY, R2_WRITE_ACCESS_KEY_ID, R2_WRITE_ENDPOINT, R2_WRITE_SECRET_ACCESS_KEY, RESEND_API_KEY. No BEA_API_KEY, no CENSUS_API_KEY, no EIA_API_KEY, no DIGEST_TO.",
    "Workflow refs vs. that list: .github/workflows/updater-daily.yml:93 `BEA_API_KEY: ${{ secrets.BEA_API_KEY }}`, :100 `CENSUS_API_KEY: ${{ secrets.CENSUS_API_KEY }}`, :359 `DIGEST_TO`; .github/workflows/updater-heavy.yml:159 `EIA_API_KEY`. Four referenced-but-nonexistent secrets, each therefore rendering as the empty string. updater-daily.yml:87-89 already documents this for BEA verbatim: \"the repo secret does NOT exist yet (gh secret list: ... no BEA key)\".",
    "PREMISE CORRECTED (impact): none of those four gates any run that actually executes. updater/orchestrate.py:238-252 `_wrong_location()` returns True when `run_location != _here()`, and :731-737 `continue`s past the unit; :235 makes `_here()` == 'cloud' whenever GITHUB_ACTIONS is set. registry.yaml has bea (live=True, run_location=local) and census (live=None, run_location=local), so BOTH are structurally excluded from every cloud run -- the missing secrets are inert forward-wiring, exactly as the file's own comment says.",
    "EIA_API_KEY is inert for a second reason: updater/strategies/fetchers/eia.py line 1 -- \"U.S. Energy Information Administration bulk datasets (api.eia.gov, no key)\". Zero key reads in that module. DIGEST_TO is inert too: updater/send_digest.py:28 falls back to admin@hfdatalibrary.com.",
    "ALL LOCAL KEYS ARE VALID RIGHT NOW. `python jobs/validate_keys.py` against the live publishers: BLS OK status=REQUEST_SUCCEEDED, CPI-U points=24; BEA OK NIPA T10101 rows=100; Census OK status=200, US pop row=['United States','331893745','1']; EIA OK WTI spot last 2026-07-24 = 88.58. 'all keys valid: True'. No credential is expired or rejected.",
    "UN Comtrade live probe (exact shape of jobs/ingest_comtrade.py::_get, BASE/C/A/HS + Ocp-Apim-Subscription-Key): real .env key -> HTTP 200, body `{\"elapsedTime\":\"0.23 secs\",\"count\":1,\"data\":[...]}`. Bogus key control -> HTTP 401 `{\"statusCode\":401,\"message\":\"Access denied due to invalid subscription key...\"}`. The local subscription is live.",
    "FRED live probe: valid key -> HTTP 200 (DGS10 series JSON). Bogus key -> HTTP 400 `{\"error_code\":400,\"error_message\":\"Bad Request.  The value for variable api_key is not registered.\"}`. Absent key -> HTTP 400 `\"Variable api_key is not set.\"`",
    "DEFECT 1 (nyfed, silent degradation on an INVALID key): updater/strategies/fetchers/nyfed.py:100-101 -- `# 4xx (e.g. a retired/invalid FRED id): no data for this series this run, not a break.` / `return []`. FRED answers a revoked/rotated key with 400 (probed above), so a bad FRED_API_KEY makes all 8 series in FRED_MAP (nyfed.py:37-41) return zero observations with no error raised. nyfed.py:44-57 `_api_key()` only guards an EMPTY key (DefinitiveError); it cannot see an invalid one. nyfed is live:true, cadence daily.",
    "DEFECT 2 (bea, same class, PROVEN end-to-end against the publisher): probed apps.bea.gov with UserID=00000000-0000-0000-0000-000000000000 -> HTTP **200** with `{\"APIErrorCode\": \"1\", \"APIErrorDescription\": \"Invalid Request - Invalid API UserId.\"}`. I then executed the SHIPPING classifier on that exact payload: `jobs/ingest_bea_full._classify_error(err)` -> **'nodata'**. ingest_bea_full.py:173 `return []  # nodata / fatal-table -> genuine empty`, and :125 `return \"nodata\"  # default: treat unknown table-level errors as empty`. An invalid BEA key therefore yields empty lists on every call and a clean zero-row run.",
    "DEFECT 3 (census, mislabelled not silent): control probes -- no key -> HTTP 200, Content-Type text/html, `<title>Missing Key</title>`; bad key -> HTTP 200, text/html, `<title>Invalid Key</title>`. census.py:568 matches only `\"missing key\" in r.text[:2000].lower()` -> DefinitiveError naming the credential. An INVALID key misses that test and falls through to census.py:574 `raise TransientError(f\"{SOURCE} {flow}: HTML body on a 200 (upstream error page)\")` -- loud, but permanently retried and reported as an upstream fault rather than a credential fault.",
    "LOUD-AND-CORRECT controls (proving the above are real defects, not a broken reading): comtrade 401 falls to ingest_comtrade.py:103 generic branch -> returns None -> fetchers/comtrade.py:135-144 tally.transient_unit; fred_releases.py:167 `raise DefinitiveError(f\"FRED /releases HTTP {r.status_code}\")` on a 400; bea.py:202-207 raises TransientError when the key is ABSENT. Absence is handled correctly everywhere; INVALIDITY is the hole.",
    "R246 check -- what the runs ACTUALLY did, from data/_aqueduct/state.db unit_state: bea last_attempt 2026-08-03T23:11:53Z, status partial, last_error '+258223 new rows; csv coherence partial: 17459 changed keys unmapped for bea'; census last_attempt 2026-08-03T01:03:21Z, '+13771 new rows; csv coherence unmet ... grain/key-form mismatch'; eia '+150676326 new rows; csv coherence unmet'. All three FETCHED real rows. Not one mentions a key. Their partial status is a catalog-mapping defect, not a credential one."
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  },
  "cso": {
   "root_cause": "code_bug",
   "confidence": "high",
   "failing_units": "36 of 46 sub-units transient-failed in CI run 30796923747 (cso/_all, 2026-08-03T09:25-09:26Z, 113s). The brief's premise is CONFIRMED but over-attributed: only 27 of the 36 carry the \"no subject mapping in catalog\" label; the other 9 are a separate, already-named parse/no-body class that DID print to the log (EDA21, MTD05, MTD06, MTD07, MTD08, MTH05, MTH06, MTH07, MTH08). Reconstructing the run's batch from the live ReadCollection map plus the R2 cursor reproduces both halves exactly: batch = changed[:60] contains exactly 27 matrices absent from the cached catalog and exactly those 9 named matrices -> 27 + 9 = 36. The remaining 24 of the 60 were pulled successfully; the other 10 tally units are subject-parquet publishes.",
   "blast_radius": "222 of the 12,908 currently-live CSO matrices (1.72%) have no entry in the cached _catalog.json, so they can never be routed to an owning subject parquet and are never ingested. Because an unmapped matrix never enters pulled_ok, its release-date cursor never advances (cso.py:369-372), so it re-enters `changed` on every single tick - permanently. `changed` is sorted newest-revision-first, and new tables are by definition newest, so the unmapped set crowds out real work: 27 of the 60 MAX_TABLES slots (45%) in the reconstructed batch are matrices guaranteed to fail again. The source therefore cannot converge over its ~44 subject parquets / 48.9M-row store. The cache's effective as-of date is before 2026-06-11: the 222 unmapped matrices' live RlsLiveDatetimeFrom values span 2026-06-11T11:00:00 to 2026-07-31T11:12:54.53 (73 released in June, 149 in July). It also holds 678 codes no longer present in ReadCollection.",
   "fixable_by_us": true,
   "needs_from_ahmed": "",
   "recommended_fix": "Two one-line-class fixes, both on our side; no publisher contact and no credential needed.\n\n1. Refresh-on-miss in updater/strategies/fetchers/cso.py `_matrix_subject_map()` (lines 111-141): after computing `batch`, if ANY matrix in it is absent from the map, re-fetch the Search catalog from ws.cso.ie ONCE per run, rewrite the cache via blob.write_bytes_atomic, and re-look-up before failing. Add an age bound (e.g. rebuild if the cached object is older than 7 days) so the cache cannot silently freeze again. One extra 14 MB request per run at most, and only when a miss actually occurs.\n\n2. Make jobs/ingest_cso_ireland.py `build_catalog()` bypassable (line 83): add a `force`/`refresh` parameter so `if os.path.exists(CATALOG_FILE)` can be skipped. Without this, fix 1's fallback still cannot reach the network on the workstation, and the same stale bytes keep being re-uploaded to R2.\n\n3. Add a `print(f\"[cso] {mtr}: no subject mapping in catalog\")` to the branch at cso.py:281-285. It is the only failure branch that names itself in the tally but writes nothing to the log - the reason 27 of 36 failures were invisible in CI while the other 9 were diagnosable at a glance.\n\nAfter the fix, the 222 currently-unroutable matrices become fetchable (verified: SIH13 and SIA208 both return outcome='ok' today) and stop consuming 45% of every run's 60-table budget, which is what has been preventing convergence.\n\nOut of scope but observed in the same run and worth a separate ticket: `last_obs=5630-12-31` and `[merge] IMPOSSIBLE DATES: 6,000 of 70,581 row(s) ... 5_Services.parquet are dated after 2200 - e.g. CSO:RSA03:TLIST(A1)=2005:STATISTIC=RSA03C1 -> 4711-12-31` (a swapped time axis), plus the 9 EDA21/MTD*/MTH* no-row matrices, which are a distinct parser-grammar defect, not this one.",
   "evidence": [
    "CI run 30796923747 log (the run that produced the brief's number): `2026-08-03T09:30:45.7859325Z   !! cso                  partial         last_obs=5630-12-31  err=36/46 sub-unit(s) transient-failed; will retry [SIH13: no subject mapping in catalog, SIA2` (orchestrator truncates the list there). Same run: `[orchestrator] <<< cso/_all took 113s, peak_rss=574MB` - 113s against BUDGET_MIN=20 min, so this is NOT a budget deferral (R303 checked and ruled out).",
    "NOT upstream. Live POST https://ws.cso.ie/public/api.jsonrpc method=PxStat.System.Navigation.Navigation_API.Search {\"LngIsoCode\":\"en\"} -> HTTP 200, Content-Type application/json, chunked, 14,201,189 bytes, 13,586 matrices. The response body LITERALLY OPENS with the matrix the run said had no mapping: `{\"jsonrpc\":\"2.0\",\"result\":[{\"MtrCode\":\"SIH13\",\"MtrTitle\":\"At Risk of Poverty Rate Threshold\",\"MtrOfficialFlag\":true,\"ThmCode\":3,\"ThmValue\":\"People and Society\",\"SbjCode\":106,\"SbjValue\":\"Survey on Income and Living Conditions (SILC)\"...`. SIA208 likewise: SbjCode 106, SbjValue \"Survey on Income and Living Conditions (SILC)\", RlsLiveDatetimeFrom 2026-07-31T11:11:37.12.",
    "Live POST ReadCollection (PxStat.Data.Cube_API.ReadCollection, datefrom=2000-01-01, language=en) -> HTTP 200, chunked, 23,736,420 bytes, 12,908 matrices parsed. Set difference against live Search: 0 of 12,908 ReadCollection matrices are absent from Search (0.0%). The publisher's own catalog is complete and internally consistent - no credential, no 429, no 404, no schema change.",
    "The gap is OUR cache. E:\\research\\econfindatalibrary\\data\\clean_full\\cso\\_catalog.json and r2://econ-data/clean_full/cso/_catalog.json are BYTE-IDENTICAL (both sha256 145c39056e6246b9..., both 3,140,483 bytes), hold 13,675 entries / 13,364 distinct MtrCode, and contain neither SIH13 nor SIA208 (`SIH13 in catalog: False`, `SIA208 in catalog: False`). 222 of the 12,908 live matrices have no mapping in it. Control in the same check: AKA03, CPM01, MTD05 all `in catalog: True`, so the probe is not broken.",
    "Exact reconstruction of the failing batch: with the live ReadCollection map and the actual R2 cursor r2://econ-data/clean_full/cso/_collupd.json (2,031 bytes, 61 entries), changed = 12,847; batch = changed[:60] (MAX_TABLES=60) yields `unmapped 27` and `the 9 log-named no-row matrices present in batch: ['EDA21','MTD05','MTD06','MTD07','MTD08','MTH05','MTH06','MTH07','MTH08'] -> 9`. 27 + 9 = 36, matching the reported 36 transient failures exactly. Unmapped list: SIH13, SIA208, SIA207, TRA244-249, TRA257-261, TRA300-311, TEA30.",
    "CONTROL that the matrices themselves are fine: jobs/ingest_cso_ireland.py fetch_table_detailed('SIH13') -> `rows= 24 outcome= ok`, first row ('CSO:SIH13:STATISTIC=SIH13C01:C04136V04902=10', date(2020,12,31), 7.9); fetch_table_detailed('SIA208') -> `rows= 18 outcome= ok`, first row ('CSO:SIA208:STATISTIC=SIA208C01:C02199V02655=1', date(2020,12,31), 5.9). Both fetch and parse cleanly. The ONLY thing blocking them is the local mapping lookup.",
    "updater/strategies/fetchers/cso.py:281-285 - the failing branch: `sbj = m2s.get(mtr)` / `if not sbj:` / `tally.transient_unit(f\"{mtr}: no subject mapping in catalog\")` / `continue`. Note it is the one failure branch in the loop with NO print() - which is why 27 of the 36 failures left zero trace in the CI log while the other 9 each printed a line.",
    "updater/strategies/fetchers/cso.py:122-141 - `_matrix_subject_map()` rebuilds from the Search API ONLY when the cached blob is missing or unparseable (`raw = blob.read_bytes(...)`, then `if not cat: cat = _ingester().build_catalog()`). There is no TTL and no refresh-on-miss: a matrix absent from the cache can never trigger a rebuild, so the cache is frozen at whatever date it was first written.",
    "jobs/ingest_cso_ireland.py:34 `CATALOG_FILE = os.path.join(OUT, \"_catalog.json\")` and :81-86 `def build_catalog(): if os.path.exists(CATALOG_FILE): ... log(f\"Loaded catalog: {len(cat)} tables\"); return cat`. This is the second short-circuit: cso.py's documented recovery path (\"we BUILD one from CSO's own Search API\") returns the same stale bytes and never touches the network on any machine where the local file exists - which is how the stale 3,140,483-byte file got rewritten (mtime 2026-08-02 23:47) and then propagated to R2 unchanged."
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  },
  "abs-ecb-ssb": {
   "root_cause": "budget_deferral",
   "confidence": "high",
   "failing_units": "Zero real failures in all three. Corrected denominators (deferrals removed from `attempted`): abs 0 failed of 417 attempted; ecb 0 of 250; ssb 0 of 1,380. The headline 805/1222, 290/540 and 135/1515 are 100% budget deferrals — 1,230 phantom failures.",
   "blast_radius": "1,230 sub-units mislabelled as transient failures across three sources (abs 805, ecb 290, ssb 135), all of them deferrals. No data harm: status was `partial`, the vintage was not advanced and existing rows were kept, so the deferred units simply rotate to the next tick. The damage is purely reporting — these three account for a large share of the live queue's apparent failure volume. ecb additionally shows throughput decay across three consecutive runs at a constant 540 denominator and constant ~2,105 s duration: real attempted fell 297 (08-01) -> 288 (08-02) -> 250 (08-03) while deferrals rose 243 -> 252 -> 290. That is not the R303 bug and its cause is NOT established.",
   "fixable_by_us": true,
   "needs_from_ahmed": "",
   "recommended_fix": "Do not re-fix the R303 mislabel — it is already fixed and guarded. Three things remain. (1) Nothing needs to happen for the numbers to become honest except letting abs/ecb/ssb run once on post-664f7b82 code; the stale last_error strings are overwritten on the next successful tick, and the same three will then report 'N attempted, none failed; M deferred by budget'. If the queue/digest is being triaged before then, treat abs 805, ecb 290 and ssb 135 as zero failures. (2) Fix the three now-false comments (abs.py:173-174, ecb.py:493-494, ssb.py:520-521) that still say deferred units are 'recorded transient' — leaving wording that contradicts behaviour is what caused the first sweep to miss ten fetchers; consider extending the grep guard test to flag 'recorded transient' text inside a dl.spent() block. (3) Separately from R303, investigate ecb's declining coverage: real attempted fell 297 -> 288 -> 250 over 2026-08-01/02/03 at a fixed 540 denominator and a fixed ~2,105 s budget, so the 35-minute slice is buying less each day and the rotation may not be draining the tail. Cause not established.",
   "evidence": [
    "PREMISE CORRECTED — all three already use deferred_unit() at every budget-break site. E:\\research\\econfindatalibrary\\updater\\strategies\\fetchers\\abs.py:179 `tally.deferred_unit(f\"{flow} deferred (budget {BUDGET_MIN:.0f} min)\")`; ecb.py:495 `tally.deferred_unit(f\"{fn}: budget {BUDGET_MIN:.0f} min spent, deferred\")`; ssb.py:522 (group) and ssb.py:550 `tally.deferred_unit(f\"{tid}: budget spent, table deferred\")`. Grepping BUDGET/deadline/dl.spent()/transient_unit/deferred_unit across all three files found no budget path still routed to transient_unit().",
    "Fix commit `git show -s --format=%cI 664f7b82` -> 2026-08-03T09:52:43-05:00 (= 14:52:43Z), 'tally: a budget deferral is not a transient failure (11 fetchers)', naming abs/ecb/ssb explicitly and quoting the exact strings in the brief ('ecb recorded \"252/540 sub-unit(s) transient-failed\", abs \"805/1222\"'). Extended 2026-08-03T10:26:02-05:00 by e88178cb ('the deferral class was TWICE what I shipped — 10 more fetchers').",
    "THE BRIEF IS READING STALE STATE. data/_aqueduct/state.db unit_state.last_attempt_utc: abs 2026-07-30T10:31:20+00:00 (4 days BEFORE the fix), ssb 2026-08-01T09:23:08+00:00 (2 days before), ecb 2026-08-03T10:08:33+00:00 (4h44m before the 14:52:43Z commit). SQL `select source_id,ts_utc from runs where source_id in ('abs','ecb','ssb') and ts_utc > '2026-08-03T14:52:43'` returned [] — none has re-run since the fix, so the pre-fix strings are still on display.",
    "ALL 1,230 ARE DEFERRALS — arithmetic proof, not inference. In the pre-fix tree (`git show 664f7b82^`) the ONLY transient_unit() call sites passing a LABEL are the budget ones (abs.py:179, ecb.py:495, ssb.py:522/550); every real-failure site passes none (abs.py:195,231; ecb.py:543,559,563,607; ssb.py:564,611). _common.py:236 _named() emits one id per labelled unit plus an explicit '+N more'. Parsing each stored error: abs 6 shown + 799 more = 805 ids vs transient count 805; ecb 6 + 284 = 290 vs 290; ssb 6 + 129 = 135 vs 135. ids == count exactly in all three, so every counted unit carried a budget label. Zero unlabelled (i.e. real) failures.",
    "Tally already separates the two: _common.py:216 deferred_unit() docstring — 'Deliberately does not touch `attempted`'; _common.py:274 finalize() emits the transient message, _common.py:279 the deferral message ('N sub-unit(s) attempted, none failed; M deferred by budget and taken next tick').",
    "Regression guard exists and passes: `python -m pytest tests/test_tally_deferred_is_not_a_failure.py -q` -> '7 passed in 0.11s'. It includes a repo-wide grep guard (test file line 146-151) that fails any fetcher regressing a deferral onto transient_unit().",
    "CONTROL — ABS live at its own publisher: GET https://data.api.abs.gov.au/rest/dataflow/ABS?detail=allstubs -> HTTP 200, ct=application/vnd.sdmx.structure+xml;version=2.1, body starts '<?xml version=\"1.0\" encoding=\"utf-8\"?>\\n<!--NSI Web Service v8.19.9.0-->'. Then a literal DEFERRED unit named in abs's error: GET https://data.api.abs.gov.au/rest/data/ABS_SEIFA_LGA/all?format=csvfile&startPeriod=2011 -> HTTP 200, 1,330,581 bytes, header 'DATAFLOW,LGA_2011,INDEX_TYPE,MEASURE,TIME_PERIOD,OBS_VALUE,UNIT_MEASURE,OBS_STATUS,OBS_COMMENT' — exactly the shape the parser expects. It would have succeeded had the budget reached it.",
    "CONTROL — ECB live: GET https://data-api.ecb.europa.eu/service/dataflow -> HTTP 200, 147,052 bytes SDMX structure XML. Then the flow named in ecb's deferral labels (ECB__CSEC__M__LU__*.parquet): resolved CSEC's DSD to ESTAT:NA_SEC (18 dimensions) from the enumeration, then GET https://data-api.ecb.europa.eu/service/data/CSEC/M..LU...............?startPeriod=2025-01-01&detail=dataonly -> HTTP 200, ct=text/csv, 44,436,952 bytes, header 'KEY,FREQ,ADJUSTMENT,REF_AREA,...,TIME_PERIOD,OBS_VALUE' with first row 'CSEC.M.N.LU.W0.S1.S1.N.L.F.F3.L._Z.EUR.EUR.F.V.N._T,M,N,LU'. (Two earlier 400/404s were my own wrong key arity, corrected against the DSD — not publisher faults.)",
    "CONTROL — SSB live: the two literal deferred tables named in ssb's error. GET https://data.ssb.no/api/v0/en/table/Fbu03 -> HTTP 200, application/json, 3,301 bytes, '{\"title\":\"14156: Expenditure per household per year, by commodity and service group...\",\"variables\":[...]}'. Fbu04 -> HTTP 200, 3,467 bytes, title '14157: ...'. Requests paced 1.5 s apart.",
    "Alternative root causes ruled out by those probes: no 401/403 anywhere (not credential); no 404/410/DNS/TLS failure on any real endpoint (not endpoint_gone); no 429 and no Retry-After header on any response (not rate_limited); the parsers' expected columns are all present in the live bodies (not schema_drift/upstream_recode).",
    "RESIDUAL DEFECT 1 — comment/code drift at all three deferral sites, the exact trap that made the first sweep miss half the class. abs.py:173-174 'Deferred flows are recorded transient, so the run is `partial`'; ecb.py:493-494 'deferred files are recorded transient'; ssb.py:520-521 'the deferred groups are recorded transient'. All three sit directly above a deferred_unit() call and are now false. Commit e88178cb's own message: the second pass had to be 're-derived by BEHAVIOUR ... Wording is incidental' precisely because comments lied.",
    "RESIDUAL DEFECT 2 — _common.py:274 tests `if tally.transient:` BEFORE _common.py:279 `if tally.deferred:`, so a run with 1 real transient and 800 deferrals reports '1/N transient-failed' and the deferral count disappears from the message entirely. Deliberate (a test asserts a real transient still surfaces when both occur), but the deferred information is lost rather than merged."
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  },
  "ilostat": {
   "root_cause": "budget_deferral",
   "confidence": "high",
   "failing_units": "Zero units failed. 675 of 1,956 were attempted and all 675 succeeded; the other 1,281 were never attempted — the 25-minute Deadline stopped the sweep at sorted index 675 and the pre-2026-08-03 code counted those untouched units through transient_unit(), so they were reported as failures and also inflated the `attempted` denominator. Under the current code the same run reports \"675 sub-unit(s) attempted, none failed; 1281 deferred by budget and taken next tick\".",
   "blast_radius": "No data loss and no broken endpoint — 126,799,245 rows were published on 2026-07-30. What is real is coverage lag: 1,281 of 1,956 indicator-frequency ids (265,102,798 of 391,910,883 catalog rows, 68%) have not been refreshed since the 2026-06-03 first pass, and logs/_untouched_sweep.txt:118 independently records \"ilostat 1,274 of 1,949 file(s) (65%) NOT written since 2026-07-16\". The source has had exactly one updater run, is on monthly cadence, and shows ATTENTION/_all:partial in logs/_health_gate.log:37.",
   "fixable_by_us": true,
   "needs_from_ahmed": "",
   "recommended_fix": "The mislabel is already fixed (664f7b82, on origin/main) — re-run and the message becomes an honest deferral. What still needs doing is the coverage arithmetic, which the fix does not touch: measured throughput was 675 indicators / 25 min ≈ 2.2 s each, so a full sweep of 1,956 needs ~72 min but BUDGET_MIN=25 (ilostat.py:51) buys ~35% of it, and ILO restamps in bulk (350 of the 675 already-published ids went dirty within 4 days), so roughly half of each future budget is consumed re-walking the alphabetical prefix before the tail is reached. Two changes, either of which works: (1) add `run_location: local` to the ilostat registry entry (updater/registry.yaml:2530) so it runs on the workstation, where tools/run_local_heavy.ps1 sets AQUEDUCT_BUDGET_MIN_OVERRIDE and the 25-min cap is lifted (this is exactly why abs/bea were routed local); or (2) raise BUDGET_MIN to ~90 and add load_rotation/save_rotation/rotate_after (updater/strategies/fetchers/_common.py:534-587) so a budget-bounded run does not always restart at the alphabetical top. Do (1) first — it needs no code change and drains the 265M-row backlog in one pass. Separately, ilostat.py:136-138 swallows the exception text (`except Exception: tally.transient_unit(iid)`), so a REAL per-indicator failure here is currently undiagnosable from state.db; record str(e)[:120] with the id.",
   "evidence": [
    "state.db unit_state(ilostat,_all): status='partial', last_attempt_utc='2026-07-30T23:15:01+00:00', obs_count=126,799,245, last_error=\"1281/1956 sub-unit(s) transient-failed; will retry [EMP_DWAP_SEX_EDU_GEO_RT_Q, EMP_DWAP_SEX_EDU_MTS_RT_A, EMP_DWAP_SEX_EDU_MTS_RT_Q, EMP_DWAP_SEX_EDU_RT_A, EMP_DWAP_SEX_EDU_RT_M, EMP_DWAP_SEX_EDU_RT_Q, +1275 more]\" — the exact string in the brief.",
    "state.db runs id=351: ilostat, dur_s=1535.8 s. E:\\research\\econfindatalibrary\\updater\\strategies\\fetchers\\ilostat.py:51 sets BUDGET_MIN = 25 (=1500 s). The run ended one budget-length after it began (previous source idb finished 22:49:24Z; 23:15:01Z − 1535.8 s = 22:49:25Z, orchestrator is serial). The run spent its budget, it did not fail.",
    "git show 599a8b4c:updater/strategies/fetchers/ilostat.py (the code in force on 2026-07-30) — the budget branch read `if dl.spent(): deferred += 1; tally.transient_unit(iid)   # deferral, not a verdict`. The comment said deferral while the call said failure. Fixed 2026-08-03 in 664f7b82 → `tally.deferred_unit(iid)  # deferral, not a verdict (R303)`; 664f7b82 is on origin/main. Its own commit message names this case: \"ilostat's call site carried the comment 'deferral, not a verdict' while doing the opposite, and its 1,281 'failures' were the largest count in the live queue.\"",
    "Arithmetic proof that ZERO of the 1,281 were real failures: 1956 − 1281 = 675 units attempted, and the first named 'failing' id EMP_DWAP_SEX_EDU_GEO_RT_Q sits at 0-based index 675 in the live TOC sorted by id (i.e. the 676th — the first one never reached). The six named ids are the six alphabetically consecutive ids at that boundary. transient_ids is append-ordered (updater/strategies/fetchers/_common.py:204-209), so a genuine exception among the first 675 would have been named first. None was.",
    "LIVE PROBE, TOC: GET https://rplumber.ilo.org/metadata/toc/indicator?lang=en&format=.csv → HTTP 200, 737,669 bytes, 1.4 s, Server: cloudflare. Parsed 1,956 rows, last.update populated 1956/1956. Every named 'failure' is present with real records: EMP_DWAP_SEX_EDU_GEO_RT_Q n.records=228,440 last.update=29/07/2026 07:16:23; EMP_DWAP_SEX_EDU_RT_A n.records=95,240 last.update=31/07/2026 20:49:23.",
    "LIVE PROBE, per-indicator bulk (GET https://rplumber.ilo.org/data/indicator?id=<ID>&format=.parquet, paced 1.6 s apart, streamed 8 bytes each): EMP_DWAP_SEX_EDU_GEO_RT_Q → HTTP 200, 0.8 s, magic b'PAR1\\x15\\x04\\x15\\xa6'; EMP_DWAP_SEX_EDU_RT_A → HTTP 200, 0.8 s, b'PAR1\\x15\\x04\\x15\\xf4'. Both named 'failures' serve valid parquet sub-second. No Retry-After, no X-RateLimit-* header on any response.",
    "CONTROL (same probe against known-good): CCF_XOXR_CUR_RT_A, an id from the prefix that DID succeed on 07-30 → HTTP 200, 1.0 s, b'PAR1...'. Plus far-tail UNE_TUNE_SEX_MTS_NB_Q → HTTP 200, 0.7 s, PAR1 and mid-tail EMP_TEMP_SEX_MJH_MTS_NB_A → HTTP 200, 1.2 s, PAR1. 5/5 probes 200+PAR1 — the probe is not broken, and the defect is not per-indicator.",
    "Not disk/resource exhaustion on our side: the entire on-disk ilostat corpus is 2,055,232,379 B (2.06 GB) across 1,947 parquets, of which the 674 files preceding the boundary are only 691,146,226 B (0.69 GB).",
    "CHURN MEASUREMENT (why the tail may not drain): live TOC last.update by date — 913 ids stamped 2026-07-29, 734 on 2026-07-31, 59 on 2026-08-01. Of the 675 indicators published on 07-30, 350 have been restamped since, so the next run re-fetches them FIRST (the loop walks `sorted(rows, key=id)` — ilostat.py:119) and only ~325 units of budget reach the 1,281-unit tail.",
    "Scale still owed: TOC n.records for the 1,281 deferred ids = 265,102,798 rows of a 391,910,883-row catalog; state.db records 126,799,245 published. ilostat has run through the updater exactly ONCE (SELECT COUNT(*) FROM runs WHERE source_id='ilostat' → 1), cadence: monthly (updater/registry.yaml:2543)."
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  },
  "stat_slovenia": {
   "root_cause": "code_bug",
   "confidence": "high",
   "failing_units": "1 of 85 attempted sub-units in the 2026-08-04 slice: table 2221405S.px (group 222, \"Public scheduled road passenger transport (interurban and international) by MEASURES, INTERURBAN/INTERNATIONAL TRANSPORT, DISTANCE and YEAR\"). The fetcher never labels its structural_unit() calls, so the id had to be recovered by re-running the fetcher's own code read-only. NOTE: the brief's premise is WRONG — there is no shape mismatch. The response shape is exactly what the parser expects; SURS published an empty (all-null) 2024 year slot on a table last updated 2018-10-19, and our inline gate misreads a 36-element all-null array as a \"non-trivial body\".",
   "blast_radius": "Zero rows lost — merge is untouched and 249.parquet grew 501,866 -> 876,614 rows during the same run. But the ONE sub-unit sinks the ENTIRE source: finalize() raises DefinitiveError, update() never returns a Result, and orchestrate.py:937-939 records 'partial' while discarding last_obs_date, obs_count, series_cursors and last_success_utc. Measured in data/_aqueduct/state.db: unit_state(stat_slovenia,_all) has last_success_utc=None, last_obs_date=None, obs_count=None, and there is NO source_state row at all. Every recorded run since 2026-07-14 ended this way (2/442, 4/510, 3/3, 1/85), so the source has never gone green and, because series_cursors is dropped, _derive_changed_csvs has never run for it — published CSVs never follow the parquet. Scale: 4,696 tables / 145 groups / 150 group parquet files. Same defect class present in 2 sibling fetchers (hagstofa.py:382, statfin.py:590); hagstofa's run 9 minutes later on 2026-08-04 produced the identical error shape (2/1074, offenders UMH11120.px and UMH11165.px).",
   "fixable_by_us": true,
   "needs_from_ahmed": "",
   "recommended_fix": "Two-line change in updater/strategies/fetchers/stat_slovenia.py. (1) At lines 735-737 replace the inline gate with the shared rule already used by bfs and stat_estonia: `from ._common import structural_on_zero_rows` and `if structural_on_zero_rows(safe_max, resp): tally.structural_unit(tid) else: current += 1`. Use `safe_max` (computed at line 682), not the raw `stored_max`, which is what that helper's contract requires. This makes an all-null value array — a period slot SURS published ahead of its data — a confirmed-quiet sub-unit instead of a fabricated schema break, and 2221405S.px stops sinking the source. (2) Pass a label to every tally.structural_unit()/transient_unit() call (lines 623, 634, 662, 737) so the error message names the offending table id; _common._named already renders up to 20 of them and orchestrate._clip_err now carries 1400 chars. Without this the finding costs a 40-minute bisect, which is exactly why it has sat unfixed since 2026-07-14. Then apply the same fix to the two siblings in the class: hagstofa.py:382 and statfin.py:590. Nothing here is a data repair — no rows were ever lost; the fix is purely to stop a valid empty response from being reported as a structural break and blocking last_success_utc, the vintage bump and the CSV re-derive for all 4,696 tables. Separately (lower priority, spotted in passing, not the cause): stat_slovenia strips '.px' case-sensitively at lines 619/606, so catalog ids ending '.PX' keep the suffix in their series_key and cursors (e.g. '2490109S.PX'), and the fetcher never calls Tally.deferred_unit(), so its 40-minute budget stop is invisible to finalize() and cannot be reported as an R303 deferral.",
   "evidence": [
    "state.db unit_state(stat_slovenia,_all): last_error='stat_slovenia: 1/85 sub-unit(s) returned 200 but parsed 0 rows from a non-trivial body (schema/structural break); existing data kept', status='partial', last_success_utc=None, last_obs_date=None, obs_count=None; no source_state row exists for stat_slovenia",
    "state.db runs table, all 6 stat_slovenia runs: id156 2026-07-14 '2/442'; id174 2026-07-15 transient_fail ConnectionReset; id379 2026-08-01 killed at 45-min cap; id394 2026-08-02 '4/510'; id439 2026-08-03T06:22:04 '3/3'; id465 2026-08-04T02:02:47 '1/85' (dur 2409.1s)",
    "Read-only replica of the real fetcher (imports updater.strategies.fetchers.stat_slovenia, merge_and_write and blob.write_bytes_atomic neutered) printed: '[STRUCTURAL] 2221405S.px  posted=True value_len=36 nonnull=0'",
    "LIVE probe I ran: GET https://pxweb.stat.si/SiStatData/api/v1/en/Data/2221405S.px/ -> 200 application/json; variables MERITVE(2), 'MEDKRAJEVNI/MEDNARODNI PREVOZ'(3), RAZDALJA(6), LETO time=True n=15 last5=['2020','2021','2022','2023','2024']",
    "LIVE probe: POST same URL with the fetcher's exact date-tail body (LETO=['2024']) -> 200, 1790 bytes, first bytes '{\"class\":\"dataset\",\"label\":\"Public scheduled road passenger transport (interurban and international) by MEASURES, INTERURBAN /INTERNATIONAL TRANSPORT, DISTANCE and YEAR\",\"source\":\"Statistical Office of Republic Slovenia\",\"updated\":\"2018-10-19T08:30:00Z\",\"id\":[...'; id=['MERITVE','MEDKRAJEVNI/MEDNARODNI PREVOZ','RAZDALJA','LETO'], size=[2,3,6,1], value=[None x36], status={'0':'-','6':'...','24':'...',...} (PxWeb suppression codes: '-' no occurrence, '...' not available)",
    "CONTROL, same table + same query shape, LETO=['2017','2018','2024'] -> 200, size=[2,3,6,3], 70 non-null of 108: value starts [31903, 28612, None, 19556, 17146, None, 10981, 10163, None, ...] — every third element (the 2024 slice) is None while 2017/2018 are fully populated. Identical dimension ids and cube shape; the parser handles it. Proof there is NO schema drift — only an empty year slot.",
    "On-disk frontier, data/clean_full/stat_slovenia/222.parquet: SI:2221405S:* has 372 rows, min obs_date 2010-12-31, max obs_date 2023-12-31 — so the date-tail correctly selects LETO=2024 and will re-trigger this every run forever.",
    "DEFECT, updater/strategies/fetchers/stat_slovenia.py:735-737 — `has_body = bool(resp.get(\"value\")) if isinstance(resp, dict) else False` / `if stored_max is not None and has_body: tally.structural_unit()`. bool([None]*36) is True.",
    "CORRECT RULE ALREADY IN THE REPO, updater/strategies/fetchers/_common.py:645-671 structural_on_zero_rows(): docstring says 'every value is null -> a period slot published ahead of its data, not a break', implemented as `any(v is not None for v in (vals or []))`. Used by bfs.py:422 and stat_estonia.py:621; stat_slovenia never adopted it, and it also passes the RAW stored_max instead of the safe_max it already computed at line 682.",
    "CONTROL that the probe itself is sound: the same read-only replica run against group 249 (89 tables) completed with 89 live 200s, returned status='no_change', obs=876,614 and 92 series cursors (e.g. '2490002S': '2026-12-31') — endpoint, no-auth access, resolver and parser are all healthy.",
    "CLASS SWEEP (grep over updater/): inline value-truthiness gates at stat_slovenia.py:735, hagstofa.py:382 (`body_has_values`), statfin.py:590 (`nonempty_values`); only bfs.py:422 and stat_estonia.py:621 call structural_on_zero_rows(). runs id466 2026-08-04T02:11:56 confirms hagstofa hit the same thing: '2/1074 sub-unit(s) returned 200 but parsed 0 rows ... [1_natturufar/2_vedurfar/UMH11120.px, 1_natturufar/2_vedurfar/UMH11165.px]'",
    "3/3 -> 1/85 EXPLAINED (rotation, not a health change): the Aug-04 run's own log logs/slo_diag_202235.log:4 '145 group(s), 4,696 table(s) to consider; starting at offset 90 (218)' and line 97 'budget of 40 min spent after 40.1 min - stopping cleanly after 92 of 145 group(s)'; data/clean_full/stat_slovenia/_sweep_offset.json now reads {\"next_group\": 37} = (90+92) mod 145. Group '222' is at sorted index 93, inside the Aug-04 slice (90..144 + 0..36) and outside the Aug-03 slice, which must have ended at index 89 for the next run to start at 90. Both numerators/denominators are tally.attempted, which EXCLUDES tables found already current (counted in the separate `current` variable at stat_slovenia.py:644,664,695,716,739) — so '3/3' means only 3 tables in that slice had anything to do and all 3 were offenders, and '1/85' means 85 had work and 1 was an offender. Different tables, different slice, no change in health."
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  },
  "stat_estonia": {
   "root_cause": "code_bug",
   "confidence": "high",
   "failing_units": "Zero sub-units failed upstream. In my sandbox run 54 of 54 attempted tables succeeded (0 transient, 0 structural) and 2,778 were deferred by budget. In CI run 30799503843 the whole unit stat_estonia/_all was killed at 2,700 s, so the tick's per-table outcomes were never tallied or reported at all. The sweep needs 5-6 ticks of 4,978 tables; no single tick can cover more than ~1,100 tables inside 18 minutes.",
   "blast_radius": "No data loss and no data staleness caused by this. All 7 subject parquets are intact — 17,470,207 rows total (Lepetatud_tabelid 7,775,126; rahvaloendus 3,149,883; majandus 2,557,706; sotsiaalelu 1,417,750; rahvastik 1,309,947; eri-valdkondade-statistika 851,078; keskkond 408,717) — and merge_and_write is never-shrink, so every failure branch preserves them. What is affected is COVERAGE VELOCITY and REPORTING: the source has never stamped last_success_utc, shows as ATTENTION/transient_fail (attn=_all:transient_fail, 8d stale as of 2026-08-03), and under the current 18-min budget a full 4,978-table rotation needs ~5 ticks at 7-day partial spacing, i.e. any single table's new period can wait up to ~5 weeks to be picked up. The 2026-08-03 kill also cost the run 45 minutes of its 240-minute budget and produced nothing (statfin, adb, worldbank_esg were all skipped NOT DUE right after it).",
   "fixable_by_us": true,
   "needs_from_ahmed": "",
   "recommended_fix": "Three parts, in order of payoff.\n\n(1) ALREADY DONE, NEEDS CI PROOF. HEAD (ad6360b0, updater/strategies/fetchers/stat_estonia.py:534-553) checks dl.spent() inside the per-table loop and writes a table-grain bookmark (_rotation_table.json) while winding the subject bookmark back to prev_subj. I ran the real HEAD update() in a sandbox data root with a 2-minute budget: it stopped at 2.02 min, printed the budget message, returned status='partial' with an honest deferral tally, and wrote both bookmarks. Worst-case overshoot is now budget(18) + one in-flight table (MAX_ATTEMPTS 5 x DATA_TIMEOUT 120 s + backoff = ~11 min ceiling) + one subject read (~1-3 min) = ~32 min, comfortably under AQUEDUCT_UNIT_TIMEOUT_MIN=45 (orchestrate.py:152). Just let one updater-daily run execute and confirm the budget line appears instead of TIMEOUT — no daily run has happened since the fix landed.\n\n(2) STOP PAYING 46 MINUTES PER SWEEP FOR DEAD TABLES. Lepetatud_tabelid (\"discontinued tables\") is 2,832 of 4,978 sub-units and 99.0% of the ones holding data have max(obs_date) <= 2024. Give that subject a long-cadence gate — e.g. visit it on one tick in N, or skip any table whose stored_max is older than ~3 years unless a cheap catalog/updated signal says otherwise. Dropping it from the hot path cuts the sweep from ~83 min to ~37 min, which fits in TWO ticks instead of five and lets the live subjects refresh weekly instead of five-weekly.\n\n(3) MAKE A COMPLETED SWEEP POSSIBLE, OR THE SOURCE CAN NEVER READ GREEN. As written, every tick defers, so finalize() always returns 'partial' (_common.py:279-288), last_success_utc is never stamped, and stat_estonia is permanently ATTENTION even when nothing is wrong. Either (2) above must make a full sweep fit one tick, or the source needs a completed-rotation signal (e.g. stamp success when the table bookmark wraps back to the first subject) so a healthy multi-tick sweep is distinguishable from a broken one. Do not just raise STAT_ESTONIA_BUDGET_MIN — 18 was already chosen to sit under the 45-min cap, and the arithmetic says one sweep needs 83 min, which no per-source budget under a 45-min hard cap can deliver.\n\nSeparately worth noting but not the root cause: peak_rss hit 6,663 MB on that run, driven by _max_by_table's to_pylist() over 7.78M rows; a pyarrow-native group-max would remove that spike.",
   "evidence": [
    "CI run 30799503843 (job 91647575435) is the run behind the brief. Log: `2026-08-03T11:00:46Z [orchestrator] >>> stat_estonia/_all` ... `2026-08-03T11:45:46Z [orchestrator] TIMEOUT stat_estonia/_all - stat_estonia/_all exceeded its 45-minute hard limit and was interrupted` / `[orchestrator] <<< stat_estonia/_all took 2,700s, peak_rss=6,663MB`. Between those two lines the fetcher printed NOTHING - only the orchestrator's [mem] heartbeat. No budget message, so the fetcher's own Deadline never fired.",
    "That run's headSha is 6bd8c0036724575fb6ee3df73f01e2ac8aa9b51a (2026-08-03 03:51 CDT). `git merge-base --is-ancestor 272faee5 6bd8c003` -> DOES NOT contain 272faee5. In that sha's copy of the fetcher, `grep -n` gives: line 431 `budget_min = float(os.environ.get(\"STAT_ESTONIA_BUDGET_MIN\", \"18\"))`, line 444 `if dl.spent():` (subject loop), line 482 `for t in subj_tables:` - and NO dl.spent() anywhere inside the table loop. An 18-min budget checked only between subjects cannot stop a subject that is itself ~46 min of work.",
    "Catalog size, measured from data/clean_full/stat_estonia/_catalog.json: 4,978 tables. By subject: Lepetatud_tabelid 2832, rahvaloendus 676, sotsiaalelu 603, majandus 494, eri-valdkondade-statistika 163, rahvastik 160, keskkond 50. One subject is 2,832/4,978 = 56.9% of the sweep.",
    "LIVE PUBLISHER PROBE (my own, paced 1.2 s apart, 20 tables across Lepetatud_tabelid and majandus, replicating _get_meta + _build_query + _post_data + ing.parse_jsonstat2 verbatim): every request HTTP 200. Examples: `Lepetatud_tabelid/Sotsiaalelu. Arhiiv/Kultuur. Arhiiv/KUT53.PX GET 200 0.68s len=1876 POST 200 0.66s len=24946 n_new_periods=1 rows=0`; `majandus/majandusuksused/uldandmed/ER005.PX GET 200 0.64s POST 200 0.49s len=2911 rows=168`. Totals: 7.37s/10 tables and 7.21s/10 tables -> 0.72 s/table of network. No 429, no 5xx, no non-JSON body, no auth challenge, no DNS/TLS problem, and the parser still returns rows - so this is not credential/endpoint_gone/rate_limited/schema_drift/upstream_recode. This probe is also the control: the same probe that returned 0.72s/200 for stat_estonia is the code path that failed in CI, so the probe is not broken.",
    "CONTROL RUN of the CURRENT HEAD fetcher (ad6360b0, `git diff --stat HEAD` on stat_estonia.py/orchestrate.py/base.py/_common.py = empty) against a sandbox AQUEDUCT_DATA_ROOT with STAT_ESTONIA_BUDGET_MIN=2 and RATE patched 0.25->1.0 for polite pacing. Output: `[stat_estonia] budget of 2 min spent after 2.0 min INSIDE subject 'Lepetatud_tabelid' - completed 54 of 2832 table(s); resuming after 'Lepetatud_tabelid/Majandus. Arhiiv/Majandusyksused. Arhiiv/ER073.PX' next tick (subject bookmark held at '')`, `RESULT status='partial' obs=7775126 error='54 sub-unit(s) attempted, none failed; 1 deferred by budget and taken next tick [Lepetatud_tabelid: 2778 table(s) after ...ER073.PX]'`, `WALL 121.4s (2.02 min) tables_visited=54`. It stopped 1.4 s past a 120 s budget, wrote both bookmarks correctly (_rotation.json={\"after\":\"\"}, _rotation_table.json={\"after\":\"Lepetatud_tabelid|...ER073.PX\"}). NOT stuck, NOT looping.",
    "TICKS FOR A FULL SWEEP (measured inputs, not guessed): per-table production cost = 0.72 s network (measured) + RATE 0.25 s (stat_estonia.py:92) = ~0.97 s. 4,978 x 0.97 = ~4,830 s = ~80.5 min of table work. Plus the pre-loop `_max_by_table` read of each subject parquet, timed locally over all 7 files (17,470,207 rows total): 75.8 s = 1.26 min (Lepetatud_tabelid alone 33.55 s for 7,775,126 rows). Full sweep ~82-85 min. Against `budget_min` 18 (stat_estonia.py:436) that is ceil(83/18) = 5 ticks, 5-6 once resumed ticks re-pay the subject read. Cross-check against CI: 2,700 s / 0.97 s = ~2,780 tables, consistent with the 2,146 non-Lepetatud tables it started on (rotation bookmark was 'Lepetatud_tabelid') plus ~600 of Lepetatud before the kill.",
    "THE COST DRIVER IS DEAD DATA. `Lepetatud_tabelid` = discontinued/archive tables. Of the 1,869 tables in that subject that have stored data, 1,850 (99.0%) have max(obs_date) in 2024 or earlier - modes at 2007 (266 tables), 2016 (187), 2017 (168), 2019 (132), 2010 (119). My live probe confirms they yield nothing: stored_max=2007-12-31 / 2015-12-31 / 2018-12-31 / 2019-12-31 all resolved to `NO date-tail (empty_unit)` after one 0.65 s GET. ~46 min of every 83-min sweep is spent re-polling terminated tables.",
    "STEADY STATE AFTER THE FIX IS A PERPETUAL PARTIAL. _common.py:279-288 - finalize() returns status='partial' whenever tally.deferred is set. orchestrate.py:907-914 only advances last_success_utc on a clean success. base.py:122-132 - a partial reschedules at min(CADENCE_DAYS['monthly']=28, PARTIAL_RETRY_DAYS)=7 days (base.py:27). Since rotate_after (_common.py:575-587) offers all 7 subjects every tick and no tick can finish 4,978 tables in 18 min, EVERY tick defers, last_success_utc is never stamped, and one complete rotation takes ~5 ticks x 7 days = ~5 weeks.",
    "No updater-daily run has executed since the fix: `gh run list --workflow updater-daily.yml` newest = 30799503843 created 2026-08-03T08:59:07Z, while 272faee5 landed 09:16 CDT (14:16Z) and ad6360b0 18:26 CDT (23:26Z). The inner-loop cap is proven locally, unproven in CI.",
    "Health line from the same run: `ATTENTION stat_estonia monthly 0d 8d 2026-07-26 attn=_all:transient_fail` and `!! stat_estonia transient_fail last_obs=2026-12-31 err=stat_estonia/_all exceeded its 45-minute hard limit...`. transient_fail is deliberately excluded from the partial-reschedule rule (base.py:111-112), so the killed source stayed immediately retryable - it was not stranded."
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  },
  "small-batch: ember, idb, ksh_stadat, fred_releases, sec_edgar, worldbank_esg (econdatalibrary updater)": {
   "root_cause": "code_bug",
   "confidence": "high",
   "failing_units": "THE BRIEF'S PREMISE IS PARTLY WRONG — these are four different diseases, not one. (1) ember 4/48 and (2) idb 10/40 = SAME defect, ours, permanent: merge.merge_and_write's never-shrink guard refuses forever because the stored parquets carry non-unique dedup keys. ember's 4 are exactly methane_chart_satellite_emissions, necp_Ember_NECP_data_2024, turkiye_..._srmc_chart, turkiye_..._unlicensed_capacity_chart (all 4 reproduced; a 5th, power_price_chart_eng, merges clean as control). idb's failures are the 11 social-indicators-of-latin-america resources in its fixed first-40 window (reported 10, I measured 11 that trip today — one may have entered the window after the run). (3) ksh_stadat 1/60 = NOT a defect: www.ksh.hu connect-timeouts on ene0009.csv / ido0015.csv on 2026-07-31; host answers 200 today. (4) fred_releases 1/165 = NOT a failure and NOT stale-by-accident: the source was DELETED from the registry on 2026-07-23 as non-redistributable; the row is a 40-day-old fossil. (5) sec_edgar 1/8 = one key, edgar_insider/2026q2, from the already-root-caused pandas-3.0.5 us->ns cast bug; the failing attempt (11:00:46Z) PREDATES the fix commits (11:55Z / 12:03Z) by ~1 hour and nothing has re-attempted it since. (6) worldbank_esg 8/9 = a World Bank 502 storm, self-healed — but the source has a MUCH worse hidden defect (see blast_radius): 13 of its 71 indicator ids were re-coded upstream and our code files the resulting error envelope as \"empty\", silently, forever.",
   "blast_radius": "ember: 4 of 65 stored datasets frozen (9,325 + 4,813 + 1,058 + 195 rows) and they can never unfreeze on their own — the sidecar is deliberately not advanced, so they retry and fail every tick. idb: 11 of 40 fail every run, and worse, idb has NO rotation bookmark (idb.py:121 `todo.sort(key=lambda x:x[2])` + idb.py:130 `todo[:MAX_PER_RUN]`), so every run re-walks the SAME 40 lowest-uuid resources out of 1,915 owing — 1,875 resources have never been reached by the updater at all; health.json shows idb newest_obs=2019-12-31 with 178/178 series discontinued. Also self-perpetuating: empty_unit advances the sidecar without writing a parquet, so `sidecar==lm and blob.exists(path)` can never both be true and those resources stay in `todo` permanently. worldbank_esg: measured live, source=75 now publishes 80 indicators; 13 of our 71 stored ids are GONE from it (CC.EST, GE.EST, PV.EST, RL.EST, RQ.EST, VA.EST re-coded to GOV_WGI_*; EN.ATM.CO2E.PC / METH / NOXE / GHGR re-coded to the EN.GHG.*.AR5 family; plus IC.LGL.CRED.XQ, NY.GDP.MKTP.KD.ZG, SI.SPR.PCAP.ZG) and 22 published indicators we never fetch — all 13 store files are still stamped 2026-06-30, their first-pass date, never updated once. sec_edgar: 1 missing partition (edgar_insider 2026q2); edgar_13f is complete 53/53. ksh_stadat: 1 table, self-healing. fred_releases: zero — nothing is scheduled.",
   "fixable_by_us": true,
   "needs_from_ahmed": "",
   "recommended_fix": "Four separate fixes, in priority order. (1) DEDUPE THE STORED PARQUETS (ember + idb, and sweep the class): the first-pass ingesters wrote rows whose (series_key, obs_date) is not unique -- idb's rows_to_long emits ~99% duplicate keys on the social-indicators resources -- so every subsequent merge collapses the file and merge.py:323 refuses. Run a one-off store-side dedup rewrite of the affected parquets (idb: the 11 social-indicators files; ember: the 4 named datasets), and add a test that a fetcher's own key builder produces unique dedup keys, or the same files re-fail on the next first-pass. Note turkiye unlicensed_capacity_chart is a GENUINE upstream shrink 1058->498 and needs an explicit lower min_ratio or a deliberate overwrite, not dedup. (2) GIVE IDB A ROTATION BOOKMARK (load_rotation/rotate_after from _common, exactly as worldbank_esg got in R190) -- without it 1,875 of 1,918 resources are unreachable forever -- and fix the skip predicate so an examined-but-empty resource (sidecar advanced, no parquet written) does not re-enter `todo` every run. (3) WORLDBANK_ESG: stop treating the API's `[{\"message\":[{\"id\":\"120\",\"key\":\"Invalid value\"}]}]` envelope as an empty window (worldbank_esg.py:95) -- detect the message envelope and file it as a structural/recode signal; then enumerate from /v2/source/75/indicator instead of from the store (worldbank_esg.py:115) and map the 13 dead ids to their new ones (CC.EST->GOV_WGI_CC.EST etc., EN.ATM.*->EN.GHG.*.AR5) as an in-place repair preserving live series ids, per the imf_hpdd/imf_fiscaldecentralization precedent in config.py. The 8/9 itself needs no fix -- WB 502'd for 20 minutes and is fine now -- though capping get_json's ladder (6 tries x 120 s + ~61 s backoff burns the whole 20-min budget on ~9 indicators) and wiring the existing TransientStreak would stop a WB outage from costing a whole tick. (4) HOUSEKEEPING: delete the fred_releases row from state.db (and consider deleting the orphaned fetcher) so a de-registered source stops appearing in failure triage; re-run sec_edgar to prove commit 3d7f4e3e actually clears edgar_insider/2026q2 -- it has not been attempted since the fix landed. ksh_stadat needs nothing.",
   "evidence": [
    "data/_aqueduct/state.db unit_state (etag 515a5e5628e6273dd24acf9b2d2bfdbc == live r2://econ-data/_aqueduct/state.db.zst): ember _all partial 2026-07-30T21:46:31Z '4/48 sub-unit(s) transient-failed'; idb 2026-07-30T22:49:24Z '10/40'; ksh_stadat 2026-07-31T23:43:37Z '1/60'; fred_releases 2026-06-24T00:18:38Z '1/165'; sec_edgar 2026-08-03T11:00:46Z '1/8'; worldbank_esg 2026-08-03T09:25:06Z '8/9'",
    "EMBER REPRODUCED (download+parse+route all OK, merge refuses): DefinitiveError: refusing shrink 9325->3013 (methane_chart_satellite_emissions); 4813->471 (necp_Ember_NECP_data_2024); 195->65 (turkiye srmc_chart); 1058->498 (turkiye unlicensed_capacity_chart). CONTROL turkiye power_price_chart_eng merged OK 1848->1876. Guard at updater/merge.py:323-325; swallowed at ember.py:185 `except DefinitiveError: tally.transient_unit()`",
    "IDB REPRODUCED end-to-end on a scratch copy: resource 03f2bf5c, upstream total=165, rows_to_long -> 165 values, then `DefinitiveError: refusing shrink 165->11 (< 97% of existing)`. Swallowed at idb.py:172. Stored-parquet duplication measured for all 11 in-window files, e.g. 0073815a rows=134367 distinct(series_key,obs_date)=475 (ratio 0.0035); 04ada3f0 117764->547; 010bdff7 108448->490; 01f58a1c 80113->298",
    "IDB UPSTREAM IS HEALTHY: all 40 datastore_search?limit=0 calls returned HTTP 200 (totals 31..134367, fields 5..808) and all 40 page-1 pulls (limit=5000) returned 200 with real records; NON-200 count: 0. Manifest current_package_list_with_resources -> HTTP 200, 2,747,912 bytes, 1,918 datastore_active resources vs 32 sidecar entries and 555 store files",
    "WORLDBANK_ESG CI LOG (gh run 30796923747, 2026-08-03): repeated 'retry 6/6 after 30s (HTTP Error 502: Bad Gateway)' and 'retry N/6 after Ns (The read operation timed out)' from 09:06:31Z to 09:24:26Z, then '[worldbank_esg] budget of 20 min spent after 20.6 min; deferring 62 of 71 indicators to the next tick (resuming after EG.ELC.COAL.ZS.parquet)'. The 9 attempted are exactly AG.LND.AGRI.ZS..EG.ELC.COAL.ZS (r2://econ-data/clean_full/worldbank_esg/_rotation.json = {\"after\":\"EG.ELC.COAL.ZS.parquet\"}). SAME URLs today: AG.LND.FRLS.HA?source=75&date=2022:2027 -> HTTP 200 in 0.5s, total=1040, lastupdated 2026-06-24. The 62 deferrals were correctly filed as deferrals, not failures (R303 fix working)",
    "WORLDBANK_ESG SILENT RECODE: GET /v2/source/75/indicator?per_page=1000 -> HTTP 200, meta total=80; CC.EST/GE.EST/PV.EST/RL.EST/RQ.EST/VA.EST absent, GOV_WGI_CC.EST etc. present. GET /v2/country/all/indicator/CC.EST?source=75... -> HTTP 200 body `[{\"message\":[{\"id\":\"120\",\"key\":\"Invalid value\",\"value\":\"The provided parameter value is not valid\"}]}]`. worldbank_esg.py:95 `if not isinstance(j, list) or len(j) < 2: return []` turns that into tally.empty_unit() -- 'a quiet window is normal for annual data'. worldbank_esg.py:115 enumerates the STORE (blob.list_parquets), never the catalog, so new ids are invisible too",
    "KSH_STADAT CI LOG (gh run 30667078530, 2026-07-31T23:41:49Z and 23:43:19Z): \"ERR: HTTPSConnectionPool(host='www.ksh.hu', port=443): Max retries exceeded with url: /stadat_files/ene/en/ene0009.csv (Caused by ConnectTimeoutError(... connect timeout=60))\" and the same for ido0015.csv. LIVE CONTROL TODAY: ene0009.csv HTTP 200 in 3.0s (1,947 bytes), ido0015.csv HTTP 200 in 0.9s (12,740 bytes), toc.json HTTP 200 (1,198,282 bytes)",
    "SEC_EDGAR: ran the fetcher's own _published_keys/_keys_on_disk against the live SEC pages -> edgar_13f published=53 on_disk=53 MISSING=0; edgar_insider published=82 on_disk=81 MISSING=['2026q2']. Prior state row 2026-08-02T08:11:53Z: \"UNEXPECTED:ArrowInvalid('Casting from timestamp[us] to timestamp[ns] would result in out of bounds timestamp: -61950355200000000')\". Commit 3d7f4e3e (2026-08-03T07:03:28-05:00 = 12:03Z, on origin/main) 'ROOT CAUSE FOUND - dev runs pandas 2.3.3, CI runs pandas 3.0.5' + guard ea1cf229 (11:55Z) both POSTDATE the 11:00:46Z attempt",
    "FRED_RELEASES IS DE-REGISTERED, NOT BROKEN: `grep -n fred updater/registry.yaml` returns ZERO hits across its 141 `- source_id:` entries. updater/config.py:39-40 states verbatim: 'Also -2 same day: fred_releases and sdmx_nso were still being crawled while GATED, so a run would have re-uploaded to R2 exactly what the purge deleted.' (2026-07-23). Its fetcher updater/strategies/fetchers/fred_releases.py is orphaned code; last state write 2026-06-24, i.e. it has not been ATTEMPTED in 40 days (R246)"
   ],
   "verdict": null,
   "verdict_reason": null,
   "corrected_root_cause": null
  }
 }
}
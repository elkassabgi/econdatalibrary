# insee_melodi

*Generated by `tools/gen_runbook.py` on 2026-08-04 05:35 UTC — every fact below is read from the system. Re-run the tool to refresh; do not hand-edit.*

## 1. At a glance

| | |
|---|---|
| in registry | yes |
| live (scheduled) | True |
| served to users | yes |
| catalogued series | 139 |
| strategy | `sdmx_delta` |
| check cadence | monthly |
| runs where | cloud (GitHub Actions) |
| refresh cost | medium |
| store dir | `data/clean_full/insee_melodi/` |

## 2. Current state (from `data/_aqueduct/state.db`)

- **unit `_all`** — status `partial`
  - last SUCCESS: `NEVER`  ·  last attempt: `2026-07-31T23:23:21+00:00`
  - obs_count: `36430103`  ·  last_obs_date: `2025-12-31`  ·  stored vintage: `—`
  - last error:
    ```
    129/144 sub-unit(s) transient-failed; will retry [DS_BPE_EDUCATION, DS_BPE_EVOLUTION,
    DS_BPE_SPORT_CULTURE, DS_BTS_SAL_EQTP_SEX_AGE, DS_BTS_SAL_EQTP_SEX_PCS, DS_CNT_CB, +123 more]
    ```

> `obs_count` is **not comparable across runs** — most fetchers report rows merged this run, thirteen fall back to the whole store when nothing was written. To ask how big this source is, count the store. See ledger R326 and the comment in `updater/state.py`.

> A `partial` NEVER sets `last_success_utc` (R231). A source reading `last SUCCESS: NEVER` may be ingesting millions of rows perfectly well and failing one sub-unit. Check `obs_count` and the run history before concluding it is dead.

> **A FUTURE `last_obs_date` IS USUALLY CORRECT, NOT A BUG.** Many publishers ship genuine PROJECTIONS: CSO Ireland's `18_Population_Projections` runs to 2057, Estonia's `rahvastik` to 2085, ABS to 2046/2071, UN WPP to 2101, IMF WEO to 2031. `health.py` already separates the two — `frontier` is the furthest period held INCLUDING projections (display only), `newest_obs` filters to periods <= today and is the recency signal — because 28 of 93 units legitimately reported a future frontier. Do NOT "repair" a future date until you have looked at the series key and the year sequence.
>
> What IS a defect: a SENTINEL (9999-12-31, 2999-12-31) or a COUNTER read as a year (years 1, 2, 3 ... contiguous from 1 — no projection starts at year 1). Tell them apart by the key and the sequence, never by the size of the number. See R320/R322 and `tools/audit_impossible_dates.py`, whose bounds (before 1500, after 2200) are deliberately far outside every real projection horizon.

### Store vs state — do they agree?

- local parquet files: **84**
- store FRONTIER (furthest period held, projections included): `2026-12-31`
- store newest OBSERVED (not in the future): `2026-05-31`
> Read LOCALLY. Under `AQUEDUCT_BACKEND=r2` this directory is a scratch mirror of the last run, so for a cloud source these can UNDER-report. Confirm anything you act on against R2.

### Recent runs

| when (UTC) | status | obs | dur | note |
|---|---|---|---|---|
| 2026-07-31T23:23:21 | partial | 36430103 | 1580.6s | 129/144 sub-unit(s) transient-failed; will retry [DS_BPE_EDUCATION, DS_BPE_EVOLUTION, DS_BPE_SPORT_CULTURE, DS |

## 3. How it works

**Why this strategy** (registry `strategy_reason`):

> Melodi /data/{FLOW_CODE} supports startPeriod/lastNObservations and the catalog exposes a per-dataflow lastUpdate; gate on lastUpdate and pull startPeriod=last_obs+1 per flow rather than re-paginating all flows.

**Adapter contract** (registry `adapter`):

- `vintage_signal`: Catalog lastUpdate field per dataflow (dataflow/all catalog) compared against a stored per-flow marker; enumerate ~109 flow codes from that catalog.
- `since_param`: startPeriod=<max(obs_date)+1 period> on /data/{FLOW_CODE} (gated by catalog lastUpdate > stored marker).
- `out_paths_note`: Multi-file: one parquet per dataflow at data/clean_full/insee_melodi/{FLOW_CODE}.parquet (~79 files). Merge by read+concat+dedup on (flow,series_key,obs_date); some flows large (DS_BPE* ~25 MB). Add .tmp staging (no atomic write today).
- `rate_note`: No API key (keyless 'libre' plan, Accept: application/json). Documented 30 req/min cap -> RATE_DELAY 2.1s between pages/flows; HTTP 429 -> 60s sleep.

**Fetcher module**: [`updater/strategies/fetchers/insee_melodi.py`](../../updater/strategies/fetchers/insee_melodi.py)

<details><summary>Its own module docstring — the authoritative description, including every defect previously found and why the code looks the way it does</summary>

```
S3 (sdmx_delta) fetcher — INSEE Melodi, France. Keyless SDMX 2.1 catalog.

Layout (set by jobs/ingest_insee_melodi.py): ONE parquet per DATAFLOW under
clean_full/insee_melodi/<FLOW_CODE>.parquet with schema
  flow        : string  (constant within a file — the dataflow code)
  series_key  : string  ("DIM=val:DIM=val:..." over every dimension EXCEPT TIME_PERIOD,
                         sorted by dimension name — exactly the ingester's composite key)
  obs_date    : date32  (parsed from TIME_PERIOD: YYYY->Dec-31, YYYY-Qn->quarter-start,
                         YYYY-MM->month-start, YYYY-MM-DD->that day)
  value       : float64 (first non-null measure's .value)

The dedup identity is (flow, series_key, obs_date): a TIME_PERIOD plus the full
dimension tuple uniquely identifies one observation, so a re-fetched (revised) value
overwrites the prior vintage (merge keeps the last row per key) instead of duplicating.

INCREMENTAL (date-tail per flow):
  Each dataflow is a sub-unit. We read the existing parquet's max(obs_date), take its
  YEAR, and request only that year forward via the SDMX 2.1 native filter
      GET /data/{FLOW}?startPeriod=<year>&page=N
  Re-fetching the boundary YEAR (not year+1) is deliberate: it catches in-place
  revisions to the latest annual/quarterly/monthly/daily periods (merge dedups the
  overlap) — the same revision-capture stance as the treasury/bcb fetchers. For a
  daily flow this is at most one year of daily rows, still tiny next to the full
  history. `updatedAfter` is NOT requested: Melodi's DSDs reject it (HTTP 400
  "La DSD ne possède pas le composant updatedAfter"), so startPeriod is the only
  supported server-side window — verified live 2026-06.

ENUMERATION / PARSE reused verbatim from jobs/ingest_insee_melodi.py:
  - flow catalog: GET /dataflow/all  (list of {code,label})
  - obs parse: dimensions{TIME_PERIOD,...} -> series_key + obs_date; first measure -> value
  - parse_date: the same YYYY / YYYY-Qn / YYYY-MM / YYYY-MM-DD handling.
Pagination is corrected vs the ingester: Melodi pages signal end via paging.isLast
(and a SHORT page), NOT a "next" key, so we follow until isLast / a < PAGE_SIZE page.

HONEST-STATUS CONTRACT (Tally + finalize):
  Per flow we record on a Tally:
    added_unit(n)     rows merged for the flow (n>0 new, n==0 nothing newer than boundary)
    empty_unit()      flow legitimately had no observations in the window
    transient_unit()  flow hit a TransientError (timeout/5xx/429/network/bad-json) — record
                      & KEEP GOING so one flaky flow can't strand the other ~107
    structural_unit() flow returned a 200 with a real envelope but, on a FULL (origin)
                      fetch of a previously-populated file, 0 parseable rows -> schema break
  finalize() -> 'partial' on any transient (orchestrator does NOT stamp success; re-runs);
  DefinitiveError on a structural break or a large all-empty window; else ok/no_change.
  merge_and_write preserves existing data in every failure path (never shrink).

License: Licence Ouverte / Open Licence 2.0. Source: INSEE Melodi www.insee.fr
```

</details>

**Ingest script(s)**: `jobs/ingest_insee_melodi.py`

## 4. If it stops updating — diagnose in this order

Run everything from `E:\research\econfindatalibrary`.

```bash
# 1. What does the system actually say? (not what you remember)
python -c "import sys,sqlite3;sys.path.insert(0,'.');from updater import config;con=sqlite3.connect(f'file:{config.STATE_DB}?mode=ro',uri=True);print(*con.execute(\"SELECT unit_id,status,last_success_utc,last_attempt_utc,last_error FROM unit_state WHERE source_id='insee_melodi'\"),sep=chr(10))"

# 2. Is it even being ATTEMPTED? 'scheduled' is not 'attempted' (R246).
#    A daily run reaches ~20 of ~106 cloud sources; most are skipped on cadence, which is normal.
python -m updater.run --source insee_melodi --dry

# 3. Run it for real and READ THE REASON. Failures name themselves now.
AQUEDUCT_BACKEND=r2 python -u -m updater.run --source insee_melodi --force

# 4. Is the PUBLISHER healthy, or is it us? Probe upstream directly, never a relay.
#    (DBnomics is BANNED — every source must be reached at its own publisher.)

# 5. Is the store intact? obs_count in state is NOT the answer.
AQUEDUCT_BACKEND=r2 python -c "import sys,os;sys.path.insert(0,'.');from updater import config,blob;d=config.source_dir('insee_melodi');fs=[f for f in blob.list_parquets(d) if not os.path.basename(f).startswith('_')];print(len(fs),'files',sum(blob.row_count(os.path.join(d,os.path.basename(f))) for f in fs),'rows')"

# 6. Are its dates real? A counter-as-year or a 9999 sentinel is silent (7 sources hit).
#    NOTE: a FUTURE date is usually a legitimate projection - check the key before repairing.
python tools/audit_impossible_dates.py --r2 --source insee_melodi
```

**Read the failure class before fixing anything.** From the 2026-08-04 audit of every unfetchable source, the causes were: `budget_deferral` (NOT broken — ran out of its time slice), `code_bug`, `rate_limited`, `gated_by_design`. **Zero** were an expired credential or a dead endpoint, although that is the usual first guess.

If the error names sub-units as `deferred (budget N min)` or `budget spent`, **nothing has failed** — the source ran out of its slice and the rest is taken next tick (R303). Do not 'fix' it.

## 4b. Root cause when this was last investigated (2026-08-04)

*From the unfetchable-source audit — 29 agents, every finding challenged by a skeptic before it was accepted. This source was covered by the target `insee_melodi`.*

- **root cause**: `budget_deferral`  (confidence high)
- **which units**: ZERO of 144 failed. 15 sub-units attempted and succeeded (catalog positions 1-15, DD_CNA_AGREGATS through DS_BPE); 129 sub-units (positions 16-144) were never attempted -- the 25-minute wall-clock budget stopped the sweep before reaching them. The '129/144 transient-failed' string is a labelling artefact of the pre-e88178cb code, in which the `if dl.spent():` branch called tally.transient_unit(code) instead of tally.deferred_unit(code); with the current code the same run would read '15 sub-unit(s) attempted, none failed; 129 deferred by budget and taken next tick'.
- **blast radius**: Zero rows lost or corrupted: the deferral path does `total += before` and never touches the parquet, and merge_and_write never shrinks. 36,430,103 observations across 84 parquet files remain published. The real exposure is FRESHNESS, not failure: because the budget has no rotation bookmark, the same 15-flow prefix is re-walked every tick and catalog positions 16-144 are never refreshed. Measured on the local store -- positions 1-14 last written 2026-07-25, position 15 onward last written 2026-07-14 or earlier (positions 132-144 stuck at 2026-06-10), and 65 of the 144 catalog flows have never landed at all. Status side-effect: `partial` never stamps last_success_utc (which is NULL for insee_melodi), so the source also reads as 'never succeeded'.
- fixable by us: **True**

**Recommended fix, in order:**

> Two parts; part one is already done.
> 
> (1) DONE -- do not redo. Commit e88178cb (2026-08-03 10:26:02 -0500) changed insee_melodi.py:305 from `tally.transient_unit(code)` to `tally.deferred_unit(code)` inside the `if dl.spent():` branch. The stale 129/144 state row predates it (attempt 2026-07-31T23:23:21Z). No further code change is needed for the reported symptom; the row will correct itself on the next run. If you want the queue to read true before then, re-run insee_melodi or clear that unit_state.last_error.
> 
> (2) OPEN and worth fixing -- the budget is a truncation. insee_melodi has a Deadline (BUDGET_MIN=25, insee_melodi.py:68/290) but no rotation and no per-flow freshness sidecar, so it re-walks catalog positions 1-15 forever and positions 16-144 are never fetched. Add the standard bookmark from _common.py, which is already imported in this module's neighbourhood:
>   - `bookmark = load_rotation(out_dir)` before the loop,
>   - `flows = rotate_after(flows, bookmark, key=lambda f: f.get("code",""))`,
>   - `save_rotation(out_dir, code)` after each completed flow (save on a complete pass too, so the next run wraps through the same code path).
> This is the same fix R190 applied to abs/ecb and is exactly what the Deadline docstring (_common.py:92-100) requires. Alternatively/additionally raise BUDGET_MIN for this source under AQUEDUCT_BUDGET_MIN_OVERRIDE on the workstation -- INSEE's per-flow pages are slow (one flow took 23.0 s for page 1 alone, and the ingest log shows 27+ minute flows), so 25 minutes cannot cover 144 flows in any ordering; rotation is what makes the bound honest.
> 
> Do NOT investigate INSEE as an upstream problem. api.insee.fr/melodi is healthy, keyless, and answering HTTP 200 with full 10,000-observation pages in the exact envelope the parser expects.

<details><summary>Evidence produced during that investigation — HTTP codes, file:line, measured counts. Re-verify before acting: it was true on 2026-08-04.</summary>

- BRIEF PREMISE IS WRONG. Nothing failed. state.db (E:\research\econfindatalibrary\data\_aqueduct\state.db, unit_state) row: source_id=insee_melodi unit_id=_all status=partial last_attempt_utc=2026-07-31T23:23:21+00:00 obs_count=36430103 last_error='129/144 sub-unit(s) transient-failed; will retry [DS_BPE_EDUCATION, DS_BPE_EVOLUTION, DS_BPE_SPORT_CULTURE, DS_BTS_SAL_EQTP_SEX_AGE, DS_BTS_SAL_EQTP_SEX_PCS, DS_CNT_CB, +123 more]' -- that string was emitted by a budget-deferral branch, not a failure branch.
- CODE PROOF, file:line. E:\research\econfindatalibrary\updater\strategies\fetchers\insee_melodi.py:298-307 is `if dl.spent(): print('[insee_melodi] budget 25 min spent - {code} not pulled this run ...'); tally.deferred_unit(code); total += before; continue`. `git show e88178cb -- updater/strategies/fetchers/insee_melodi.py` shows that exact line was `- tally.transient_unit(code)` / `+ tally.deferred_unit(code)` -- committed 2026-08-03 10:26:02 -0500, i.e. AFTER the 2026-07-31 run that wrote the state row. The word 'deferred' lived only in the print above the call, so the label passed to the tally was a bare flow code and _common.finalize() rendered it as 'transient-failed'. BUDGET_MIN = 25 at insee_melodi.py:68.
- ARITHMETIC MATCHES EXACTLY, verified against the LIVE publisher catalog. GET https://api.insee.fr/melodi/dataflow/all -> HTTP 200, 0.71s, 30,706 bytes, Content-Type application/json, no Retry-After, no rate-limit headers; body starts b'[ {\n  "code" : "DD_CNA_AGREGATS", ...'; parsed to a list of 144 flows -- exactly the denominator in '129/144'. DS_BPE_EDUCATION is at position 16 of 144 in server order; DS_BPE_EVOLUTION 17, DS_BPE_SPORT_CULTURE 18, DS_BTS_SAL_EQTP_SEX_AGE 19, DS_BTS_SAL_EQTP_SEX_PCS 20, DS_CNT_CB 21. 144 - 15 = 129. The named 'failures' are a CONTIGUOUS TAIL starting one past the last flow attempted (positions 1-15 = DD_CNA_AGREGATS .. DS_BPE) -- the signature of a wall-clock deadline, not of scattered upstream failures.
- DURATION PROVES IT INDEPENDENTLY. runs table row: ts_utc=2026-07-31T23:23:21+00:00 source_id=insee_melodi status=partial obs=36430103 dur_s=1580.6. Budget is 25 min = 1500 s. The run spent its budget on 15 flows and then took ~80 s to walk the remaining 129 (deferral is just a row_count + print). A run in which 129 flows genuinely transient-failed CANNOT take 26 minutes: insee_melodi.py:77-78 sets MAX_ATTEMPTS=5, TIMEOUT=120 plus backoff, so 129 real failures is many hours. Control from the same publisher and the same state.db: insee_bdm's genuine all-transient event (2026-07-31T00:59:33Z, '201/201 sub-unit(s) transient-failed') took dur_s=6272.0 (104.5 min) for 201 flows. 1580.6 s for 144 is the deferral shape, not the failure shape.
- THE FOUR NAMED DATASETS ALL SERVE FINE RIGHT NOW (probed live, >=2.2 s apart, keyless, no auth): GET https://api.insee.fr/melodi/data/DS_BPE_EDUCATION?startPeriod=2023&page=1 -> HTTP 200, 3.16s, 5,408,182 bytes, top-level keys ['identifier','observations','paging','publisher','title'], paging={'first': ..., 'isLast': False, 'next': '...page=2'}, n_obs=10000, obs[0]={"attributes":{"UNIT_MULT":"0"},"dimensions":{...,"TIME_PERIOD":...},"measures":{...}}. DS_BPE_EVOLUTION -> HTTP 200, 5.48s, 3,581,237 bytes, n_obs=10000, obs[0] measures {'OBS_VALUE_NIVEAU': {'value': 1.0}}. DS_BPE_SPORT_CULTURE -> HTTP 200, 8.64s, 7,246,088 bytes, n_obs=10000. DS_BTS_SAL_EQTP_SEX_AGE -> HTTP 200, 23.00s, 3,633,599 bytes, n_obs=10000, obs[0] dimensions {'GEO':'2025-UU2020-35216','SEX':'_T','FREQ':'A','TIME_PERIOD':'2023','DERA_MEASURE':'SALAIRE_NET_EQTP_MENSUEL_MOYENNE','AGE':'Y_LT25'}, measures {'OBS_VALUE_NIVEAU': {'value': 1691.908732}}. Envelope shape is exactly what insee_melodi.py:105-125 `_obs_to_row` parses (dimensions.TIME_PERIOD -> obs_date, first measure .value) -- NO schema drift, NO recode, NO credential, NO throttle.
- CONTROL (so ABSENT is distinguishable from a broken probe): GET https://api.insee.fr/melodi/data/DS_THIS_FLOW_DOES_NOT_EXIST_XYZ?page=1 -> HTTP 400, 0.14s, 45 bytes, body b"DS_THIS_FLOW_DOES_NOT_EXIST_XYZ n'existe pas.". The probe does report a missing id as missing; the four brief-named ids are present and serving. (insee_melodi.py:249-250 maps 400/404 to an empty window, which is correct for this publisher's 'n'existe pas' response.)
- RESIDUAL REAL DEFECT (separate from the mislabelling, still open): the budget is a TRUNCATION, not a rotation. grep for load_rotation|save_rotation|rotate_after|rotation in updater/strategies/fetchers/insee_melodi.py returns ZERO hits, and the loop at insee_melodi.py:291 is a plain `for flow in flows:` over the publisher's order with no per-flow freshness skip (`_flow_max_obs` at :309 only sets startPeriod, it never `continue`s a fresh flow). _common.py Deadline docstring lines 92-100 states the rule verbatim: 'A budgeted fetcher MUST also either (a) skip already-fresh sub-units cheaply via a per-sub-unit sidecar ... or (b) rotate its starting point with load_rotation / save_rotation / rotate_after. A bound without one of those is a truncation, not a budget.'
- THE TRUNCATION IS VISIBLE IN THE STORE, not just inferred. Local store E:\research\econfindatalibrary\data\clean_full\insee_melodi holds 84 parquets against 144 catalog flows (65 catalog codes have NO parquet at all). Mtimes by catalog position: positions 1-14 all written 2026-07-25 09:05-09:07 UTC; position 15 (DS_BPE) onward carry 2026-07-14 or older (positions 132-144 are 2026-06-10). That is the contiguous stale tail the Deadline docstring predicts. Only 4 later files exist (DS_ICA p52, DS_PARC_LOGEMENTS_SERIES p61, DS_RP_SERIE_HISTORIQUE p84, DS_SIDE_CREA_DEP_REG_NAT p124, all 2026-07-29 16:05) and those bear a single identical timestamp, i.e. a targeted job, not the budgeted sweep.
- CATALOG ORDER IS STABLE, which is what makes the fixed-start bound bite: two GETs to /dataflow/all 2.2 s apart both returned HTTP 200 with n=144 and byte-identical code ordering (`new == old` -> True), first three ['DD_CNA_AGREGATS','DD_CNA_APU','DD_CNA_BRANCHES'], position 16 DS_BPE_EDUCATION both times. A 25-minute cap over that fixed order can never reach position 16+: jobs' own ingest log data/clean_full/insee_melodi_log.txt shows single flows taking 27+ minutes (DS_ETAT_CIVIL_DECES_COMMUNES 05:23:51 -> 05:51:26).
- ALREADY-KNOWN, ALREADY-LOGGED: D:\research\hfdatalibrary\.claude\MISTAKES.md R303 (line 7515) and its follow-up (line ~7665) name this exact instance -- 'insee_melodi writes tally.transient_unit(code) with the word only in the print above it' and 'insee_melodi 129/144 ... All four were deferrals'. R246 also satisfied: the run DID attempt (last_attempt_utc 2026-07-31T23:23:21Z, dur_s 1580.6), so this is not a never-ran source.

</details>

## 5. Everything that has already gone wrong here

2 ledger entries mention this source. Read them in `D:\research\hfdatalibrary\.claude\MISTAKES.md` before changing anything — several describe a fix that was tried and was WRONG.

- **R285** — R273's prediction held, and its count was wrong: I searched for one filename, not for the mechanism
- **R306** — I derived a class from the label text and shipped half of it as "the fix"

## 6. Rules that apply to every repair here

- **A key/parser change is a RE-PULL, never a merge** (R22). Changing what `series_key` means leaves the old and new grains coexisting; dedup cannot collapse them because they no longer collide. Use `mode="replace"`, or re-pull the table.
- **Never patch dates — re-pull** (R288). A naive date repair on `cso` would have destroyed 11 of 12 rows; only a negative control caught it. And 41,091 of `stat_slovenia`'s fabricated dates land in 1900–2200, indistinguishable from real data, so no date test can find them.
- **Verify against the store you are actually serving** (R296/R36). Under `AQUEDUCT_BACKEND=r2` the local directory is a scratch mirror of the last run only; a local check can under-report or give the opposite answer.
- **When a probe reports ABSENCE, run it against something known PRESENT** (R316). An absence looks identical whether the data is missing or the probe is broken.
- **Compare (key, date) pairs, not just keys** (R314). A re-key check that compares identifiers verifies the half you changed on purpose; the half you changed by accident is in the values.
- **A bound over a fixed order is a truncation, not a budget** (R190). If work is capped, check it ROTATES — and specifically that it rotates when the item at the head never completes (R313).
- **Read the job's ARGV, not its progress** (R323). A `--dry-run` and a real run print the same numbers.


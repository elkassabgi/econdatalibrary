# eurostat

*Generated by `tools/gen_runbook.py` on 2026-08-05 16:43 UTC — every fact below is read from the system. Re-run the tool to refresh; do not hand-edit.*

## 1. At a glance

| | |
|---|---|
| in registry | yes |
| live (scheduled) | True |
| served to users | yes |
| catalogued series | 7,637 |
| strategy | `giant_changed_units` |
| check cadence | monthly |
| runs where | cloud (GitHub Actions) |
| refresh cost | giant |
| store dir | `data/clean_full/eurostat/` |

## 2. Current state (from `data/_aqueduct/state.db`)

- **unit `_all`** — status `partial`
  - last SUCCESS: `NEVER`  ·  last attempt: `2026-07-30T21:46:41+00:00`
  - obs_count: `—`  ·  last_obs_date: `—`  ·  stored vintage: `—`
  - last error:
    ```
    eurostat: existing data still uses the UNSTABLE 'LAST UPDATE=' series_key — run the one-time re-
    key migration before incremental updates (else keys split and the file duplicates). Existing
    data left untouched.
    ```

> `obs_count` is **not comparable across runs** — most fetchers report rows merged this run, thirteen fall back to the whole store when nothing was written. To ask how big this source is, count the store. See ledger R326 and the comment in `updater/state.py`.

> A `partial` NEVER sets `last_success_utc` (R231). A source reading `last SUCCESS: NEVER` may be ingesting millions of rows perfectly well and failing one sub-unit. Check `obs_count` and the run history before concluding it is dead.

> **A FUTURE `last_obs_date` IS USUALLY CORRECT, NOT A BUG.** Many publishers ship genuine PROJECTIONS: CSO Ireland's `18_Population_Projections` runs to 2057, Estonia's `rahvastik` to 2085, ABS to 2046/2071, UN WPP to 2101, IMF WEO to 2031. `health.py` already separates the two — `frontier` is the furthest period held INCLUDING projections (display only), `newest_obs` filters to periods <= today and is the recency signal — because 28 of 93 units legitimately reported a future frontier. Do NOT "repair" a future date until you have looked at the series key and the year sequence.
>
> What IS a defect: a SENTINEL (9999-12-31, 2999-12-31) or a COUNTER read as a year (years 1, 2, 3 ... contiguous from 1 — no projection starts at year 1). Tell them apart by the key and the sequence, never by the size of the number. See R320/R322 and `tools/audit_impossible_dates.py`, whose bounds (before 1500, after 2200) are deliberately far outside every real projection horizon.

### Recent runs

| when (UTC) | status | obs | dur | note |
|---|---|---|---|---|
| 2026-07-30T21:46:41 | partial | 0 | 6.8s | eurostat: existing data still uses the UNSTABLE 'LAST UPDATE=' series_key — run the one-time re-key migration  |

## 3. How it works

**Why this strategy** (registry `strategy_reason`):

> One of the 4 named giants; the TOC catalogue carries a per-dataset lastUpdate change-feed, so refresh only whole dataset codes whose lastUpdate moved instead of the blind skip-if-exists crawl.

**Adapter contract** (registry `adapter`):

- `vintage_signal`: Eurostat TOC catalogue (catalogue/toc/txt) per-dataset lastUpdate/lastModified column; persist code->lastUpdate state map and re-fetch only codes whose upstream lastUpdate is newer (plus brand-new codes).
- `since_param`: n/a (no obs-level since; unit-level change-feed via TOC lastUpdate, not startPeriod — this profile is the bulk SDMX-TSV dissemination path, format=TSV&compressed=true, not the per-obs SDMX query API)
- `out_paths_note`: WRITES TO TWO DIRS/STAGES: raw data/raw/eurostat/<code>.tsv.gz (~7,640 files, fetch_eurostat_bulk.py) AND clean_full/eurostat/<CODE>.parquet (~7,753 files, separate ingest_eurostat.py). A true delta must purge/refetch only changed raw codes then reparse only those in ingest (today ingest reparses ALL; its advertised --delta flag is a no-op).
- `rate_note`: No key. Polite UA, 6 concurrent workers, 4 retries linear backoff on 429/5xx, 300s timeout, atomic .part->replace. Constraint is sheer volume (~7,600 datasets, multi-GB, ~6.1B obs); legacy files/ bulk path is 410 Gone.

**Fetcher module**: [`updater/strategies/fetchers/eurostat.py`](../../updater/strategies/fetchers/eurostat.py)

<details><summary>Its own module docstring — the authoritative description, including every defect previously found and why the code looks the way it does</summary>

```
S4 fetcher — Eurostat (one of the four named giants, ~7,600 datasets, ~6.1B obs).

Change-feed: the dissemination catalogue TOC
  https://ec.europa.eu/eurostat/api/dissemination/catalogue/toc/txt
is a tab-separated, quoted table whose columns are
  title | code | type | last update of data | last table structure change | data start | data end | values
Rows of type `dataset`/`table` are real datasets; `folder` rows are not. The
"last update of data" cell (DD.MM.YYYY) is the per-dataset vintage: re-fetch only
codes whose lastUpdate moved (plus new codes, plus any flow whose last run was
partial/failed/empty/absent).

Per changed flow: incremental SDMX-CSV pull
  .../sdmx/2.1/data/<code>/?format=SDMX-CSV&startPeriod=<last_obs+tail>
merged into clean_full/eurostat/<CODE>.parquet (series_key, obs_date, value).

STABLE series_key — the load-bearing fix:
  The SDMX-CSV columns are DATAFLOW, `LAST UPDATE`, <dims...>, TIME_PERIOD,
  OBS_VALUE, OBS_FLAG, CONF_STATUS. The legacy ingest joined ALL non-time/value
  columns as `k=v`, so `LAST UPDATE` (a SPACE, which slipped past the LAST_UPDATE
  underscore skip-set) AND OBS_FLAG/CONF_STATUS leaked into the key:
      LAST UPDATE=13/05/26 11:00:00:freq=A:...:OBS_FLAG=e
  Because `LAST UPDATE` changes EVERY release, every series got a brand-new key on
  every publish -> the whole file duplicated. Here the key is built from the
  DIMENSION columns only (everything strictly between `LAST UPDATE` and
  TIME_PERIOD), so it is STABLE across releases. lastUpdate is carried in the
  change-feed sidecar, not the key. See the RE-KEY data-op note in
  giant_changed_units.py for the existing-data migration (designed, NOT executed).
```

</details>

**Ingest script(s)**: `jobs/fetch_eurostat_bulk.py`

## 4. If it stops updating — diagnose in this order

Run everything from `E:\research\econfindatalibrary`.

```bash
# 1. What does the system actually say? (not what you remember)
python -c "import sys,sqlite3;sys.path.insert(0,'.');from updater import config;con=sqlite3.connect(f'file:{config.STATE_DB}?mode=ro',uri=True);print(*con.execute(\"SELECT unit_id,status,last_success_utc,last_attempt_utc,last_error FROM unit_state WHERE source_id='eurostat'\"),sep=chr(10))"

# 2. Is it even being ATTEMPTED? 'scheduled' is not 'attempted' (R246).
#    A daily run reaches ~20 of ~106 cloud sources; most are skipped on cadence, which is normal.
python -m updater.run --source eurostat --dry

# 3. Run it for real and READ THE REASON. Failures name themselves now.
AQUEDUCT_BACKEND=r2 python -u -m updater.run --source eurostat --force

# 4. Is the PUBLISHER healthy, or is it us? Probe upstream directly, never a relay.
#    (DBnomics is BANNED — every source must be reached at its own publisher.)

# 5. Is the store intact? obs_count in state is NOT the answer.
AQUEDUCT_BACKEND=r2 python -c "import sys,os;sys.path.insert(0,'.');from updater import config,blob;d=config.source_dir('eurostat');fs=[f for f in blob.list_parquets(d) if not os.path.basename(f).startswith('_')];print(len(fs),'files',sum(blob.row_count(os.path.join(d,os.path.basename(f))) for f in fs),'rows')"

# 6. Are its dates real? A counter-as-year or a 9999 sentinel is silent (7 sources hit).
#    NOTE: a FUTURE date is usually a legitimate projection - check the key before repairing.
python tools/audit_impossible_dates.py --r2 --source eurostat
```

**Read the failure class before fixing anything.** From the 2026-08-04 audit of every unfetchable source, the causes were: `budget_deferral` (NOT broken — ran out of its time slice), `code_bug`, `rate_limited`, `gated_by_design`. **Zero** were an expired credential or a dead endpoint, although that is the usual first guess.

If the error names sub-units as `deferred (budget N min)` or `budget spent`, **nothing has failed** — the source ran out of its slice and the rest is taken next tick (R303). Do not 'fix' it.

## 4b. Root cause when this was last investigated (2026-08-04)

*From the unfetchable-source audit — 29 agents, every finding challenged by a skeptic before it was accepted. This source was covered by the target `eurostat`.*

- **root cause**: `gated_by_design`  (confidence high)
- **which units**: 0 flows attempted of 7,562 catalogued dataflows. This is not a per-flow failure profile at all: _require_rekeyed() raises DefinitiveError inside update() BEFORE _giant.run_giant selects a single flow (run id 345 lasted 6.8s and recorded obs=0). Store side, the dry run has so far found 4,999 of the first 5,000 files still carrying the unstable key and exactly 1 already clean.
- **blast radius**: The entire source is frozen and has never once updated: last_success_utc is NULL and the runs table contains a single eurostat attempt. 7,754 store parquet files / ~1.48B rows scanned so far still hold the unstable 'LAST UPDATE=' key scheme. Key inflation measured on one file: NAMA_10_GDP carries 53,747 distinct keys where 36,493 are real — 17,254 phantom series in that dataset alone, and it is one of 7,754. Nothing is corrupt or lost; the data is stale and over-keyed, and the freshest upstream vintages in the TOC are 08.07.2026 / 18.07.2026 / 30.07.2026, so real published revisions are being missed daily.
- fixable by us: **True**
- **needs Ahmed**: Go-ahead to run tools/rekey_eurostat.py --apply, which overwrites the production eurostat store (backend=r2, 7,754 files) and resolves same-(key,obs_date) conflicts by keeping the LAST occurrence. This is only a real decision if the dry run's final 'conflicting revisions' count is non-zero; if it prints 0, every collapse was an exact duplicate, the migration changes no served value, and no sign-off is needed.

**Recommended fix, in order:**

> 1) The gate is CORRECT and is NOT masking a fetch-side problem — do not touch the fetcher's endpoints, keys or parser; they are verified working against the live publisher today. 2) Let the --dry-run finish and read its final 'conflicting revisions' line, then re-run the SAME tool with --apply (the currently running invocation writes nothing — that is the actual reason the gate has not lifted). 3) BEFORE the --apply, fix eurostat.py:209: `blob.list_parquets(out_dir)[:5]` must not be a prefix sample of a sorted list that the migration walks in the same order. Either scan every file, or sample randomly, or — cheapest and strongest — have the migration write a completion marker (e.g. _rekeyed.json holding the file count it converted) and have _require_rekeyed() demand that marker match the current store count. Otherwise a re-key that dies partway (as one already did at file 4,403/7,754) silently disarms the guard at 0.06% done and the next daily tick duplicates ~3,300 files under two key schemes. 4) Only after a clean --apply, let the normal daily tick run; expect several 'partial' ticks by design (DEFAULT_MAX_FLOWS_PER_TICK = 400 in _giant.py caps the sweep) — those are not failures.

<details><summary>Evidence produced during that investigation — HTTP codes, file:line, measured counts. Re-verify before acting: it was true on 2026-08-04.</summary>

- LIVE TOC probe (publisher's own host, no relay): GET https://ec.europa.eu/eurostat/api/dissemination/catalogue/toc/txt -> HTTP 200, Content-Type 'text/plain;charset=UTF-8', 1,971,935 bytes, no Retry-After. Fed to the fetcher's OWN eurostat._parse_toc: 7,562 datasets parsed, e.g. ('ei_bpm6ca_q', {'vintage': '08.07.2026', 'filename': 'EI_BPM6CA_Q.parquet'}).
- LIVE data probe: GET https://ec.europa.eu/eurostat/api/dissemination/sdmx/2.1/data/NAMA_10_GDP/?format=SDMX-CSV&startPeriod=2024 -> HTTP 200, Content-Type 'application/vnd.sdmx.data+csv;version=1.0.0', 5,368,348 bytes. Body first line: 'DATAFLOW,LAST UPDATE,freq,unit,na_item,geo,TIME_PERIOD,OBS_VALUE,OBS_FLAG,CONF_STATUS' then 'ESTAT:NAMA_10_GDP(1.0),30/07/26 11:00:00,A,CLV05_MEUR,B1G,AL,2024,10873.1,p,' — exactly the shape eurostat._parse_csv expects. No schema drift.
- Same body through eurostat._parse_csv: 68,524 rows, 35,502 distinct series_key, sample key 'freq=A:unit=CLV05_MEUR:na_item=B1G:geo=AL', dates [2024-12-31, 2024-12-31, 2025-12-31], values [10873.1, 282992.9, 284890.0]; 'LAST UPDATE' present in new keys = False. CONCLUSION: if the gate were lifted, eurostat WOULD fetch and parse.
- Gate premise VERIFIED TRUE against the store: pyarrow read of E:\research\econfindatalibrary\data\clean_full\eurostat\NAMA_10_GDP.parquet — 1,112,628 rows, first five series_key values are literally 'LAST UPDATE=09/06/26 11:00:00:freq=A:unit=CLV05_MEUR:na_item=B1G:geo=AL'. The unstable key really is still in the data.
- Migration correctness round-trip: tools/rekey_eurostat.stable_key applied to that file collapses 53,747 distinct keys -> 36,493, and 35,498 of the 35,502 keys the LIVE fetch emits are already present in the migrated set. The 4 misses are genuinely new upstream series (e.g. 'freq=A:unit=CP_MPPS_EU27_2020:na_item=D21:geo=EA21'), not a keying mismatch. The gate is satisfiable and the two code paths agree.
- State DB E:\research\econfindatalibrary\data\_aqueduct\state.db, unit_state: source_id='eurostat', strategy='giant_changed_units', status='partial', last_success_utc=None, last_attempt_utc='2026-07-30T21:46:41+00:00', last_error="eurostat: existing data still uses the UNSTABLE 'LAST UPDATE=' series_key - run the one-time re-key migration...". runs table holds exactly ONE eurostat row: id=345, dur_s=6.8, obs=0 — it aborts before a single flow is fetched.
- CONTROL (is the probe/scheduler broken, or just eurostat?): same state.db, 77 of 141 units have last_attempt_utc >= 2026-07-30; newest is hagstofa at 2026-08-04T02:11:56Z (partial, 2/1074 sub-units structural) and ofr at 2026-08-04T00:48:33Z status='ok', obs=433,301, '+261 new rows'. The updater is alive and other sources are succeeding — eurostat's stall is the gate, nothing else.
- CORRECTION TO THE BRIEF: the re-key running locally is a MEASUREMENT PASS, not the migration. Win32_Process PID 26252, CreationDate 8/3/2026 5:40:26 PM, CommandLine: python.exe -u tools/rekey_eurostat.py --dry-run. tools/rekey_eurostat.py:192 `if a.dry_run: print('--dry-run: nothing written.')` and blob.write_table_atomic is reached only under `if a.apply` (line 171). Log logs/rekey_eurostat_20260803T224026Z.log tail: '[5000/7754] touched=4,999 clean=1 rows=1,483,519,001 -> 1,481,208,948'. When it finishes, ZERO files will have been re-keyed and the gate will still fire.
- SECOND, INDEPENDENT DEFECT (latent, will bite on the real --apply): eurostat.py:209 `for fn in blob.list_parquets(out_dir)[:5]:` — the guard inspects only the FIRST FIVE of 7,754 files. blob.py:203-246 returns `sorted(...)` in both the R2 and local branches, and rekey_eurostat.py:81 walks that identical sorted list. So a partial --apply run re-keys those same 5 files first and the guard releases at 0.06% completion. The tool's own comment (rekey_eurostat.py:110-118) records a prior pass dying at file 4,403 of 7,754 after ~4 hours on a transient R2 read — i.e. the interrupt case is observed, not hypothetical. A resumed daily run would then merge stable-key fetches into ~3,300 still-unstable files: the exact mixed-scheme duplication the guard exists to prevent, and one never-shrink cannot catch.
- Store vs catalogue size: 7,754 .parquet files in data/clean_full/eurostat (7,755 dir entries incl. the _giant_state sidecar) against 7,562 dataflows in today's TOC. Not investigated further; noted, not diagnosed.

</details>

## 5. Everything that has already gone wrong here

11 ledger entries mention this source. Read them in `D:\research\hfdatalibrary\.claude\MISTAKES.md` before changing anything — several describe a fix that was tried and was WRONG.

- **R229** — I re-derived an analysis that existed, and my conclusion was the one it had rejected
- **R244** — the audit scored every module against a store of the same NAME, so the shared helper was unflaggable
- **R255** — I broke R131 while the rule was sitting in the digest I read at the start of this cycle
- **R258** — a colon in prose broke the load for all 141 sources, and only a pre-commit parse caught it
- **R295** — the second docstring in one day promising a safety property the code did not implement
- **R296** — I re-ran a migration's safety measurement against the wrong store, and the tool is what caught me
- **R307** — the watchdog covered the jobs I inherited and none of the jobs I started
- **R320** — the impossible-date class was never confined to PxWeb, and nobody had asked all 141 stores
- **R323** — I watched a --dry-run for six hours and told the user it was fixing eurostat
- **R324** — two I caught before shipping, recorded anyway because "caught in time" is luck talking
- **R329** — I "corrected" a correct number twice, using three broken probes that all failed toward reassurance

## 6. Rules that apply to every repair here

- **A key/parser change is a RE-PULL, never a merge** (R22). Changing what `series_key` means leaves the old and new grains coexisting; dedup cannot collapse them because they no longer collide. Use `mode="replace"`, or re-pull the table.
- **Never patch dates — re-pull** (R288). A naive date repair on `cso` would have destroyed 11 of 12 rows; only a negative control caught it. And 41,091 of `stat_slovenia`'s fabricated dates land in 1900–2200, indistinguishable from real data, so no date test can find them.
- **Verify against the store you are actually serving** (R296/R36). Under `AQUEDUCT_BACKEND=r2` the local directory is a scratch mirror of the last run only; a local check can under-report or give the opposite answer.
- **When a probe reports ABSENCE, run it against something known PRESENT** (R316). An absence looks identical whether the data is missing or the probe is broken.
- **Compare (key, date) pairs, not just keys** (R314). A re-key check that compares identifiers verifies the half you changed on purpose; the half you changed by accident is in the values.
- **A bound over a fixed order is a truncation, not a budget** (R190). If work is capped, check it ROTATES — and specifically that it rotates when the item at the head never completes (R313).
- **Read the job's ARGV, not its progress** (R323). A `--dry-run` and a real run print the same numbers.


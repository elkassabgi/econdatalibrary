# Per-source landmines — mined from all 150 ledger entries, 2026-08-04

> BEFORE touching a source, grep THIS file and the ledger for its id. A landmine
> listed here has already cost at least one broken run.

### From R1-R207

| source_id | warning | (Rn) |
|---|---|---|
| vdem | OOM-unsafe; never include in an unbounded local sweep; `numpy` was an undeclared direct import | R39, R178 |
| bls | never run `ingest_bls_full.py` (created 1.6M dup rows that trip never-shrink); browser headers 403 while the custom `ig.UA` works; download.bls.gov has a persistent Akamai edge block from this IP — fallback is api.bls.gov; store keys on `series_id` (+`period` in dedup), so cursor helpers need `key_col`; holds 328M obs (string obs_date files break naive scans) | R18, R173, R206-helper, R99 |
| ons_uk | mandatory bot UA format (no emails), 15 req/10s CSV tier, `RATE=0.7` already in ingester, 1-hour blocks; `series_key` embeds `calendar-years` → 25.4M one-point series, re-grain + clean re-pull required; OOM was the Python cursor dict (5.3M distinct keys), not Arrow; `group_by` 2GiB kill site; prune stale cursors only AFTER a run on the new grain | R46, R40b, R132, R127, R48, R207-arrow, R263 |
| boe | ~613 serial IADB requests → ThreadPool ~5-6, own Session per thread; was live for weeks with 21 of 30,674 series in the catalog | R40, R60 |
| scb | raw `pq.read_table` at scb.py:370 no-ops in CI; `Tid` codes (municipality 0114..2584) parsed as years — fabrication survives range tests; fix in the ingester ≠ fix in the fetcher (two parsers) | R36, R22, R331/R333-digest |
| statfin / hagstofa | statfin's structural gate is intentionally conservative — don't "unify" it; hagstofa values contain colons (`K: 65`) breaking `=`-segment splits; time-only tables hit `extract_regex` empty-string (not null) capture; sentinels 3001–3004 sit ON the time axis; MAN01201's 74,880-row detail exceeds MAX_CELLS and is NOT reproducible — never delete+repull it | R25, R52, R30, R269-digest, R26 |
| stat_estonia | killed at the 45-min cap before ever writing its rotation bookmark (budget checked between subjects); its `csv_derive failed 1415/3437` is the r2 scratch-mirror artefact, not staleness | R286, R151 |
| cbs_nl | period formats `SJ`/`X0`/`YYYYMMDD`; time dims named by content (`JaarVanImmigratie`); `$skip` cost is O(offset) — partition on `Perioden`; ~4-min emit cadence, don't kill on short samples; registered-but-fetcherless = STRANDED, not scheduled | R55, R58, R54, R262 |
| istat | `sdmx.istat.it` 302s to a TLS-dead host — dead-host state (`_DEAD_HOSTS`) required or it re-dials for every year-slice | R62, R63 |
| un_wpp | 27,756,924 mid-year (07-01) observations — a period-end re-stamp shifts every value half a year with zero collisions; country names read at ingest_un_wpp.py:100 and discarded at :128 (titles); attribution once shipped a literal `{year}` template | R97, R145, R144 |
| wid | resume marker mandatory (loop restarted at country 1 forever); derive against a consolidated sorted parquet (11.6×); the resolver entry was once a comment, not code | R130, R88, R125 |
| fao_qa / fao_qp | served by QCL (merged) — `restrict_to_published` or the rebuild mints ~75,786 duplicate series; FAO licence is `verified-nc`, NOT cc-by-4.0 — catalog_complete inherits the local (wrong) row | R91, R117 |
| fao_gt family | element re-code crosswalk (7231→723113 etc.) takes 27%→79%; UNION across 10 datasets is worth exactly zero — don't refactor `_faostat.py` for it; residual ~20% is a restructure with no 1:1 successor | R180 |
| faostat | per-domain `structural_unit()` landmine (one odd domain vetoes the source); omits series_cursors | R44, R41 |
| yale_epi | pinned `epi2024results.csv` missed the 2026 edition at a new URL; 2026 ships `iso` only while published ids use numeric `code` — naive merge forks the id space; its licence row was fully inverted | R78, R79, R86 |
| frankfurter | store key `EURUSD` vs catalog `frankfurter:EUR:USD` — punctuation gap silently stops CSV regeneration | R79(3) |
| imf_commodity / imf_* | `provider_code` is our id uppercased, NOT `PROVIDER/DATASET`; IMF renames flows (PSBSFAD→PSBS, PCTOT→CTOT, GENDER_*→GS_*, HPDD→HPD) — search by name before declaring retirement; PCPS serves well-formed responses carrying 4.6% of data — volume-gate | R74, R75, R83 |
| imf_hpdd / imf_fiscaldecentralization | upstream genuinely ends 2015/2020 — needs `upstream_verified` declaration; `gen_registry.py` silently DROPS `upstream_verified` on regeneration | R84, R52-B hazard |
| imf_fsi / imf_gfs* | legacy host data.imf.org/SDMX/BI returns 403 — no fetcher can fix it; successor trio FSIC/FSIBSIS/FSICDM are NEW ids (originals stay frozen); `imf_gfse` = G26 subset and `imf_gfsmab` = G11/G12 subset of the ONE flow `GFS_SOO`; retire only via the ksh pattern at INDICATOR grain | H/J notes, R141 |
| comtrade | ingest dropped mot/customs/partner2 dims — conflicting values under one (key,date); responses cap at exactly 100,000 rows silently; pass aggregate dims as query params; `_get()` must return None, never `[]` | R177, R201 |
| census | tails can mint new series (merge only stored keys, report skipped); 11 of 24 intltrade flows under-keyed (need extra dims in dedup); bds tail returns 0 for the stored columns; the header claim "non-EITS files don't gain periods" was false — probe one excluded file per source | R278, R280, R283, R284 |
| treasury / ofr / worldbank_esg | non-default dedup keys (per-file identity tuple; `(series_id, obs_date)`; `(country, obs_date)`) — auditors must read DEDUP from the code or produce 166 false positives and skip 71 files | R281 |
| bea | NOT a DBnomics source despite the provider-name collision (`bea:A191RC:Q` vs `BEA/GDPbyIndustry-1:...`); fetcher writes only bea.parquet — the 91 under-keyed files are the ingester's served tree; 913,230-series store serves 240 (gated on D1 capacity, a DECISION not a bug) | R171, R282, R268-digest |
| abs | 376,332,763 distinct series — any run-global per-series fold is a ~94 GB OOM; alphabetically FIRST, so it destroyed the runner before anything else ran; future dates (2046/2061) are real projections | R187–R190, R204 |
| bis | LBS has 608,570 series / a >2GiB series_key column — `group_by` kills the process; Apache ETag mtime-half flaps across replicas — gate Content-Length + 30-day backstop; ingest helpers are backfill-shaped (skip-if-present), not wrappable | R197, R195, R165, K-note |
| snb | `/dimensions/en` item ids are a DIFFERENT id space from the CSV codes (25% "reproduction" was an artefact); date conventions are mixed IN the store: annual period-END, monthly/quarterly period-START — derive conventions from the store per frequency by set equality | R179 |
| fed_board | `Output.aspx` has NO stable validator (per-request Last-Modified, no ETag/CL/Range) — content-hash the zip; `lxml` module-level import must be in requirements; probe URL must match `download_zip`'s actual params (no `label=include`) | R164, R178, R162 |
| whr | OWID grapher CSV Last-Modified is CDN cache-fill time — hash the body; two-probe stability audits pass it | R184 |
| defillama | parent protocols (aave, uniswap…) are absent from `/protocols` listing but live at `/protocol/<slug>` — never delist from the listing; its moving ETag is genuine content change, not a defect | R61, R165 |
| ksh / ksh_stadat | key grains differ (compare at TABLE grain — 96.4% redundant, not 13%); 16 retired tables are NOT re-crawlable (the exception to "deletion is recoverable"); ksh_stadat's parser rejects one-column snapshot tables (`Denomination;2024`) as SKIPs nothing counts | R141, R149, I-note, R156 |
| ipea | `$filter=VALDATA` is accepted and silently IGNORED (full series returned); ingest skips any series already on disk | R160 |
| stats_nz | discovery was a hardcoded period list frozen at December-2024 — derive periods from today's date; 10 of 12 datasets resolve for neither ingest nor fetcher (upstream reorganisation) | R159 |
| insee_melodi / insee_bdm | Etalab requires the last-update DATE: melodi has `/melodi/catalog/{FLOW}.modified`, bdm publishes `LAST_UPDATE` per IDBANK; melodi fragmentation is per-flow, not source-wide | R136, R127 |
| cepii_gravity | derive once stopped at 13% (991,707 missing) while the first-alphabetical sample looked complete; source row was an attribution stub (Etalab breach); worker key-encoding mismatch on `! ' ( ) *` | R167, R194, R192 |
| eia | `write_dataset` returns `n_obs` (not `rows`) — a guessed key reported `no_change` on a 119,105-row publish | R186 |
| shiller | has fetcher + ingester but NO registry entry — cannot run; its `unit_state` row is a stale ghost | R59 |
| sec_edgar | ONE id, TWO products (served XBRL set vs unserved 13F/insider giant) — health gate and coverage audit both wrong, opposite directions; data lives under `clean_grouped/`, not `clean_full/`; 2,050 sibling tickers (GOOG vs GOOGL) resolve to nothing | R275-digest, R57, R106 |
| owid | licence DISPUTED — store holds 1,048,968 series vs 64 catalog rows, and `make_servable` would publish all of them; gated per-source in LEGACY_KEEP (licence row shared with 36 sources) | R150 |
| bcrp / ofr | later periods are `n.d.` placeholders / weekend business-day arithmetic — both false-stale alarms; verify against the publisher before believing the gate | R51 |
| riksbank / boc / tcmb | `_max_by_key` returns ISO strings: boc/tcmb crashed on `.isoformat()`, riksbank's isinstance filter returned an empty map silently — fix per-CALLER, never uniformly; riksbank's "(over derive-all cap)" note is a hardcoded lie (117 rows vs 5,000 cap) | R270, R152 |
| adb | 54 per-flow parquets — `sync_parquet`'s `<src>/<src>.parquet` assumption dies on it; rotation genuinely stuck (44 of 54 flows frozen — behavioural write-time test, not filename test) | R126, R285 |
| ilostat / zillow | cursor folds explode (30.8M distinct series; ~543k across 206 cubes) — accumulated `CURSOR_CAP` required | R175, R176 |
| gapminder / maddison / ggdc | pre-1677 dates overflow pandas datetime64[ns] — keep `date_as_object`; NEVER `errors="coerce"` (silently blanks the oldest data); maddison pins `mpd2020.xlsx` + a Dataverse id, so new releases are invisible | R68, R69, K-note |
| worldbank / worldbank_wdi | `SERIES_CARVEOUTS` keyed on `worldbank` alone leaked ILO/IMF data through `worldbank_wdi` — carve-outs must cover sibling ids | R32 |
| cso | sentinels 3001/9998/9999; was the ONE ingester not routed through the shared PxWeb resolver (first-match-wins let a classification axis become years); its `_catalog.json`/cursor were local-only and ephemeral under r2; a change-driven fetcher can't rebuild a deleted file (no repull_file.py); 92 tables span multiple parquets (PUT-overwrite trap); audited in the verbatim file's summary TABLE, not a `###` section | R265, R266, R272, R133, R137 |
| sipri / sipri_polity, edgar_jrc, damodaran | prefix-match guards trip on `sipri_polity`; missing `openpyxl`/`xlrd` deps surfaced as "no adapter built"/"parsed 0 rows", never naming the dep | R33, R178 |

### From R208-R276

| source_id | warning | (Rn) |
|---|---|---|
| (all `*_dbnomics` relays) | BANNED upstream — never fetch or probe api.db.nomics.world; 98/101 datasets frozen >180d; watchdog + daily audit tool were still contacting it | R251, R259 |
| noaa | key `<station>:<element>` spans gsom+gsoy — 1,046,291 keys mix monthly+annual, invisible to (key,date) collision checks; was "SERVED" with only 10 D1 rows of 3,135,873; R2 coherence copy held 10 rows vs 3.1M local | R209, R224, R245 |
| ksh | RETIRED 2026-07-02 (ksh_stadat is the owner) and WITHDRAWN after I re-served it — do not resurrect; fetcher imports a deleted ingest; R2 objects remain but unreachable | R226 |
| zillow | RESTRICTED permission_required, recorded withdrawal — its 52 catalogue rows were the only live licence breach; keep gated | R213, R227 |
| istat | flows 183_207/183_277/183_285 REFUSED by derive (96.7M rows) and absent from summary JSON; paired-split ids like `183_277#X1360~IT111` work — don't invent test ids | R219, R220 |
| imf_* (8 direct) | IMF retired dataflow ids + codelists — titles must fall back to the raw key, never empty (694,300 blank rows shipped); unmapped==catalog-count fingerprint means grain mismatch, not missing catalogue | R221, R245 |
| imf_gfs*_direct | CI OOM was an iterparse parent-retention leak (fix: detach from parent), NOT a pyexpat 2 GiB ceiling — 2.29 GB parses in 137 MB | R223 |
| imf legacy (36), unctad (38), fao (18), unesco (4) | NOT fetcher work: publishers re-coded id schemes; slugged display-label keys (aggregator-era) may never reproduce — Ahmed's reserved decision (#46) | R253 |
| who_rs / who_hwf / who_sdg | migrated to WHO GHO exactly (publisher codes) — but indicator-level availability ≠ series-level: who_rs is 70.6%, not 100% | R252, R253 |
| fed_board | sidecar `dataset` column is a presentation grouping (cross-listed series) — measure ambiguity in observation files (29 keys, not 219); rows ≠ distinct keys | R214 |
| bea | resolver opens the whole `bea/` tree (913,230 series), not `bea.parquet` (17,699 — the fetcher's file); `Year=` is a comma LIST; key lives in `.env`; `_tree_frontier` must be blob-routed; 912,990 series still dark pending D1 capacity (#45) | R240–R242, R261, R268 |
| worldbank_wdi | accumulate-then-merge — was killed at 45 min every run and had NEVER stored anything; now self-bounds at 35 min; store contents predate the fetcher | R243 |
| dst | JSON-stat v1 dialect (id/size/role under `dimension`); `data_cadence` 84d vs `cadence` daily; DST_BUDGET_MIN=12 in CI; `_total_rows` scratch-mirror listing published a 40x-wrong self-count | R233, R238, R256, R264 |
| statfin / stat_estonia | self-bound 30 min + rotation resume; were pinned at the 45-min cap eating the budget while "safe" | R246 |
| cso (Ireland) | served year-9998 dates via first-match-wins time-dim (only ingester not on the shared resolver); the 11 bad files need a CLEAN RE-PULL, not merge; change-driven fetcher — deleting a parquet loses rows forever; sidecars are `_catalog.json`/`_collupd.json`, must be store-routed at BOTH ends | R265, R266, R272 |
| hagstofa | sentinels 3001–3004 sit ON the flagged time axis — parser was still broken after the "fix"; `3001M03` needed its own branch; annual periods stored as Dec-31 | R269, R256 |
| stat_slovenia | 05W0101S legitimately has NO time dimension (resolver returns None); historic 6152-12-31 dates = NASELJA settlement-code cardinality; genuine start 1857; scb genuinely starts 1749 | R269, R231 |
| bfs / abs / un_wpp | far-future dates are REAL projections (bfs 2150, abs 2046, un_wpp 2101) — do not "fix"; fabrication fingerprint is time-axis-in-key + sequential code runs | R267 |
| treasury | four DATELESS endpoints (null obs_date by design, identity = dimension columns) — dedup must treat null=null as same key or rows vanish (185→1) | R254 |
| census (EITS) | qtax variables are UPPERCASE (case-insensitive lookup required); `for=state:*` and `for=us:*` return different headers AND different key shapes; keyless API returns HTTP 200 + HTML "Missing Key"; `run_location: local` (key in .env) | R235–R237 |
| unesco_sdg | series title contains 🪵 — cp1252 console crash kills sync; set PYTHONIOENCODING=utf-8 | R234 |
| oecd / eurostat (via `_giant`) | `_max_obs_date` must use row-group statistics — oecd's largest flow file is 1.79B rows (>125 GB decoded); the old bare read silently triggered full-history re-pulls | R244 |
| riksbank / boc / tcmb / bcrp / scb | `_max_by_key` returns ISO STRINGS; riksbank's `isinstance(v, dt.date)` filter yielded an empty map → permanent silent `partial`; fix per-caller (uniform string-passthrough crashes riksbank's consumer) | R270 |
| sec_edgar | one id, two products: registry `sec_edgar` = UNSERVED 13f/insider giant (live:True, throws ArrowInvalid); the SERVED XBRL product is catalogued under `sec_edgar` but registry-named `sec_edgar_xbrl` (not live) and stays fresh only via sec-edgar-daily.yml — health gate and coverage audit are both wrong in opposite directions; repair is Ahmed's call (public ids) | R275, R276 |
| cbs_nl / gus_dbw | registered with `run_location: local` (not "missing from registry"); count as STRANDED, not scheduled — no adapter in the local run path | R262, R276 |
| ons_uk | crashed pass (0xC0000005) lost a whole run's state; cursor prune = delete-100% until one run completes and writes current cursors — sequence, don't guard harder | R208 "stamp", R263 |
| unhcr / bcb | merge AFTER the sweep loop — kill = total discard (worst class); hagstofa/stat_latvia/stat_slovenia/insee_bdm merge inside the loop (truncate only) — verified by control flow, not grep or AST counts | R249 |
| whr / transparency_ti / gpi | data actually retrieved from OWID fallbacks while cited to the primary publisher — provenance/licence exposure; check the ingest log's URLs | R215 "whr" |
| bis / bls / eia / faostat / vdem / statcan | workstation-scheduled via `run_location: local` despite `live:false` — reporting them "unscheduled" is the R262 error | R262, R234 "bypass" |

### From R277-R347

| source_id | warning | cite |
|---|---|---|
| sec_edgar | One id, two products: serving = XBRL company facts (17,276 series, refreshed by `sec-edgar-daily.yml` OUTSIDE the registry, with its own end-to-end parity gate); registry's `sec_edgar` = 13F/insider bulk trees, not catalogued. Serving reads `clean_grouped/`, not `clean_full/`. | (R275, R276, R289) |
| sec_edgar | Year-6 timestamp crash only on CI's pandas 3.x (`datetime64[us]` kept); invisible on dev's 2.3.3 — repro must build the us-array directly. | (R297, R312) |
| pwt / penn_world_table | `pwt` is SUPERSEDED by `penn_world_table` (PWT 11.0, current). Real defect: catalogue serves 7,159 rows for old 10.0 vs 60 for 11.0. | (R317) |
| ksh / zillow | `live: False` — retired; RED-UNRUN rows for them are display noise, not gate failures. D1 absence checks on them need a present control. | (R276, R338) |
| cbs_nl, gus_dbw | Registered with `run_location: local` — never expect them from the cloud pass. | (R276) |
| treasury | Dedup key is per-file `_identity_keys(["series_key","obs_date"] + out_cols)` — series_key is the ENDPOINT PATH; the default-pair uniqueness audit false-positives 166 files. `fbp_dpai_account_summary`'s `refusing shrink 185->1` was R254's null-conjunction drop — CLOSED 2026-08-31 (fix 756f502c9 predated the stale claim; 8 recent runs show zero); do NOT skip dateless endpoints (that broke two working ones, 161,664 rows). US-debt data from 1790 is real. | (R281, R305, R309 treasury, R322) |
| ofr | Keys on `(series_id, obs_date)`; local workstation state can be AHEAD of CI for it (shared R2 store, two state DBs). | (R281, R340) |
| worldbank_esg | Keys on `(country, obs_date)` — has no series_key column; audits that skip it as "lacking key columns" report a false clean pass. | (R281) |
| bea | Fetcher writes ONLY `clean_full/bea/bea.parquet` (<2% of source); the served subdirectory tree (NIPA/ etc.) is first-pass ingester output no running code merges — its 91 "under-keyed" files are prospective (#65), not live. Committing while a long local run executes from the same checkout causes old-module/new-caller crashes. | (R282, R297) |
| bds | 2023 vintage doesn't populate all 21 store columns — Census answers EMPTY, not nulls; the date tail returns zero and needs a per-vintage column set. Key needs `(series_key, obs_date, NAICS)`; intltrade's period-suffix rule does NOT generalise here. | (R283) |
| census (intltrade) | Store is a curated SUBSET of the API (imports/naics = 2% of API rows) — merge only keys already held and report skips; 11 of 24 flows were under-keyed; the "non-EITS files don't gain periods" exclusion was false (16 flows two months behind). | (R278, R280, R284) |
| cso | Daily (`2010M01D01`) and academic-year (`2003-2004`) grammars live in BOTH core/pxweb.py and cso's drifted private copy — the private one runs. Swapped-axis class: `TLIST(A1)=` in a series_key is the exact signature; repair at MATRIX grain by re-pull, never patch dates. 2030–2057 `Population_Projections` are legitimate. | (R299, R288, R327) |
| hagstofa | Positional time codes (values '0','1',… with year in valueTexts), sometimes period split across Year+Month dims — NOT the daily-grammar class; fixed 1188fb62. | (R300, R301, R304) |
| stat_slovenia | Bookmark is `_sweep_offset.json` (no `_rotation.json`, by design). 05W was a counter-from-year-1 file: 41,091 fake rows sat in 1900..2200 undetectable by any range test — re-pull, never band-pass. | (R285, R322, R334) |
| stat_estonia | Serial 45-min-cap victim: 18-min budget, per-sub-unit bookmark save, inner deadline + wind-back + table-grain bookmark (records last VISITED, not last succeeded); end-of-function `save_rotation` must not overwrite the wind-back. `Lepetatud_tabelid` = 99% discontinued at TABLE grain; its `Rahvastik.Arhiiv` 2080 frontiers are projections. | (R286, R298, R311 stat_estonia, R337) |
| statfin | Thin budget headroom (30/45); `12tc.px` read `Koulutus` education codes (0011..9999) as years once the time grammar failed. | (R286, R331) |
| scb | `Tid` grammars: weeks use `V` (`2025V01`) not `W`; multi-year windows `yyyy-yyyy` map to the OPENING year; unparsed Tid made Region municipality codes (0114..2584) the "years". Fix lives in BOTH ingest_scb.py and fetchers/scb.py; five tables quarantined in `_REGRAIN_QUARANTINE` until legacy-grain rows are pruned — enabling without pruning silently doubles the store. | (R331, R333, R334) |
| bcrp | `_max_by_key` returns strings; crash sites are CONSUMERS far from the call (`.isoformat()` at two points, `date > str` TypeError at bcrp.py:343 — different exception type from the known one); type hints lie. Business-day publisher. | (R310 _max_by_key, R321, R318) |
| boc / tcmb / riksbank | Same `_max_by_key` string class, fixed a1c42881 — stale state rows kept advertising it afterwards. | (R297, R302) |
| bls | `current_vintage()` raised for weeks (2-way unpack of 4-tuple) — CALL it, don't read it; `_bls_selftest.py` is not in CI and its fixture deltas are NOT production numbers; obs_age 63d is publication-in-arrears, not a stall; its CURSOR_CAP-adjacent `transient_unit` is a genuine failure, not a deferral. | (R292, R294, R310 count-cap) |
| ecb | `obs_count` flips to store-total on no-write runs (the "lost 168M rows"); budget-limited by design (35 min over 540 files); deferrals were mis-filed as transient failures. | (R326, R303) |
| abs / ssb | Same deferral-as-failure class (805/1222, etc.). abs really holds ~976M rows (verified via parquet footers; distinct-count still unverified). | (R303, R342) |
| ons_uk | `todo[:MAX_PER_RUN]` starved by 10 permanent non-publishers (parser gap) — rotation now required; quarter periods map to the FIRST month (match `parse_ons_period`), `mmm-mmm-yyyy` rolling windows exist; fetcher had been UNDOING the approved re-key on four ashe datasets (grain doubling to 20.2M rows); `/versions/{n}/csv` 404s — use the `downloads` href (download.ons.gov.uk); ONS rate-limits sweeps (429). | (R313, R314, R315, R333) |
| eurostat | `rekey_eurostat.py --dry-run` writes NOTHING — read ARGV; conflicting-revision count is the safety number (identical-dup vs real revision); the guard sampling the first 5 of a sorted list is disarmed by a partial --apply; measurement must run with `AQUEDUCT_BACKEND=r2` (local gave the opposite answer); its `LAST UPDATE=` keys are a distinct impossible-date cause from the positional class. | (R295, R296, R323, R320) |
| wid | Six 47-byte header-only upstream files are EMPTY, not schema breaks — fixed 691e6126; audit_stale_errors must show more than `git log -1` to find it. | (R304) |
| insee_melodi, bis, ember, fed_board, idb, ipea, stats_nz, wikidata, zillow, cso | The "deferral spelled differently" half of the class — `if dl.spent():` followed by a bare `transient_unit(code)` with "deferred" only in the print. Several files contain the same call twice; only one is the deferral. | (R306) |
| who_gho | FALSE positive of the deferral sweep — its deadline block `break`s with no tally; the nearby `transient_unit` is a genuine handler. | (R306) |
| usda | Its `save_rotation` bookmark means "cursor window DELIVERED in the Result", not "work persisted" — moving it into the loop would silently skip up to 50,000 cursor keys. Leave it. | (R287) |
| noaa | Source-scoped derives full-scan the 8.5 GB catalog (no index on `series(source_id)`) — minutes of "silence" before first output; store outgrew the catalogue (1,998 underived series) and any catalog-drawn sample can't see them; `catalog_noaa.py` projects from sidecars, so order is derive → verify → catalogue; re-derives need `--skip-newer-than`. | (R308 noaa, R341, R307) |
| adb | Genuinely stuck rotation (2 write-time blocks, 44 of 54 flows on a fixed prefix) — behaviour test, not filename test, found it. | (R285) |
| imf_* family | 36 `imf_*` sources ≠ task #46's reserved 8 (old-id question only — `_direct` successors are buildable); `imf_weo` has its own live fetcher, don't duplicate it; `_direct` `dims.json` sidecars must go through `blob` at the parquet's path (bop: 7 key parts vs 5 dims, inference fails; cpi: inference mistitled all 27,094); worker deploy is MANUAL, D1 sync must emit `source` rows, and registry adds require the EXPECTED_SOURCE_COUNT bump. | (R343, R344, R345, R346, R347) |
| dbnomics | BANNED host; `connectors/dbnomics/connector.py` now raises ImportError (keep it that way); `jobs/ingest_all.py` runs every listed connector; 1,236 series still carry `dbnomics_path` mirror URLs in the catalog (open, fix via catalog refresh). | (R311 repo-wide) |
| gapminder, un_wpp, fao_*, imf_weo, boc | Forecast horizons to 2031–2101 are REAL data — sane-date ceiling is 2126; a today-derived bound erased 636,021 series' coverage. | (R235) |
| oecd | In the impossible-date class via SDMX (25,160 rows) — the class was never PxWeb-only. | (R320) |
| cnb, frankfurter, nyfed, riksbank | Business-day publishers: the gate already uses `_business_age_days` for cadence "daily" — do not add calendar-day tolerances on top. | (R318) |

{
  "name": "elkassabgidata-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "Hosted 'ask the data' chat for the ElkassabgiData family — grounded over the existing free endpoints, DeepSeek-backed, with anonymous chat + registration-gated downloads.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.20.0"
  }
}
